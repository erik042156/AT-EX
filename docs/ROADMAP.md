# AT-EX 프로젝트 실행 로드맵 (ROADMAP)

## 1. 문서 개요

이 문서는 AT-EX 프로젝트(Python + Selenium + pytest 기반 웹 UI 자동화 테스트 프레임워크, 대상 사이트: [Automation Exercise](https://automationexercise.com/))의 **실행 계획서**입니다. `docs/prd/` 하위 PRD 문서에 이미 확정된 요구사항을 재정의하지 않으며, "지금 무엇을 해야 하는가"와 "그 작업의 완료 기준"에만 집중합니다. 개발팀은 이 문서를 매일 열어보며 진행 상황(착수/완료/블록)을 체크리스트로 관리합니다.

### 기준 문서 및 버전
| 문서 | 버전 |
|------|------|
| `docs/prd/project-prd.md` | v1.5 |
| `docs/prd/features/signup.md` | v1.4 |
| `docs/prd/features/login.md` | v1.4 |
| `docs/prd/features/logout.md` | v1.5 |
| `docs/prd/features/product_search.md` | v1.6 |
| `docs/prd/features/shopping_cart.md` | v1.4 |
| `CLAUDE.md` | v1.0 (프로젝트 공통 개발 규칙, §6 "Playwright MCP 기반 페이지 탐색 규칙" 포함) |
| `shrimp-rules.md` | 버전 미표기 (AI Agent 운영 규칙, §5 "Playwright MCP 기반 Locator 조사 절차" 포함) |

### 작성/갱신 이력
- **작성일**: 2026-07-24
- **최종 갱신일**: 2026-08-03
- **상태**: shrimp-task-manager MCP의 Task 실행 이력(T1~T19 completed)을 저장소 실제 파일 상태와 교차검증하여 진행 상태를 전면 재동기화(v1.3). Phase 0 전체와 Phase 1의 1-A/1-B/1-C가 실제로 구현·테스트 작성·PASS까지 완료되어 있음을 확인하고 체크박스·진행률·상태를 갱신했습니다. Phase 1의 1-D는 `pages/products_page.py` 구현까지만 완료되어 있어(v1.2에서 이미 확인) 아직 진행중이며, `test_data/search_keywords.json`과 `tests/test_product_search.py`는 저장소에 존재하지 않음을 확인했습니다. 이전 정책 변경(v1.2, Locator 확보 절차)에 대한 설명은 변경 이력 섹션 8을 참고하십시오. 이번 재동기화는 Phase 0/1(1-D 일부 제외)에 한정되며, Phase 2/3과 섹션 5 블로커 트래킹 표는 손대지 않았습니다.
- **[v1.9 추가]** 섹션 5의 "회원가입 테스트 고정 이메일 중복" 블로커를 실제 코드 수정으로 해결함: `utils/helpers.py`에 `generate_random_email()` 추가, `conftest.py`의 `valid_signup_data`/`relogin_signup_data` fixture가 매 실행마다 새 이메일을 동적 생성하도록 변경하고 teardown 계정 정리 안전장치(`_cleanup_signup_account`)를 추가함. 로컬에서 `test_signup.py` 단독 3회 연속 2/2 PASS, 전체 테스트(`tests/`) 20개 회귀 없음으로 검증(상세는 섹션 3 Phase 3 Task4, 섹션 5 참고). 이 변경은 로컬 검증만 완료된 상태이며 아직 커밋/푸시 및 GitHub Actions 재확인 전입니다. 별도로 `SLACK_WEBHOOK_URL` 저장소 시크릿 등록 여부를 `gh api repos/erik042156/AT-EX/actions/secrets`로 재확인했으나 `total_count: 0`으로 **여전히 미등록 상태**이며 변경 사항이 없어 해당 블로커는 그대로 유지됩니다.
- **[v1.10 추가]** v1.9의 이메일 중복 수정(커밋 `30a3db7`)과 로드맵 갱신(커밋 `0ce1023`)을 push한 뒤 실제 GitHub Actions를 재실행(run_id `30346729676`)해 최종 확인함. **이메일 중복 문제 자체는 해결이 실제로 검증됨**: chrome/firefox 두 job 모두 `test_signup.py` 실패 지점이 기존의 "`/login`에서 `Email Address already exist!`로 즉시 막힘"이 아니라, 회원가입이 정상 성공(`ACCOUNT CREATED!`)한 **이후**의 후속 단계(Continue 클릭, Delete Account 클릭, 재로그인 후 로그아웃 링크 대기)로 이동했고, 다운로드한 페이지소스에서 `already exist` 문자열이 0건임을 grep으로 확인함. 다만 **여전히 chrome/firefox 모두 2 failed, 18 passed**로 CI는 실패 상태이며, 원인은 이메일 중복과는 무관한 **새로운 종류의 광고 오버레이 차단**으로 확인됨: chrome은 "Powered by Google" 설문조사 전면 오버레이가 Delete Account 확인 메시지/로그아웃 링크 영역을 가림(`h2[data-qa='account-deleted']`, `a[href='/logout']` 대기 타임아웃), firefox는 "Now Withdraw Without Waiting"(Take Profit Trader) 광고 팝업이 계정 생성 완료 페이지의 "Continue" 버튼을 가려 클릭이 진행되지 못함(`a[href='/logout']` 대기 타임아웃으로 이어짐). 두 브라우저에서 광고의 실제 내용은 다르지만 "제3자 광고/설문 오버레이가 버튼·링크를 가려 클릭이나 대기가 실패한다"는 동일한 성격의 문제로 판단됨. 사용자가 "발견한 문제를 로드맵에 업데이트"로 명시적으로 결정하여 **이번 세션에서는 오버레이 처리 로직을 코드로 구현하지 않고 문서화만 수행**함(섹션 5 신규 블로커, 섹션 7 다음 액션 참고).
- **[v1.11 추가]** v1.10 섹션 5의 "(신규, v1.10, 최우선)" 광고/설문 오버레이 블로커를 실제 코드 수정 3커밋으로 상당 부분 개선함: (1) `pages/base_page.py`의 `click_when_clickable()`이 클릭 직전 `document.elementFromPoint`로 클릭 좌표를 가로채는 요소를 감지해 JS 클릭으로 우회하도록 수정(커밋 `fa91e03`), (2) `wait_for_element_presence()`를 5초 청크 폴링 + 청크 타임아웃마다 전면 오버레이(화면 50% 이상을 덮는 `position: fixed`/`absolute` 요소)를 JS로 제거하는 방식으로 수정(커밋 `5d2d44a`), (3) `config/settings.py`의 `PAGE_LOAD_TIMEOUT`을 20초 → 35초로 상향(커밋 `67249c2`, `docs/prd/project-prd.md` v1.4 §11.4에 이미 반영됨). 3커밋을 각각 push한 뒤 GitHub Actions로 순차 검증(run_id `30540357346` → `30543091405`(+rerun) → `30544240887`)한 결과, Chrome 기준 `test_signup.py` 실패가 2/2 → **1/2로 감소**했고 실전 오버레이 우회 성공 사례(`blocker=<iframe>`가 Delete Account 링크를 가리는 것을 감지해 JS 클릭으로 우회)도 로그로 직접 확인됨. 다만 `test_signup_with_valid_info` 1건은 35초로 늘린 뒤에도 여전히 간헐적으로 타임아웃되어 **완전히 해결되었다고 표기하지 않음**. 이 검증 과정에서 사용자가 **Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정함(최종 CI 실행에서 Firefox가 7 failed·19분 이상 소요, 전부 `ReadTimeoutError(read timeout=120)` 등 WebDriver 프로토콜 레벨 무응답으로 페이지·오버레이와 무관한 인프라 문제로 판단). 이 결정은 `docs/prd/project-prd.md` v1.4(§11.2, §11.4, §15.3, §16.2, §17.1, §18.2)에 이미 반영되어 있으며, 이번 갱신으로 `docs/ROADMAP.md`의 기준 문서 버전 표와 관련 섹션(2, 3, 5, 6, 7)을 동기화함. Firefox 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job) 자체는 이번 세션에서 아직 제거하지 않았으며, 이는 다음 액션(섹션 7)에 별도 정리 대상으로 남겨둠. 상세는 섹션 2·3·5·6·7·8 참고.
- **[v1.12 추가]** v1.11에서 다음 액션 2번으로 남아있던 "Firefox 관련 코드 정리"를 사용자의 명시적 요청("Firefox가 검증 범위에서 제외되게 관련 코드도 모두 수정")에 따라 실제로 완료함: 커밋 `c473106`으로 `conftest.py`의 `pytest_addoption`(`--browser` 옵션)·`FirefoxOptions` import를 전부 삭제하고 `driver` fixture를 Chrome 전용으로 단순화(더 이상 필요 없는 `request` 파라미터도 제거), `.github/workflows/test.yml`의 `strategy.matrix.browser=[chrome, firefox]`/`fail-fast: false`를 삭제해 matrix 없는 단일 `test` job으로 정리하고 아티팩트명·Slack 알림 문구에서도 브라우저 접미사를 제거함. 로컬에서 `python -m py_compile conftest.py` 통과, `pytest --collect-only` 20개 정상 수집, `pytest tests/test_smoke.py` 2개 PASS, YAML 파서로 workflow 문법 검증까지 완료함. 이 코드 정리 커밋(`c473106`)과 v1.11 반영분 docs 커밋(`ad6fae0`)을 함께 push한 뒤 실제 GitHub Actions로 최종 검증(run_id `30549234555`)한 결과 **`20 passed in 111.61s`, 실패 0건**을 확인함(job 이름도 `test (chrome)`/`test (firefox)`에서 단일 `test`로 변경 확인). 이로써 v1.10에서 발견된 광고/설문 오버레이 블로커가 **완전히 해결**된 것으로 확정함(`test_signup.py` 2건 포함 20/20 전체 통과). `SLACK_WEBHOOK_URL` 미등록 문제 자체는 이번 실행에서 실패가 없어 Slack 알림 스텝이 스킵되었을 뿐 여전히 잠재적으로 남아있으며 변경 사항은 없음. 상세는 섹션 2·3·5·6·7·8 참고.
- **[v1.13 추가]** 사용자가 "프로젝트에 이미 Slack hook이 설정되어 있다"고 알려와 확인한 결과, `.claude/hooks/notify-ci-slack.sh`(로컬 Claude Code hook, `git push` 후 CI 결과를 감시해 Slack 전송)는 이미 동작 중이었으나, `.github/workflows/test.yml`의 `Notify Slack on failure` 스텝(`8398a7/action-slack@v3`)이 참조하는 **GitHub 저장소 시크릿 `SLACK_WEBHOOK_URL`은 여전히 미등록**(`gh api repos/erik042156/AT-EX/actions/secrets` → `total_count: 0`)임을 재확인함. 사용자 승인 하에 `gh secret set SLACK_WEBHOOK_URL --repo erik042156/AT-EX`로 **실제 등록 완료**(`total_count: 1`로 확인). 이어서 사용자가 "실패 시 Slack 메시지가 git 커밋 메시지만 보여줄 뿐 실패 지점을 알 수 없다"고 지적해, `.github/workflows/test.yml`을 개선함: pytest 출력을 `reports/pytest_output.log`로 저장하고, 실패 시(`if: failure()`) `FAILED ` 로 시작하는 pytest의 short test summary info 줄만 추출해 `GITHUB_ENV`의 `FAILED_TESTS`로 저장한 뒤 Slack 메시지 `text`에 포함시키도록 수정(커밋 `023dd4e`). 검증을 위해 항상 실패하는 임시 테스트(`tests/test_slack_notification_check.py`)를 추가해 push했고(커밋 `480d218`), 그 결과 GitHub Actions의 단일 라인 `run:`이 `bash -e {0}`로만 실행되어(`pipefail` 미적용) `pytest | tee`의 종료 코드가 `tee`의 0으로 대체되는 바람에 **pytest가 실제로는 `1 failed, 20 passed`였음에도 워크플로우 단계가 "성공"으로 처리되어 알림 로직 전체가 실행되지 않는 버그**를 발견함(run_id `30691136890`). `set -o pipefail`을 명시한 멀티라인 `run:` 블록으로 즉시 수정(커밋 `7c1a74c`)했고, 재검증(run_id `30691221750`)에서 `Extract failed test summary`·`Notify Slack on failure` 두 스텝이 모두 정상 실행되며 `FAILED_TESTS` 환경변수와 Slack 메시지 `text`에 `FAILED tests/test_slack_notification_check.py::test_deliberately_fail_for_slack_notification_check - AssertionError: ...` 문자열이 정확히 포함된 것을 로그로 확인함(**기능 검증 완료**). 이후 임시 테스트를 제거(커밋 `6e3fd80`)했으나, 예상과 달리 CI가 다시 실패함(run_id `30691294540`) — `tests/test_signup.py::test_signup_with_valid_info`/`test_signup_then_logout_and_relogin`이 `home_page.click_delete_account_link()`(51번 줄) 대기에서 `TimeoutException`으로 실패했고, `gh run rerun --failed`로 2회 재실행해도 **동일 지점에서 3회 연속 재현**됨. 이는 v1.12에서 "완전 해결"로 확정했던 광고/설문 오버레이 문제와 같은 계열이나, 간헐적이 아니라 연속 재현된다는 점에서 사이트 측 광고 패턴이 다시 바뀌었을 가능성이 있는 **새로운 회귀**로 판단됨. 사용자가 "지금은 ROADMAP에 기록만" 하기로 결정해 이번 세션에서는 원인 조사·수정에 착수하지 않음. 이 근거로 섹션 2(Phase 3 행)·섹션 3(Task4, Phase 3 산출물)·섹션 5(`SLACK_WEBHOOK_URL` 블로커를 해결 완료로 갱신 + `test_signup.py` 오버레이 재발 블로커 신규 추가)·섹션 6(§14.2를 다시 미충족으로 갱신)·섹션 7(다음 액션 갱신)을 반영함. 현재 main 브랜치 HEAD(`6e3fd80`)는 이 새 블로커로 인해 CI가 **실패 상태**로 남아있음(run_id `30691294540`). 상세는 섹션 2·3·5·6·7·8 참고.
- **[v1.14 추가]** 사용자가 "실패 시 Slack 메시지가 `실패위치 : example.py ( "203줄" )` / `실패원인 : AssertionError` 포맷으로 오게 해달라"고 구체적 포맷을 지정함에 따라, v1.13에서 도입한 `FAILED <파일::함수> - <예외 전체 메시지>` 요약 방식을 교체함. `.github/scripts/extract_failed_tests.py` 신규 추가(커밋 `a6ccf54`): pytest 트레이스백을 테스트별 블록으로 나눈 뒤 (1) 정규식 `^(tests/\S+\.py):(\d+):`로 테스트 파일 자신의 호출 지점(라이브러리 내부까지 들어가기 전 줄)을 찾아 "실패위치"로, (2) 정규식 `^E\s+(\S+):`로 첫 예외 표현 줄을 찾아 모듈 경로를 제외한 클래스명만 "실패원인"으로 추출해 `FAILED\n실패위치 : <파일> ( "<줄>줄" )\n실패원인 : <예외클래스>` 블록을 테스트마다 생성(여러 실패는 빈 줄로 구분). `.github/workflows/test.yml`의 "Extract failed test summary" 스텝이 `grep` 대신 이 스크립트를 호출하도록 변경. 검증 방법으로 실제 CI 실패 아티팩트 2종을 `gh run download`로 받아 로컬에서 스크립트를 직접 실행함: (a) run_id `30691294540`의 중첩 호출 실패(Selenium `TimeoutException`) 로그 → `tests/test_signup.py ( "51줄" )`/`TimeoutException` 정상 추출, (b) run_id `30691221750`의 테스트 함수 내부 직접 `assert` 실패 로그 → `tests/test_slack_notification_check.py ( "8줄" )`/`AssertionError` 정상 추출. push 후 실제 CI(run_id `30691891358`, main HEAD가 여전히 겪고 있는 오버레이 회귀로 인해 실제로 실패)에서 Slack `text`에 실제로 담긴 내용을 로그로 확인함 — `FAILED\n실패위치 : tests/test_signup.py ( "42줄" )\n실패원인 : TimeoutException`과 두 번째 실패 블록이 빈 줄로 구분되어 정확히 포함됨(**요청 포맷과 완전히 일치, 실제 CI 실패로 검증 완료**). 이번 실행에서 실패 줄 번호가 v1.13의 51번에서 42번으로 달라진 점은 동일 테스트 내에서도 실패하는 assertion 지점이 매번 다를 수 있음을 보여주는 추가 정황이나, 이 회귀 자체의 원인 조사는 이번 세션 범위가 아니므로 착수하지 않음. 이 근거로 섹션 2(Phase 3 행에 v1.14 반영)·섹션 3(Task4, Phase 3 산출물에 `.github/scripts/extract_failed_tests.py` 반영)·섹션 5(오버레이 재발 블로커 행에 새 실패 줄 번호 정보 추가)·섹션 7(다음 액션의 SLACK_WEBHOOK_URL 해결 완료 항목에 포맷 개선 내용 추가)을 갱신함. 상세는 섹션 2·3·5·7·8 참고.
- **[v1.15 추가]** v1.13~v1.14에서 연속 재현되던 `test_signup.py` 광고 오버레이 회귀의 **근본 원인을 스크린샷 분석으로 확인**함: `pytest_runtest_makereport` hook이 저장한 실패 시점 스크린샷(`test_signup_with_valid_info_failed_2026-08-02_18-26-37.png`)을 보면, 화면은 여전히 홈페이지(헤더에 "Delete Account" 링크가 그대로 노출됨, `/delete_account`로 이동하지 않음)였고 그 위를 신규 전면 광고(Sudden Attack 게임 영상 팝업, `aswift_2` 광고 iframe, `position:absolute; width:100vw; height:100vh`)가 덮고 있었음. 즉 `click_delete_account_link()`의 클릭이 광고 스크립트에 가로채여 **실제 페이지 이동(navigation) 자체가 발생하지 않은 것**이 확인됨 — 이 경우 오버레이를 아무리 제거해도 이미 유실된 클릭은 되살아나지 않으므로, "클릭 1회 후 목적지 요소만 반복 대기"하던 기존 구조로는 근본적으로 해결이 불가능함을 실증함.

  이 원인 분석에 앞서, 사용자가 "광고 패턴이 계속 바뀌는 것을 인지해 20회 시도 후 정의되어 있지 않은 코드 로케이터를 추가"하라고 지시함에 따라(shrimp-task-manager MCP로 Task를 분해해 실행), 1차로 `pages/base_page.py`의 오버레이 완화 로직을 확대함: 시간 청크 기반(`_OVERLAY_DISMISS_CHUNK_SECONDS=5`) 폴링을 최대 20회 시도 기반(`_MAX_OVERLAY_DISMISS_ATTEMPTS=20`, `chunk=max(timeout/20, 0.5초)`, 기존 timeout 예산은 그대로 유지)으로 교체하고, 오버레이 탐지 스크립트(`_dismiss_overlays`, 기존 `_dismiss_full_page_overlay`를 대체)에 z-index 999 초과(포지셔닝 요소 한정, 오탐 방지)·뷰포트 30% 이상 iframe·close/×/skip/닫기 텍스트 패턴 클릭 휴리스틱을 추가함. 이 확대 로직을 `click_when_clickable`/`find_element_visible`/`wait_for_text`/`wait_for_element_invisible`/`wait_for_element_presence` 5개 메서드 전부(BasePage 전체)로 적용함(시그니처 변경 없음). 1차 수정만으로 로컬 검증(`test_signup.py` 4회 반복)에서 5회 PASS/3회 FAIL로 일부 개선은 있었으나 완전 해결에는 이르지 못했고, 실패 시점 스크린샷을 분석해 위의 "클릭 유실" 근본 원인을 발견함.

  이에 따라 2차로 `pages/base_page.py`에 `click_and_wait(click_locator, condition, timeout)` 공용 메서드를 신설함: 대기 실패마다 **오버레이 제거 → 링크 재클릭(`click_when_clickable`) → 재대기**를 최대 20회 반복하도록 해, 클릭 자체가 광고에 가로채여도 다음 재시도에서 오버레이가 제거된 상태로 다시 클릭을 시도함. 이 패턴이 반복되던 3곳(`account_created_page.py::click_continue_button`, `home_page.py::click_delete_account_link`, `home_page.py::click_logout_link`)을 모두 이 메서드로 교체함(공개 메서드 시그니처는 무변경, CLAUDE.md §19.3 "2회 이상 반복 코드는 공통 메서드로 분리" 기준에 부합).

  **검증**: 1차+2차 수정 반영 후 로컬에서 `pytest tests/ -v` 20개 전체 PASS(회귀 없음) 재확인. `test_signup.py`만 총 6회 연속 재실행(서브테스트 12개) 결과 **전부 PASS**(2차 수정 전 8회 중 3회 실패와 대비되는 뚜렷한 개선). 커밋(`2a1d1bf`)·push 후 실제 GitHub Actions로도 재검증(run_id `30742305431`)해 **`test` job 성공, pytest 스텝 성공**(실패 없어 `Extract failed test summary`/`Notify Slack on failure` 스텝은 스킵됨)을 확인함. 다만 이 문제는 광고 노출이라는 외부 요인에 근본적으로 기인하므로, 이번 검증(로컬 6연속 + CI 1회)만으로 향후 완전히 재발하지 않는다고 100% 단정하지는 않음 — 다만 v1.13~v1.14 시점보다 재시도 폭(20회)과 대응 방식(클릭 자체 재시도)이 구조적으로 훨씬 강화되어, 유사한 광고 하이재킹이 재발하더라도 자동 복구될 가능성이 크게 높아짐. 이 근거로 섹션 2(Phase 3 행)·섹션 5(오버레이 재발 블로커를 해결로 갱신)·섹션 6(§14.2를 충족으로 갱신)·섹션 7(다음 액션 갱신)을 반영함.

  이 커밋을 push한 직후, ROADMAP.md만 수정한 후속 docs 커밋(`0049cfc`, 코드 변경 없음)의 CI(run_id `30742482848`)에서 **16 failed/2 passed/2 errors**의 대량 실패가 발생함 — 첫 테스트인 `test_login`의 `login_page.enter_email()`부터 타임아웃되는 등 v1.7~v1.8에서 조사했던 "CI 환경 자체의 구조적 이슈" 가설과 정확히 같은 패턴이었음(`curl` 리치빌리티는 HTTP 200으로 정상). 코드 변경이 전혀 없는 커밋에서 발생했으므로 이번 코드 수정과는 무관함이 명확함. `gh run rerun`으로 같은 커밋을 재실행하니 1 failed/19 passed(무관한 `test_shopping_cart.py` 1건만 실패)로 완전히 다른 결과가 나왔고, 그 1건도 `gh run rerun --failed`로 재실행하니 통과해 최종적으로 main HEAD가 그린 상태가 됨. 즉 "CI 환경 자체의 간헐적 구조적 이슈"는 완전히 배제할 수 없는 실제 현상으로 재확인되었으나(섹션 7 4번 항목 갱신), 재실행으로 매번 복구되는 일시적 현상이라는 점도 함께 확인됨. 상세는 섹션 2·3·5·6·7 참고.

- **[v1.16 추가]** v1.8~v1.15에서 "원인 미확정"으로 남아있던 CI 환경(GitHub Actions)의 간헐적 대량 실패(16 failed/2 passed/2 errors 패턴)의 **정체를 실측으로 확정**함: 커밋 `c5348b0`(pytest 재시도 3회 + 리치빌리티 체크의 챌린지 문구 탐지)를 먼저 push한 뒤, `gh run rerun`으로 최대 10회 CI를 반복 실행하며 관찰한 결과 3회차(run_id `30744291712`, job `91488177524`)에서 실제로 재현됨 — 리치빌리티 체크의 `::warning::`이 실제 트리거되었고(`##[warning]` 어노테이션으로 확인), `gh run download`로 받은 실패 스크린샷이 전부 동일 파일 크기(26313 bytes)였으며 직접 열어본 결과 `Please wait while your request is being verified...`(Cloudflare 챌린지 페이지)였음. **핵심 발견**: 이 차단은 순간적 히트가 아니라 **job 시작(10:55)부터 종료(11:05)까지 약 10분간 끊김 없이 지속**되었고, 그 사이 pytest 재시도 3회(각 30초 대기)가 전부 동일하게 `16 failed, 2 passed, 2 errors`로 실패함 — 같은 job(러너)은 egress IP가 고정되므로 step-level 재시도로는 지속형 IP 차단을 극복할 수 없음을 실증함(반면 완전히 새로운 워크플로우 실행 1·2회차는 새 러너를 받아 곧바로 통과함).

  이 근본 원인(IP 고정)을 직접 겨냥해 2가지 대응을 설계·구현함. **(A) Job 체이닝**: `.github/workflows/_run-e2e-tests.yml`(신규, 재사용 워크플로우)로 기존 checkout~pytest~아티팩트 업로드 스텝을 분리하고, `.github/workflows/test.yml`을 `attempt-1`→`attempt-2`(`needs.attempt-1.result == 'failure'`일 때만)→`attempt-3`(notify_on_failure) 3개 job 체이닝 구조로 재편함 — 각 job이 새 러너(새 IP)를 받으므로 지속형 차단에도 회복 가능해짐. 아티팩트명은 `reports-attempt-N`/`screenshots-attempt-N`으로 분리하고, Slack 알림·실패 요약 추출은 마지막 attempt-3 실패 시에만 실행되도록 해 중간 재시도 단계의 알림 노이즈를 방지함. **(B) Fail-fast 사전 체크**: `conftest.py`에 `pytest_sessionstart` 훅을 추가해 Selenium 드라이버 기동 전 `urllib.request`로 BASE_URL을 가볍게 확인하고, 챌린지 문구(`cloudflare`/`one moment, please`/`checking your browser`/`verify you are human`) 감지 시 `pytest.exit(returncode=75)`로 세션을 즉시 중단함(신규 의존성 없이 표준 라이브러리만 사용) — 이전에는 차단 상태에서도 매 재시도가 전체 20개 테스트를 3분씩 완주하고서야 실패했던 것을, 수 초 내 실패로 단축시켜 job 재시도 사이클을 빠르게 함.

  **검증**: 로컬에서 YAML 파서로 문법 검증, `pytest --collect-only`로 정상 사이트 대상 20개 정상 수집(사전 체크가 정상 케이스를 막지 않음 확인), `unittest.mock`으로 챌린지 응답을 흉내내 `pytest_sessionstart`가 실제로 `pytest.exit`를 트리거하는지 단위 검증, `tests/test_smoke.py` 스모크 테스트로 회귀 없음 확인함. 커밋 `2fba20a`로 push한 뒤 실제 CI(run_id `30747736911`)에서 **happy path 정상 동작을 확인**함 — `attempt-1` 성공 시 `attempt-2`/`attempt-3`가 `needs.attempt-1.result == 'failure'` 조건에 따라 정확히 `skipped` 처리되어 전체 워크플로우가 `success`로 종료됨. 이어서 추가로 10회 더 `gh run rerun`을 반복했으나(총 20회) 이번에는 Cloudflare 차단이 재현되지 않아 `attempt-1`이 매번 성공했고, 따라서 **"attempt-1 실제 실패 → attempt-2가 새 러너로 기동해 복구"라는 마지막 한 조각은 라이브로 검증하지 못함**(8회차만 다른 회차 대비 유독 오래 걸려(~6.5분) step-level 재시도가 조용히 흡수했을 가능성은 있으나 상세 로그까지 파고들지는 않음). 사용자가 "현재 검증 수준으로 마무리"를 명시적으로 결정함 — job 체이닝의 조건부 skip/실행 로직 자체는 GitHub Actions 표준 `needs.result` 평가 방식이라 구조적으로 신뢰할 수 있고, fail-fast 훅은 단위 검증으로 트리거 확인이 끝난 상태이므로, "실제 지속형 차단 상황에서의 end-to-end 자동 복구"만 다음 재발 시 자연 검증되는 채로 남겨둠.

  이 근거로 섹션 2(Phase 3 행)·섹션 3(Task4 CI 최종 점검 문단, Phase 3 산출물 목록에 `.github/workflows/_run-e2e-tests.yml` 신규 및 `conftest.py` 확장 반영)·섹션 5(CI 대량 실패 블로커 행을 "근본 원인 규명 + 대응 구현 완료, end-to-end 복구는 미검증"으로 갱신)·섹션 7(다음 액션 4번을 갱신)을 반영함. 상세는 섹션 2·3·5·7 참고.

- **[v1.17 추가]** 섹션 7 다음 액션 1번으로 남아있던 "README.md 작성 여부 및 시점 결정"에 대해, 사용자가 두 남은 항목(README.md 작성 vs 세션 타임아웃 확정) 중 README.md 작성을 먼저 진행하기로 선택함에 따라 저장소 루트에 `README.md`를 신규 작성함. 프로젝트 개요, 기술 스택, 디렉터리 구조, 설치 방법, `.env` 환경변수 설명, `pytest` 실행 방법(전체/개별/스모크/HTML 리포트), 테스트 파일별 커버 기능 목록, CI/CD(job 체이닝 구조) 요약, `CLAUDE.md`/`docs/prd/`/`docs/ROADMAP.md` 참조 링크를 포함함(project-prd.md §14.4 "README.md 포함(프로젝트 개요, 실행 방법)" 성공 기준 근거). 이 변경으로 v1.7~v1.16에서 계속 §14.4 미충족의 근거로 남아있던 README.md 부재 문제가 해소되어, **project-prd.md §14(전체 성공 기준) 5개 항목(§14.1~§14.5)이 모두 충족 상태로 전환됨**(§14.1·§14.3은 기존부터 충족, §14.2·§14.5는 v1.15/v1.13에서 이미 충족, §14.4는 이번에 충족). 다만 Phase 3에는 §14 충족 여부와 별개로 "로그아웃 세션 만료 시나리오 검토"가 세션 타임아웃 값 미확정(project-prd.md §19.1)으로 여전히 보류 상태로 남아 있어, **Phase 3 전체를 ✅ 완료로 표기하지는 않음**. 이 근거로 섹션 2(Phase 3 행 진행률·근거 갱신), 섹션 5(README.md 블로커 행을 해결 완료로 갱신), 섹션 6(§14 최종 점검 항목에 v1.17 addendum 추가, 다만 세션 타임아웃 보류로 체크박스 자체는 유지), 섹션 7(다음 액션 1번을 완료로 갱신, 2번(세션 타임아웃)만 유일한 남은 액션으로 재정렬)을 반영함. 실제 파일(`README.md`)은 이번 세션에서 직접 작성함(아직 커밋/push 전).

- **[v1.18 추가]** Phase 3의 마지막 남은 블로커였던 "세션 타임아웃 값 확정"(project-prd.md §19.1)을 실측으로 해소함. 사용자 승인 하에 Playwright MCP로 `automationexercise.com`을 직접 조사함(CLAUDE.md §6 절차 준수): 로그인 전에는 `sessionid` 쿠키가 존재하지 않고(`csrftoken`만 존재), `automationexercise.com` 쿠키를 초기화한 뒤 테스트 계정(`shared_login_accounts.valid_account`)으로 실제 로그인해 로그인 클릭 직전 시각과 로그인 직후 발급된 `sessionid` 쿠키의 `expires` 값을 직접 비교한 결과 **정확히 1,209,600초(14일)**로 확인됨 — Django 프레임워크의 기본 `SESSION_COOKIE_AGE`(2주)와 소수점 단위까지 정확히 일치하며(로그아웃 기능 PRD가 이미 확인해둔 이 사이트의 Django 3.2.8 사용 사실과도 정합적), 로그인 폼에 세션 기간에 영향을 줄 수 있는 별도 옵션(로그인 상태 유지 등)이 없어 모든 로그인에 동일 적용됨을 확인함. 다만 이 방식은 브라우저 쿠키 만료 시각 실측 기준이라 서버 측 idle timeout이나 세션 스토어 만료 존재 여부, sliding expiration 여부까지 완전히 배제하지는 못하는 한계를 문서에 명시함. 값 확정 후 "이 값을 근거로 `logout.md`의 '세션 만료 후 로그아웃 시도' 시나리오를 자동화할지"를 사용자에게 물었고, 사용자는 **"값만 문서에 확정하고 자동화는 영구 제외"**를 명시적으로 선택함(사유: 14일 실제 대기 E2E는 CI에서 비현실적이고, 쿠키 조작으로 만료를 시뮬레이션하는 대안도 `logout.md`의 기존 LOGOUT-REQ-005 "세션/쿠키 값 자체는 검증 대상 아님" 원칙과 배치되어 채택하지 않음). 이 결정을 `automation-prd-writer` 서브에이전트에 위임해 PRD 문서에 정식 반영함: **`docs/prd/project-prd.md`**(v1.4 → v1.5) §19.1의 "세션 타임아웃 시간은?" 항목을 취소선 처리 후 확인 완료로 갱신, **`docs/prd/features/logout.md`**(v1.4 → v1.5) §9 흐름2("값 미확정이라 제외" → "값은 확정되었으나 영구 제외로 재작성")·§12(자동화 제외 범위 표)·§20(확인 완료 항목에 실측 근거 기록, 확인 필요 항목에서 제거)·§22(다음 단계 항목 종료 처리)를 갱신함(기존 "브라우저 뒤로가기 흐름" 제외 결정과 동일한 톤의 영구 스코프 결정으로 기술). 이로써 **Phase 3의 마지막 미해결 사항이 해소**되어, 섹션 3 Phase 3 "검토 Task 5건" 중 보류였던 로그아웃 세션 만료 검토 1건도 이제 "값 확정 + 자동화 영구 제외"라는 최종 결론으로 종결됨. 이 근거로 섹션 1(기준 문서 `project-prd.md` v1.4→v1.5, `logout.md` v1.4→v1.5, 작성/갱신 이력에 v1.18 요약 추가), 섹션 2(Phase 3 행을 🟨 진행중 약 95% → **✅ 완료 100%**로 갱신), 섹션 3(Phase 3 "검토 Task 5건" 문단에 세션 만료 검토 최종 결론 반영), 섹션 5(세션 타임아웃 미확정 블로커 행을 해결 완료로 갱신), 섹션 6(Phase 3 완료 조건 마지막 체크박스를 체크 완료로 전환, §14 최종 점검 항목도 체크 완료로 전환), 섹션 7(다음 액션을 전부 완료 처리하고 "Phase 3까지 전체 완료, 신규 Phase/기능 착수는 사용자 결정 필요"로 갱신)을 반영함. **project-prd.md·logout.md 실제 파일 변경은 automation-prd-writer 서브에이전트가 이번 세션에서 직접 수행함**(아직 커밋/push 전).

---

## 2. 마일스톤 요약

| Phase | 목표 | 상태 | 진행률 | 예상 기간(PRD 근거) | 완료 조건 요약 |
|-------|------|------|--------|------|----------------|
| Phase 0 | 프로젝트 기반 설정 (디렉터리/설정/공통 유틸) | ✅ 완료 | 100% | 미정(선행 작업, project-prd.md §13.2, §21.1 근거) | 디렉터리 구조, conftest.py, base_page.py, requirements.txt 등 기본 골격 구축 |
| Phase 1 | 기본 사용자 인증 및 탐색 (회원가입/로그인/로그아웃/상품검색) | ✅ 완료 | 100% (1-A 회원가입/1-B 로그인/1-C 로그아웃/1-D 상품 검색 4개 기능 모두 구현·테스트 작성·PASS 완료, Phase 1 통합 검증(T22)까지 완료) | 1주 (project-prd.md §8.1) | 4개 기능 Page Object + 테스트 케이스 작성, 전체 테스트 PASS |
| Phase 2 | 장바구니 및 결제 진입 제약 확인 | ✅ 완료 | 100% | 1주 (project-prd.md §8.2) | 장바구니 Page Object + 테스트 케이스 작성, 전체 테스트 PASS |
| Phase 3 | 검증 및 안정성 강화 (예외/경계값 확장 검토, 크로스브라우저, CI/CD 최종화) | ✅ 완료 | 100% (8개 항목 전부 완료 — **[v1.17]** 저장소 루트에 `README.md`를 신규 작성해 project-prd.md §14.4 미충족 문제를 해소, §14.1~§14.5 전 항목 충족 전환. **[v1.18]** 마지막 남은 항목이던 "세션 타임아웃 값 확정"을 Playwright MCP 실측(정확히 14일/1,209,600초, Django 기본값과 일치)으로 해소하고, "값은 확정하되 자동화는 영구 제외"라는 사용자 결정을 `docs/prd/project-prd.md`(v1.5)·`docs/prd/features/logout.md`(v1.5)에 반영함. 이로써 8개 항목 전부 완료로 전환되어 **Phase 3가 최종적으로 완료됨**, 아래 근거 참고) | 1주 (project-prd.md §8.3) | 확장 검토 완료, Chrome 단일 브라우저 검증 확정, CI/CD 파이프라인 최종 점검 — **[v1.17~v1.18에서 최종 완료로 전환, 아래는 그 전까지의 이력 보존]** shrimp-task-manager MCP로 6개 Task(Task1~6)를 순차 실행해 전부 `execute_task`→구현/조사→`verify_task`(80점 이상)로 completed 처리되었으나, 검증 과정에서 (1) **[v1.8 갱신]** GitHub Actions 실제 CI 실행(run_id 30201769656)에서 chrome/firefox 모두 대량 테스트 실패(각 16 failed/2 passed/2 errors)했던 문제는 진단 장치(`conftest.py` 훅 확장, `curl` 리치빌리티 체크, screenshots/page_source 아티팩트 업로드) 추가 후 재실행(run_id 30245294664)한 결과 재현되지 않음(chrome/firefox 모두 18 passed/2 failed, curl HTTP 200 확인, 실패 페이지도 봇차단이 아닌 정상 화면으로 확인) — "CI 환경 고유의 구조적 차단" 가설은 근거가 약화되었으나 완전히 배제는 못함, (2) **[v1.12 코드까지 완전 정리 완료]** Firefox 로컬 100% 안정적 통과 미달성(geckodriver 인프라 불안정성) 문제는 v1.11 최종 CI 실행(run_id `30544240887`)에서도 Firefox가 7 failed·실행 시간 19분 이상(전부 `ReadTimeoutError(read timeout=120)` 등 WebDriver 프로토콜 레벨 무응답, 페이지·오버레이와 무관)으로 재확인되어, 사용자가 **Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정함(`docs/prd/project-prd.md` v1.4 §11.2·§15.3·§16.2·§18.2에 반영). v1.11 시점에는 이 결정이 문서에만 반영되고 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job)는 남아 있었으나, **이번 v1.12에서 사용자의 명시적 요청으로 해당 코드를 커밋 `c473106`으로 완전히 제거**함(`conftest.py`의 `pytest_addoption`/`FirefoxOptions` import 삭제, `driver` fixture Chrome 전용 단순화, `.github/workflows/test.yml`의 matrix·`fail-fast` 삭제 및 단일 `test` job으로 정리). 이로써 "범위 제외 결정"과 "실제 코드 상태"가 완전히 일치하게 됨, (3) 저장소 루트에 README.md 파일이 존재하지 않아 project-prd.md §14.4 미충족(변경 없음), (4) SLACK_WEBHOOK_URL 시크릿 미등록으로 Slack 실패 알림 미동작(변경 없음), **(5) (v1.9 해결, v1.10 CI 검증 완료)** 회원가입 테스트(`test_signup.py`) 고정 이메일이 실사이트에 이미 등록되어 있어 `Email Address already exist!`로 매 실행마다 확정적으로 재발하던 실패 2건 — `utils/helpers.py`의 `generate_random_email()`과 `conftest.py`의 동적 이메일 발급 + teardown 계정 정리 안전장치로 해결, 로컬 재검증 및 실제 GitHub Actions 재실행(run_id `30346729676`)으로 이메일 중복 자체는 재발하지 않음을 확인(섹션 5 상세 참고). **(6) (v1.10 신규 발견 → v1.11 부분 개선 → v1.12 완전 해결)** 같은 CI 재실행(run_id `30346729676`)에서 chrome/firefox 모두 `test_signup.py` 2건이 여전히 실패했던 광고/설문 오버레이 문제는, v1.11의 코드 수정 3커밋(`fa91e03`/`5d2d44a`/`67249c2`)으로 Chrome 기준 2/2 실패 → 1/2 실패로 감소한 데 이어, **이번 v1.12의 Firefox 코드 정리 커밋(`c473106`)을 함께 push한 뒤 실행한 최종 CI(run_id `30549234555`)에서 20 passed, 실패 0건으로 완전히 해결됨을 확인함**(`test_signup.py` 2건 포함 20/20 전체 통과) — 남은 미해결 사항 3건(CI 대량 실패 가설 미확정, README.md 부재, `SLACK_WEBHOOK_URL` 미등록)이 있어 ✅ 완료로 표기하지 않음. **(7) (해결 완료, v1.13)** `SLACK_WEBHOOK_URL` 미등록 문제는 사용자 승인 하에 `gh secret set`으로 실제 등록 완료했고, `Notify Slack on failure` 스텝도 실패한 pytest 테스트 목록을 메시지에 포함하도록 개선(커밋 `023dd4e`/`7c1a74c`)해 의도적 실패 테스트로 3회 CI 실행을 통해 실제 동작까지 검증함(run_id `30690743669`/`30691136890`/`30691221750`). **(8) (신규, v1.13, 원인 미조사)** 다만 검증을 마무리하며 임시 테스트를 제거한 뒤에도 CI가 다시 실패했는데, 원인은 v1.12에서 "완전 해결"로 확정했던 광고/설문 오버레이 문제와 같은 계열의 `test_signup.py` 타임아웃이 간헐적이 아니라 **3회 연속** 재현되는 새로운 회귀임(run_id `30691294540`). 사용자가 "지금은 문서화만" 결정해 원인 조사·수정은 다음 세션으로 넘김. 이로 인해 남은 미해결 사항은 (a) 신규 오버레이 회귀 재조사, (b) README.md 작성 여부, (c) 세션 타임아웃 확정 3가지로 재구성됨. **(9) (v1.14, Slack 메시지 포맷 개선)** 사용자가 지정한 `실패위치`/`실패원인` 포맷으로 Slack 메시지를 재구성하는 `.github/scripts/extract_failed_tests.py`를 추가(커밋 `a6ccf54`)해, 실제 CI 실패(run_id `30691891358`, 오버레이 회귀 재현 케이스)로 요청 포맷과 완전히 일치함을 확인함. 이 개선은 알림 메시지의 가독성 개선일 뿐 (a) 오버레이 회귀 자체를 해결하지는 않음. **(10) (v1.15, 해결 완료)** `test_signup.py` 오버레이 회귀의 근본 원인을 실패 스크린샷 분석으로 규명함 — 광고가 Delete Account 클릭 자체를 가로채 페이지 이동이 발생하지 않은 것이며, 오버레이 제거만으로는 이미 유실된 클릭을 복구할 수 없음. `pages/base_page.py`의 오버레이 완화 로직을 BasePage 5개 대기 메서드 전체로 확대(최대 20회 재시도, z-index/iframe/닫기텍스트 휴리스틱 추가)한 데 이어, `click_and_wait` 공용 메서드로 "오버레이 제거 → 재클릭 → 재대기"를 최대 20회 반복하도록 근본 수정함(커밋 `2a1d1bf`). 로컬 `test_signup.py` 6회 연속 전부 PASS, 실제 GitHub Actions 재검증(run_id `30742305431`, 성공)까지 완료(섹션 1 v1.15 요약 참고) (섹션 1 v1.13·v1.14·v1.15 요약·섹션 3·5·6 상세 참고). **[v1.16 추가]** v1.8~v1.15에서 원인 미확정으로 남아있던 CI 간헐적 대량 실패(16 failed/2 passed/2 errors)의 정체가 **Cloudflare 봇차단(수분간 지속되는 챌린지 페이지)**으로 확정되었고, 근본 원인(같은 job=고정 IP)을 겨냥한 job 체이닝(`attempt-1/2/3`, 매 attempt 새 IP)과 fail-fast 사전 체크(`conftest.py`의 `pytest_sessionstart`)를 구현·실제 CI로 happy path까지 검증함(run_id `30747736911`). 다만 "실제 차단 시 attempt-2로 복구"라는 end-to-end 경로는 이번 세션에서 재현되지 않아 라이브 검증은 못함(섹션 1 v1.16 요약·섹션 5 참고). **[v1.17]** 남은 미해결 사항이던 README.md 부재를 신규 작성으로 해소해 project-prd.md §14 전체 성공 기준(§14.1~§14.5)이 충족 상태로 전환됨(섹션 1 v1.17 요약 참고). **[v1.18, 최종 완료]** 마지막 미해결 사항이던 세션 타임아웃 값을 Playwright MCP 실측(14일/1,209,600초)으로 확정하고, "값 확정 + 자동화 영구 제외" 결정을 project-prd.md v1.5·logout.md v1.5에 반영함으로써 Phase 3의 8개 항목이 전부 완료되어 **Phase 3 전체가 최종적으로 ✅ 완료로 확정됨**(섹션 1 v1.18 요약 참고) |

상태 범례: `⬜ 예정 / 🟨 진행중 / ✅ 완료 / 🟥 블록됨`

---

## 3. Phase별 실행 Task

### Phase 0: 프로젝트 기반 설정

**목표**: Phase 1 이후 모든 기능 자동화 작업의 전제가 되는 공통 인프라를 구축합니다.
**관련 문서**: `CLAUDE.md` §3(디렉터리 구조), §7.3(BasePage Wait 메서드), §9(Fixture), §14(스크린샷), §21.1(추후 작업 목록) / `docs/prd/project-prd.md` §13.2(코드 구조), §11.3(CI/CD)

- [x] 디렉터리 구조 생성: `pages/`, `tests/`, `utils/`, `config/`, `test_data/`, `screenshots/`, `reports/` (CLAUDE.md §3) — 구현 확인됨(2026-07-25)
- [x] `requirements.txt` 작성: selenium, pytest, pytest-html, python-dotenv 등 (CLAUDE.md §2) — 구현 확인됨
- [x] `pytest.ini` 작성 (기본 옵션, 마커 등) — 구현 확인됨(`smoke` 마커 포함)
- [x] `.env.example` 및 `.gitignore` 작성: `.env`, `screenshots/`, `reports/` 등 git 미추적 대상 정리 (CLAUDE.md §12.3, project-prd.md §12.4) — 구현 확인됨(`logs/`도 함께 정리됨)
- [x] `conftest.py` 작성: `driver` fixture (scope="function", yield 패턴으로 종료 시 `driver.quit()`) (CLAUDE.md §9) — 구현 확인됨
- [x] `pages/base_page.py` 구현: 공통 요소 조작 메서드(`find_element`, `click` 등) + 공통 Wait 메서드(`find_element_visible`, `click_when_clickable`, `wait_for_element_presence`, `wait_for_text`) (CLAUDE.md §7.3, §18.1) — 구현 확인됨
- [x] `utils/logger.py` 구현: Python `logging` 모듈 기반 로거, 로그 포맷 `[시간] [레벨] [모듈명] [메시지]`, `logs/` 디렉터리 저장 (CLAUDE.md §13) — 구현 확인됨
- [x] `utils/helpers.py` 골격 작성 (화면과 무관한 공통 로직 배치 위치 확보, CLAUDE.md §18.2) — 골격 파일 존재 확인됨(실제 헬퍼 함수는 필요 시점에 추가 예정)
- [x] 테스트 실패 시 자동 스크린샷 저장 hook 구현: `pytest_runtest_makereport` 사용, 파일명 규칙 `{테스트_함수명}_{상태}_{타임스탬프}.png` (CLAUDE.md §14) — `conftest.py`에 구현 확인됨
- [x] `.github/workflows/test.yml` 초안 작성: 커밋 시 pytest 실행 + `reports/` 아티팩트 업로드 (project-prd.md §11.3, §13.4) — 최종 점검은 Phase 3에서 수행 — 초안 구현 확인됨
- [x] `test_data/accounts.json` 골격 생성 (회원가입/로그인/로그아웃 공용 계정 데이터 스키마만 우선 정의, 실제 값은 각 기능 Task에서 채움) — 골격 생성 확인됨(이후 1-A/1-B에서 실제 값으로 채워짐)

**참고**: 이 Phase의 Task들은 PRD상 별도 Phase로 명시되어 있지 않으나, CLAUDE.md §21.1 "추후 작업" 목록과 project-prd.md §13.2의 코드 구조를 근거로 실행 단위를 구체화한 것입니다. Phase 1 착수 전에 최소한 `conftest.py`의 `driver` fixture와 `base_page.py`의 Wait 메서드는 준비되어 있어야 합니다.

---

### Phase 1: 기본 사용자 인증 및 탐색

**목표**: 사용자 가입부터 상품 검색까지의 기본 흐름 자동화 (project-prd.md §8.1)
**선행 조건**: Phase 0의 `driver` fixture, `base_page.py` Wait 메서드 준비 완료

#### 1-A. 회원가입 (Signup)
**관련 PRD**: `docs/prd/features/signup.md` v1.4 (NOR-001, NOR-002 / SIGNUP-REQ-001~013)

- [x] `pages/login_page.py`에 `/login` 페이지 "New User Signup!" 영역 메서드 추가: `enter_signup_name()`, `enter_signup_email()`, `click_signup_button()` (signup.md §17 권장 — 이 메서드들은 로그인 기능 PRD의 LoginPage 설계에 포함시키는 것을 signup.md가 명시적으로 권장함) — 구현 확인됨(2026-07-25)
- [x] `pages/signup_page.py` 구현: Name(조회+수정), Email(조회 전용, dim 처리), Title/Password/Date of Birth/뉴스레터 체크박스, ADDRESS INFORMATION(First name~Mobile Number), `click_create_account_button()` (signup.md §17, SIGNUP-REQ-003~006) — 구현 확인됨
- [x] `pages/account_created_page.py` 구현: `is_account_created_message_displayed()`, `click_continue_button()` (SIGNUP-REQ-007~008) — 구현 확인됨
- [x] `test_data/accounts.json`에 회원가입 테스트 데이터 반영: 정상 가입용(John Doe), 재로그인 검증용(Jane Doe), Country 기본값(India) 사용 케이스(Alex Kim) 3종 (signup.md §15) — 3종 데이터 반영 확인됨(`india_default_signup` 포함)
- [x] `tests/test_signup.py` 작성: NOR-001(유효 정보로 `/login`→`/signup`→`/account_created` 전체 흐름 회원가입 성공), NOR-002(가입 직후 로그아웃 → 동일 계정 재로그인 성공) (signup.md §12) — 작성 및 PASS 확인됨(`test_signup_with_valid_info`, `test_signup_then_logout_and_relogin`)
- [x] `/login`, `/signup`, `/account_created` 각 UI 요소의 Locator는 Playwright MCP(`browser_snapshot` 우선, 부족 시 `browser_evaluate`)로 실제 페이지를 직접 조사·검증한 뒤 확보 (CLAUDE.md §6, shrimp-rules.md §5) — 실제 Locator 미공유 자체는 더 이상 착수를 막는 블로커가 아님. MCP로 확인이 불가능한 사유(로그인 권한 없음, OTP·2FA·CAPTCHA, 다의적 해석 등, CLAUDE.md §6.4·shrimp-rules.md §5)가 있는 요소만 그 시점에 사용자 확인 요청 (signup.md §18) — 조사·확보 완료 확인됨(`data-qa` 속성 기반 Locator로 구현됨)
- [x] (계획 외 추가 산출물) `pages/delete_account_page.py` 신규 구현: `is_account_deleted_message_displayed()`, `click_continue_button()` — 회원가입 테스트가 생성한 계정을 테스트 내에서 정리(계정 삭제)하기 위해 `tests/test_signup.py` 작성 과정에서 추가로 구현됨 (CLAUDE.md §10.3 테스트 데이터 정리 원칙 근거, 원래 로드맵에는 계획되지 않았던 산출물)

#### 1-B. 로그인 (Login)
**선행**: 1-A 회원가입 완료 (계정 생성 필요) 및 로그인 테스트 계정 사전 생성
**관련 PRD**: `docs/prd/features/login.md` v1.4 (NOR-001~002, ABN-001~002 / LOGIN-REQ-001~008)

- [x] `pages/login_page.py`에 로그인 폼 메서드 추가: `enter_email()`, `enter_password()`, `click_login_button()`, `is_error_message_displayed()`, `get_error_message_text()`, `is_on_login_page()` (LOGIN-REQ-001~004) — 구현 확인됨
- [x] `pages/home_page.py`(헤더 공통 영역) 구현: `is_logout_link_displayed()`, `is_signup_login_link_displayed()`, `is_delete_account_link_displayed()`, `get_logged_in_username()` (LOGIN-REQ-003, 007, 008 — "Logged in as {username}"는 회원가입 시 입력한 Name 값과 일치해야 함에 유의, login.md §16) — 구현 확인됨
- [x] `test_data/accounts.json`에 로그인 테스트 계정(사전 생성 방식 확정, login.md §17) 반영: 정상 로그인용, 재로그인용, 비정상 테스트용(미존재 이메일) — `shared_login_accounts` 반영 확인됨
- [x] `tests/test_login.py` 작성: NOR-001(유효 자격증명 로그인 성공, "Logged in as {username}"이 회원가입 Name 값과 일치하는지 검증), NOR-002(로그아웃 후 재로그인), ABN-001(잘못된 비밀번호), ABN-002(존재하지 않는 이메일) (login.md §13~14) — 작성 및 PASS 확인됨(`test_login_with_valid_credentials`, `test_relogin_after_logout`, `test_login_with_invalid_password`, `test_login_with_nonexistent_email`)
- [ ] **블로커**: 로그인 테스트 계정(이메일/비밀번호/Name)을 실제로 사전 생성해두는 작업 필요 — 테스트 실행 전 준비 필수 (login.md §17, §22, 사전 생성 방식 확정)
- [x] 로그인 폼 및 헤더 네비게이션 요소의 Locator는 Playwright MCP로 직접 조사·검증한 뒤 확보 (CLAUDE.md §6, shrimp-rules.md §5) — 실제 Locator 미공유 자체는 더 이상 착수를 막는 블로커가 아님. MCP로 확인이 불가능한 사유(CLAUDE.md §6.4·shrimp-rules.md §5)가 있는 요소만 그 시점에 사용자 확인 요청 (login.md §20) — 조사·확보 완료 확인됨

#### 1-C. 로그아웃 (Logout)
**선행**: 1-B 로그인 완료 (HomePage, LoginPage 재사용)
**관련 PRD**: `docs/prd/features/logout.md` v1.4 (NOR-001~002 / LOGOUT-REQ-001~005)

- [x] `pages/home_page.py`에 `click_logout_link()` 추가 (1-B의 `is_logout_link_displayed()` 재사용) — 구현 확인됨
- [x] `pages/login_page.py`에 로그아웃 이후 상태 조회 메서드 추가: `is_signup_login_link_displayed()`(1-B와 공유), `is_delete_account_link_displayed()`, `is_logged_in_username_displayed()` (LOGOUT-REQ-003) — 구현 확인됨
- [x] `conftest.py`에 `logged_in_user` fixture 추가: 로그인을 사전 수행한 상태를 반환 (login.md/logout.md 공용, logout.md §19 권장) — 구현 확인됨
- [x] `tests/test_logout.py` 작성: NOR-001(정상 로그아웃 → 로그인 페이지 리다이렉트, "Logout"/"Delete Account"/"Logged in as {username}" 미노출 확인), NOR-002(로그아웃 후 동일 계정 재로그인 성공) (logout.md §13) — 작성 및 PASS 확인됨(`test_logout_from_logged_in_state`, `test_logout_then_relogin`). 이 시점에 `test_signup.py`/`test_login.py`의 NOR-002(로그아웃 의존 시나리오)도 함께 재실행하여 최종 PASS 재확인됨
- [ ] **참고(자동화 대상 아님)**: 로그아웃 후 브라우저 뒤로가기(구 ABN-001)와 비로그인 상태 `/logout` URL 직접 접근(구 ABN-002)은 스코프 결정에 따라 자동화 대상에서 완전히 제외되었으므로 별도 테스트 코드를 작성하지 않음 (logout.md §12, §14)

#### 1-D. 상품 검색 (Product Search)
**선행 없음(독립)**: 로그인 여부와 무관하게 이용 가능하도록 확인됨 (product_search.md §18). 단, Phase 1 실행 순서상 인증 3종 이후 착수
**관련 PRD**: `docs/prd/features/product_search.md` v1.6 (NOR-001~003, ABN-001 / SEARCH-REQ-001~010)

- [x] `pages/products_page.py` 구현: `navigate_to_products()`, `enter_search_keyword()`, `click_search_button()`, `get_product_count()`, `get_product_names()`, `is_on_products_page()` (product_search.md §19, SEARCH-REQ-001~010) — 사용자의 스크린샷 공유 없이 Playwright MCP `browser_evaluate`로 `/products` 페이지 DOM을 직접 조사해 `SEARCH_INPUT`, `SEARCH_BUTTON`, `PRODUCT_CARDS`, `PRODUCT_NAME` Locator를 확보하고 구현 완료를 확인함(2026-07-25). 이 사례가 CLAUDE.md §6·shrimp-rules.md §5 절차의 최초 실증 사례임
- [x] `test_data/search_keywords.json` 작성: 유효 키워드(`Blue`, `Top`, `Tshirt`, `Dress`, `Kids`), 무효 키워드(`mondaykiz`, `testkeyword`, `qwer`, `asdf`), 대소문자 검증용(`TOP`) (product_search.md §17) — T21에서 작성 완료 확인됨
- [x] `tests/test_product_search.py` 작성: NOR-001(유효 키워드 검색), NOR-002(검색어 없이 전체 목록 조회, 정확히 34개 확인 — 사이트 데이터 변경 시 기대값 갱신 필요), NOR-003(대소문자 무시 검증), ABN-001(존재하지 않는 키워드, 빈 화면 확인) (product_search.md §13~14) — T21에서 4개 테스트(`test_search_with_valid_keyword`, `test_view_all_products_without_keyword`, `test_search_case_insensitive`, `test_search_with_nonexistent_keyword`) 작성 및 PASS 확인됨, Phase 1 통합 실행(T22)에서도 재확인됨

**Phase 1 산출물**: `pages/login_page.py`, `pages/signup_page.py`, `pages/account_created_page.py`, `pages/home_page.py`, `pages/products_page.py`, `pages/delete_account_page.py`(계획 외 추가 산출물, 1-A 회원가입 테스트 계정 정리용), `tests/test_signup.py`, `tests/test_login.py`, `tests/test_logout.py`, `tests/test_product_search.py`(T21에서 작성 완료, T22 통합 실행에서 PASS 재확인됨)

---

### Phase 2: 장바구니 및 결제 진입 제약 확인

**목표**: 장바구니 기능 자동화 (project-prd.md §8.2)
**선행 조건**: Phase 1의 `pages/products_page.py` 존재 (1-D에서 이미 구현됨 — 이 Phase에서는 장바구니 담기 관련 메서드로 확장)
**관련 PRD**: `docs/prd/features/shopping_cart.md` v1.4 (NOR-001~005, ABN-001~002 / CART-REQ-001~010)

- [x] Playwright MCP로 `/products`, `/product_details/2`, `/view_cart` 3개 페이지를 실제 방문하여 장바구니 관련 UI Locator 조사·확정 (`browser_snapshot` 우선, 부족 시 `browser_evaluate`) — shrimp-task-manager Task1(95점) 완료 확인됨. 삭제(X 아이콘)와 "Proceed To Checkout" 버튼은 "되돌리기 어려운 동작"으로 분류되어 실클릭 전 AskUserQuestion으로 사용자 확인을 받았고(사용자가 "둘 다 허용" 응답), 실제 클릭으로 삭제 즉시 반영, 로그인 모달 노출 + URL(`/view_cart`) 유지, 빈 장바구니 안내 문구를 모두 확인함 (CLAUDE.md §6, shopping_cart.md §20)
- [x] `pages/base_page.py`에 `wait_for_element_invisible` 메서드 추가: 기존 4종 공통 Wait 메서드와 동일한 스타일로 구현 (shopping_cart.md §19 "재사용 가능한 메서드" 근거) — shrimp-task-manager Task2(100점) 완료 확인됨
- [x] `utils/helpers.py`에 가격 텍스트 파싱 함수 추가: `parse_price_text(price_text: str) -> int` 구현 (shopping_cart.md §19 `parse_price_text_to_number` 예시 근거) — shrimp-task-manager Task3(100점) 완료 확인됨. **(계획 외 추가 산출물)**: 실제 사이트의 광고 스크립트(`class="google-anno"`)가 상품명 텍스트에 여분의 공백을 삽입하는 문제를 Task5 실동작 검증 중 발견하여 `normalize_whitespace(text: str) -> str` 함수도 함께 추가됨
- [x] `pages/products_page.py` 확장: `add_product_to_cart_by_name()`, `open_product_detail()`, `enter_quantity()`(상품 상세 페이지 전용, 목록 페이지에는 수량 입력 UI 없음), `click_add_to_cart_button()`, `click_view_cart_in_success_modal()`, `click_continue_shopping_in_success_modal()` 6개 메서드와 비공개 헬퍼 `_find_card_by_product_name` 추가 (shopping_cart.md §19, CART-REQ-001~002) — shrimp-task-manager Task4(92점) 완료 확인됨. Phase 1에서 구현된 기존 요소(`SEARCH_INPUT`/`SEARCH_BUTTON`/`PRODUCT_CARDS`/`PRODUCT_NAME` 및 6개 메서드)는 변경 없이 유지됨. `pytest tests/test_product_search.py` 4개 테스트로 회귀 없음 재확인됨
- [x] `pages/cart_page.py` 신규 구현: `navigate_to_cart()`, `is_product_in_cart()`, `get_cart_row_count_for_product()`, `get_product_price()`, `get_product_quantity()`, `get_product_total()`, `delete_product()`, `is_empty_cart_message_displayed()`, `is_proceed_to_checkout_button_displayed()`, `click_proceed_to_checkout()`, `is_login_modal_displayed()` 11개 메서드 전부 구현 (shopping_cart.md §19, CART-REQ-003~010) — shrimp-task-manager Task5(93점) 완료 확인됨
- [x] `test_data/products.json`(장바구니 전용)에 테스트 상품 데이터 반영: Men Tshirt(Rs. 400), Printed Off Shoulder Top - White(Rs. 315) (shopping_cart.md §17) 신규 작성 완료. `conftest.py`에 기존 `accounts_data`/`search_keywords_data`와 동일한 패턴으로 `products_data`(session-scope) + `cart_test_product`/`cart_test_product_secondary` fixture 추가 — shrimp-task-manager Task6(100점) 완료 확인됨
- [x] `tests/test_shopping_cart.py` 작성: shopping_cart.md 자동화 대상 시나리오 7개(NOR-001~005, ABN-001~002)를 6개 테스트 함수로 전부 커버 — shrimp-task-manager Task7(96점) 완료 확인됨
  - [x] NOR-001 (상품 목록 페이지에서 장바구니 추가, 항상 1개만 담김) → `test_add_product_to_cart_from_products_page`
  - [x] NOR-002 (상품 상세 페이지에서 수량 지정 후 장바구니 추가) → `test_add_product_to_cart_from_detail_page_with_quantity`
  - [x] NOR-003 (장바구니에서 상품 삭제, 페이지 리로드 없이 즉시 반영 확인) → `test_delete_product_from_cart`
  - [x] NOR-004 (동일 상품 중복 담기 시 기존 행 수량 증가 확인) → `test_duplicate_add_increases_quantity`
  - [x] NOR-005 / ABN-001 (비로그인 상태에서 "Proceed To Checkout" 클릭 시 로그인 안내 모달 노출 확인, URL이 `/view_cart`로 유지되는지 확인 — 두 시나리오는 동일 사실을 다루므로 중복 구현하지 않고 하나로 통합 구현, shopping_cart.md §13 NOR-005 비고에서 권장한 방식) → `test_guest_checkout_shows_login_modal`
  - [x] ABN-002 (빈 장바구니 상태에서 안내 문구 확인, "Proceed To Checkout" 버튼 미노출 확인) → `test_empty_cart_shows_message`
- [x] "Add to cart" 버튼, "View Product" 버튼, 삭제(X) 아이콘, 담기 성공 모달, 로그인 모달 등의 Locator는 Playwright MCP(`browser_snapshot` 우선, 부족 시 `browser_evaluate`)로 직접 조사·검증한 뒤 확보 (CLAUDE.md §6, shrimp-rules.md §5) — 완료 확인됨(Task1). 삭제·결제 진입 등 "되돌리기 어려운 동작"(CLAUDE.md §6.4·shrimp-rules.md §5)에 대한 사용자 확인 절차가 실제로 정상 동작함을 이번 세션에서 실증함 (shopping_cart.md §20)

**Phase 2 산출물**: `pages/products_page.py`(확장), `pages/cart_page.py`(신규), `pages/base_page.py`(확장, `wait_for_element_invisible` 추가), `utils/helpers.py`(확장, `parse_price_text` + `normalize_whitespace` 추가 — `normalize_whitespace`는 계획 대비 추가로 발생한 산출물), `test_data/products.json`(신규), `conftest.py`(확장, `products_data`/`cart_test_product`/`cart_test_product_secondary` fixture 추가), `tests/test_shopping_cart.py`(신규)

---

### Phase 3: 검증 및 안정성 강화

**목표**: 예외/경계값 시나리오 확장 검토, 크로스 브라우저 지원, CI/CD 파이프라인 최종화 (project-prd.md §8.3)
**선행 조건**: Phase 1, Phase 2의 모든 정상/비정상 시나리오 테스트 PASS

- [x] (검토) 회원가입 실패/유효성 검증 케이스(중복 이메일, 필수 필드 누락 등) 자동화 확장 여부 논의 — shrimp-task-manager **Task1(95점)** 완료 확인됨: signup.md §11·§20을 재확인해 "이번 프로젝트 범위 한정" 스코프 사유로 이미 자동화 제외가 확정되어 있음을 원문 근거로 재검증함. **결론: 재논의 불필요, 착수하지 않음. 코드/문서 변경 없음.**
- [x] (검토) 로그인 필수 필드 공란 시나리오, 경계값(비밀번호 길이 등) 자동화 확장 여부 논의 — shrimp-task-manager **Task1(95점)** 완료 확인됨: login.md §12·§22를 재확인해 "이번 프로젝트 범위 한정" 스코프 사유로 이미 자동화 제외가 확정되어 있음을 원문 근거로 재검증함. **결론: 재논의 불필요, 착수하지 않음. 코드/문서 변경 없음.**
- [x] (검토, 블로커 해소 후 착수) 로그아웃 세션 만료 관련 시나리오 자동화 검토 — shrimp-task-manager **Task2(90점)** 완료 확인됨: project-prd.md §19.1, logout.md §9·§12·§20·§22를 재확인해 **세션 타임아웃 값이 여전히 미확정**임을 재검증함. 이번 세션에서 사용자가 "세션 타임아웃은 작업에서 제외"라고 명시적으로 지시함. **결론: 세션 타임아웃 여전히 미확정, 착수하지 않음. 섹션 5 블로커 트래킹 표의 해당 항목은 새로운 사실이 없어 변경 없이 그대로 유지됨.** — **[v1.18 최종 결론]** Playwright MCP 실측으로 세션 타임아웃 값을 **14일(1,209,600초, Django 기본값)**로 확정함(섹션 1 v1.18 요약 참고). 값은 확정되었으나, 사용자가 "값만 확정하고 실제 자동화는 영구 제외"를 명시적으로 선택해(14일 실제 대기 E2E가 CI에서 비현실적, LOGOUT-REQ-005의 세션/쿠키 값 비검증 원칙과도 정합) `docs/prd/project-prd.md`(v1.5)·`docs/prd/features/logout.md`(v1.5)에 영구 스코프 결정으로 반영함. 이로써 이 검토 Task는 "값 확정 + 자동화 영구 제외"라는 최종 결론으로 완전히 종결됨
- [x] (검토) 상품 검색 카테고리/브랜드 필터링, 정렬 기능 자동화 확장 여부 논의 — shrimp-task-manager **Task1(95점)** 완료 확인됨: product_search.md §12를 재확인해 "이번 프로젝트 범위 한정" 스코프 사유로 이미 자동화 제외가 확정되어 있음을 원문 근거로 재검증함. **결론: 재논의 불필요, 착수하지 않음. 코드/문서 변경 없음.**
- [x] (검토) 장바구니 최대 담기 개수 제한, 데이터 저장 방식(세션/쿠키/DB) 검증 여부 재논의 — shrimp-task-manager **Task1(95점)** 완료 확인됨: shopping_cart.md §12·§20를 재확인해 "사용자 판단에 따른 명시적 제외 결정"으로 이미 확정되어 있음을 원문 근거로 재검증함(4건 중 가장 확정적). **결론: 재논의 불필요, 착수하지 않음. 코드/문서 변경 없음.**
- [x] **(범위 제외로 확정, 더 이상 미완료 항목 아님)** Firefox WebDriver 추가 및 Phase 1~2 정상 시나리오 대상 크로스 브라우저 테스트 구성 (project-prd.md §8.3, §11.2, §18.2) — shrimp-task-manager **Task3(85점)** 완료 확인됨. `conftest.py`에 `pytest_addoption`으로 `--browser`(기본값 chrome) 옵션 추가, `driver` fixture를 request 파라미터로 확장. 기존 5개 테스트 파일 무수정, `--browser` 옵션 없이 실행 시 기존과 동일하게 Chrome으로 동작(하위 호환 확인). 검증 중 실제 크로스 브라우저 버그 3건을 발견해 수정함: (1) 광고 iframe이 클릭을 가로채는 `ElementClickInterceptedException` → `pages/base_page.py`에 `scroll_into_view()`/`click_element()` 추가, `click()`/`click_when_clickable()`도 스크롤 후 클릭하도록 수정 (2) project-prd.md §11.2가 명시한 1920x1080 창 크기가 애초에 미구현이었던 결함 발견 → `conftest.py`에 `driver.set_window_size(1920, 1080)` 추가 (3) 광고 많은 페이지 전환 후 대기가 `DEFAULT_TIMEOUT`(10초) 경계에서 간헐적 실패 → `config/settings.py`에 `PAGE_LOAD_TIMEOUT`(20초, project-prd.md §11.4 기 명시값) 신설, `account_created_page.py`/`home_page.py`/`signup_page.py`/`login_page.py`/`products_page.py`의 페이지 전환 대기 지점 5곳에 일관 적용. Chrome 5개 테스트 파일 재실행 결과 18 passed로 회귀 없음 재확인. Firefox는 로컬에서 7회 이상 반복 검증(계정 정리 포함)했으나 100% 재현 가능한 완전한 통과는 달성하지 못함 — 15회 반복 실험으로 순수 `driver.get()` 호출만으로도 geckodriver가 페이지/광고와 무관하게 WebDriver 프로토콜 레벨에서 120초간 응답하지 않는 경우(ReadTimeoutError/socket.timeout)를 확인해, 로컬 macOS에서 Firefox 세션을 반복 기동하며 생긴 geckodriver 자체의 인프라 불안정성이며 애플리케이션 코드로 해결할 문제가 아님을 실험으로 검증함. 사용자가 "현재 상태로 종료 후 CI에서 재확인"을 명시적으로 선택해 로컬 완전 안정화는 당시 Task 범위에서 제외되고 Task4(CI)로 이관됨. **[v1.11 확정]** 이후 Task4(CI)로 이관된 Firefox 검증도 이번 세션의 최종 CI 실행(run_id `30544240887`)에서 7 failed·실행 시간 19분 이상(전부 `ReadTimeoutError(read timeout=120)` 등 동일 계열의 WebDriver 프로토콜 레벨 무응답, 페이지 콘텐츠·광고와 무관)으로 재확인되어, 로컬·CI 양쪽에서 동일한 성격의 인프라 불안정성이 반복 재현됨. 이를 근거로 사용자가 **Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정함(`docs/prd/project-prd.md` v1.4 §11.2·§15.3·§16.2·§17.1·§18.2에 반영). 따라서 이 Task는 "완전한 통과 미달성"이 아니라 "시도 후 인프라 불안정성 근거로 범위 제외를 확정"한 것으로 결론이 확정되어 더 이상 미완료 항목으로 남지 않음. **주의**: Firefox 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job) 자체는 v1.11 시점에는 문서만 갱신했을 뿐 아직 제거하지 않았음. **[v1.12 코드 정리 완료]** 사용자가 "Firefox가 검증 범위에서 제외되게 관련 코드도 모두 수정해달라"고 명시적으로 요청해, 커밋 `c473106`("🔥 chore: Firefox 크로스 브라우저 코드 제거, Chrome 단일 브라우저로 정리")으로 `conftest.py`의 `pytest_addoption`(`--browser` 옵션) 전체 삭제, `FirefoxOptions` import 삭제, `driver` fixture를 Chrome 전용으로 단순화(더 이상 필요 없는 `request` 파라미터도 제거)하고, `.github/workflows/test.yml`의 `strategy.matrix.browser=[chrome, firefox]`·`fail-fast: false`를 삭제해 matrix 없는 단일 `test` job으로 정리함(아티팩트명·Slack 알림 문구의 브라우저 접미사도 함께 제거). `requirements.txt` 등 다른 파일에는 Firefox/gecko 관련 참조가 원래 없었음을 `grep`으로 재확인함. 이로써 "코드 정리는 별도 작업으로 남아 있음"이라는 위 서술은 더 이상 유효하지 않으며, 문서(범위 제외 결정)와 코드 상태가 완전히 일치함(섹션 3 Task4, 섹션 5, 섹션 7 참고)
- [ ] **(테스트 실행 안정성은 v1.12에서 달성, Slack 알림 자체는 SLACK_WEBHOOK_URL 미등록으로 여전히 미검증)** `.github/workflows/test.yml` 최종 점검: 실패 시 알림, HTML 리포트 아티팩트 저장, 매 커밋 자동 실행 안정성 확인 (project-prd.md §11.3, §13.4) — shrimp-task-manager **Task4(82점)** 완료 확인됨. 실제 발견한 결함(HEADLESS 환경변수 미설정으로 디스플레이 없는 GitHub Actions 환경에서 Chrome 실행 실패 가능성) 수정: "Run pytest" 스텝에 `env.HEADLESS="true"` 추가. `strategy.matrix.browser=[chrome, firefox]`(`fail-fast: false`) 도입, `pytest tests/ --browser=${{ matrix.browser }} --html=reports/report-${{ matrix.browser }}.html`로 브라우저별 리포트 분리, 아티팩트도 `reports-${{ matrix.browser }}`로 분리 업로드. 사용자가 "GitHub 기본 이메일 알림으로는 부족, 별도 Slack 알림 구축"을 명시적으로 요청해 `8398a7/action-slack@v3`로 실패 시 Slack 알림 스텝 추가(`secrets.SLACK_WEBHOOK_URL` 필요). 로컬 HEADLESS=true 검증: Chrome 19 passed/1 failed(재시도 시 즉시 PASS, 알려진 광고 로딩 플레이키니스), Firefox 16 passed/4 failed(Task3에서 파악된 동일 패턴). 3개 커밋(✨ 장바구니+Firefox 지원, 🚀 CI 워크플로우, 📝 ROADMAP)으로 origin/main에 push 완료. 사용자 승인으로 gh CLI 설치·인증 후 실제 GitHub Actions 실행(run_id 30201769656)을 직접 조회해 검증함. **워크플로우 메커니즘 자체(matrix 2개 잡 트리거, HEADLESS 적용, 브라우저별 리포트 아티팩트 2종 정상 생성·다운로드)는 정상 동작 확인됨.** 그러나 실제 테스트 결과는 chrome/firefox **둘 다** 16 failed/2 passed/2 errors로 매우 낮았고, 두 브라우저의 실패 패턴이 거의 동일(로그인 이메일 입력창부터 못 찾음, 상품 검색 결과 0개 등)해 Firefox 고유 문제가 아니라 **CI 환경(GitHub Actions 클라우드 IP 등)에서 automationexercise.com 접근이 로컬과 다르게 동작하는 별도의 심층 이슈로 추정**(원인 미확정, 추가 조사 필요). Slack 알림 스텝도 `SLACK_WEBHOOK_URL` 시크릿이 아직 등록되지 않아 "Specify secrets.SLACK_WEBHOOK_URL" 오류로 실패함(사용자가 GitHub 저장소 설정에서 직접 추가해야 하는 민감정보라 에이전트가 대신 처리하지 않음). 사용자가 "워크플로우 수정만으로 Task4 마무리, CI 대량 실패 문제는 별도 기록"을 명시적으로 선택함 — **워크플로우 수정은 완료, 실제 CI 정상 동작은 미해결**(섹션 5 신규 블로커 참고). **[v1.8 추가 조사]** 이후 세션에서 `conftest.py`의 `pytest_runtest_makereport` 훅을 확장(`report.when in ("call", "setup")`으로 setup 단계 실패도 스크린샷+동일 베이스 파일명의 `.html` page_source 캡처 대상에 포함 — 기존에는 `logged_in_user` 픽스처를 쓰는 `test_logout.py`처럼 setup 단계에서 실패하는 경우 스크린샷이 남지 않는 사각지대가 있었음)하고, `.github/workflows/test.yml`에 진단 전용 스텝 2개를 추가함(테스트 로직·타임아웃·로케이터는 변경하지 않음): "Check site reachability(diagnostic only)"(`curl -sS -o /dev/null -w "HTTP %{http_code}\n" https://automationexercise.com/login || true` — Selenium/브라우저 렌더링과 무관하게 순수 HTTP 레벨에서 러너 IP가 차단당하는지 확인하는 가장 저비용 신호, `|| true`로 실패해도 빌드를 깨뜨리지 않음), "Upload failure screenshots and page source"(`screenshots/` 디렉터리를 `screenshots-${{ matrix.browser }}`로 CI 아티팩트 업로드, `if: always()`, `if-no-files-found: ignore` — 기존에는 `reports/`만 업로드되어 실패 시 실제 화면을 확인할 방법이 없었음). 이 변경을 커밋 `1c077b1`로 기존 미푸시 커밋(`bf3dd32`, `39e10ef`)과 함께 push한 뒤 실제 GitHub Actions를 재실행(run_id `30245294664`)해 확인함: curl 스텝은 chrome/firefox 두 job 모두 `HTTP 200`을 반환(러너 IP가 순수 HTTP 레벨에서 차단당하는 것은 아님을 확인)했고, pytest 결과는 chrome **18 passed, 2 failed**(128.65s)/firefox **18 passed, 2 failed**(192.90s)로, `test_login.py`(4개)·`test_logout.py`(2개)·`test_product_search.py`(4개)·`test_shopping_cart.py`(6개)·`test_smoke.py`(2개)는 전부 PASS했음. 원래 run_id 30201769656에서 관찰된 "16 failed/2 passed/2 errors, 로그인 폼 입력창부터 못 찾는" 대량 실패 패턴은 이번 재실행에서 전혀 재현되지 않음. 남은 실패 2건은 두 브라우저 모두 `tests/test_signup.py`(`test_signup_with_valid_info`, `test_signup_then_logout_and_relogin`)뿐이었고, 다운로드한 스크린샷/페이지소스 아티팩트를 직접 열어 확인한 결과 실패 시점 페이지는 정상적인 `<title>Automation Exercise - Signup / Login</title>` 화면(실제 Cloudflare Beacon 스크립트, 실제 Google 광고 iframe 포함 — 봇차단/챌린지 페이지 아님)이었고, 화면에는 실제 인라인 에러 메시지 `Email Address already exist!`가 표시되어 있었음. 이 조사로 "GitHub Actions 클라우드 IP가 구조적으로 차단당한다"는 기존 가설(v1.7 시점 "CI 환경 고유 이슈로 추정")은 근거가 약화되었으나(재실행 1회만으로 완전 배제는 불가), 대신 회원가입 테스트 고정 이메일(`test_data/accounts.json`의 `newuser011@example.com`, `newuser012@example.com`)이 과거 실행에서 이미 실사이트에 등록되어 남아 있어 발생하는 별개의 새로운 문제(테스트 데이터 격리 문제)가 확인됨. 계정 정리(`pages/delete_account_page.py`)는 테스트가 끝까지 성공한 경우에만 같은 테스트 안에서 수행되는 구조라, 과거에 다른 이유로 중간에 실패한 실행들이 만든 계정이 정리되지 못하고 실사이트에 남아 중복 충돌을 일으킨 것으로 원인은 파악되었으나, 이 신규 문제의 수정은 사용자가 "추후 수정"으로 명시적으로 보류함(섹션 5 신규 블로커 참고) — **[v1.9 해결]** 이후 세션에서 실제로 수정함. `utils/helpers.py`에 `generate_random_email()`(uuid4 기반) 추가, `conftest.py`의 `valid_signup_data`/`relogin_signup_data` fixture가 JSON의 name/password 등은 그대로 쓰되 `email` 필드만 매 실행마다 새로 생성하도록 변경. 또한 fixture에 `yield` 이후 `_cleanup_signup_account(driver)` teardown을 추가해, 테스트가 중간에 실패해 로그인 상태로 남아도(`HomePage.is_delete_account_link_displayed()`) 계정 삭제를 한 번 더 시도하도록 안전장치를 마련함 — 이 teardown은 `TimeoutException`/`ElementClickInterceptedException`(기존에 파악된 광고 iframe 클릭 가로채기와 동일한 구체적 예외)만 잡아 로그만 남기고 넘어가도록 해, 정리 실패가 원래 테스트 실패를 가리는 이중 에러를 만들지 않게 처리함(최초 구현 시 이 이중 에러가 실제로 재현되어 발견 후 수정함). **로컬 검증**: `test_signup.py` 단독 실행 3회 연속 2/2 PASS(수정 전에는 매번 100% 확정적으로 2건 실패), 전체 테스트(`tests/`) 20개 실행 시 회귀 없음(광고 관련 클릭/로딩으로 인한 간헐적 실패 1건이 관찰되었으나 이는 Task3/Task4에서 이미 문서화된 것과 동일한 성격의 플레이키니스이며 재실행 시 즉시 PASS로 재현되지 않음). 기존에 막혀 있던 `newuser011/012@example.com` 두 계정은 더 이상 테스트가 사용하지 않으므로 별도 정리가 필요 없음. **다만 이 변경은 로컬 검증만 완료된 상태이며, 커밋/푸시 및 GitHub Actions 재실행을 통한 최종 확인은 아직 이루어지지 않음**. 별도로 `SLACK_WEBHOOK_URL` 저장소 시크릿 등록 여부도 이번에 `gh api repos/erik042156/AT-EX/actions/secrets`로 재확인했으나 `{"total_count": 0, "secrets": []}`로 **여전히 미등록 상태**임을 확인함(변경 없음) — **[v1.10 CI 재확인]** 위 수정을 커밋(`30a3db7`)·로드맵 갱신(`0ce1023`)한 뒤 push하여 GitHub Actions를 재실행함(run_id `30346729676`). **이메일 중복 문제는 해결이 실제로 재확인됨**: chrome/firefox 두 job 모두 실패 지점이 더 이상 `/login` 진입 직후(`Email Address already exist!`)가 아니라 회원가입 성공 이후의 후속 단계로 이동했고, 다운로드한 페이지소스에서 `already exist` 문자열이 0건임을 확인함. 그러나 여전히 **chrome/firefox 모두 2 failed, 18 passed**로 CI는 실패 상태이며, 이번에는 이메일 중복과 무관한 **새로운 원인**이 확인됨 — chrome은 `test_signup_with_valid_info`가 Delete Account 클릭 후 확인 메시지(`h2[data-qa='account-deleted']`) 대기 중, `test_signup_then_logout_and_relogin`이 재로그인 후 로그아웃 링크(`a[href='/logout']`) 대기 중 각각 타임아웃됐고, 다운로드한 스크린샷에서 두 경우 모두 "Powered by Google" 설문조사 전면 오버레이(질문 1/2 및 2/2)가 페이지를 덮고 있는 것을 직접 확인함. firefox는 두 테스트 모두 `a[href='/logout']` 대기 타임아웃이었고, 스크린샷에서 계정 생성 완료 페이지(`ACCOUNT CREATED!`)의 "Continue" 버튼을 "Now Withdraw Without Waiting"(Take Profit Trader) 광고 팝업이 정확히 덮고 있어 클릭이 진행되지 못한 것으로 확인됨. 두 브라우저의 광고 내용은 다르지만 "제3자 광고/설문 오버레이가 버튼·링크를 가려 클릭·대기가 실패한다"는 동일한 성격의 문제로 판단됨(Task3에서 이미 문서화된 "광고 iframe 클릭 가로채기"와 유사 계열이나, 전면 모달 형태는 이번에 처음 관찰됨). 사용자가 "발견한 문제를 로드맵에 업데이트"로 결정하여 **이번 세션에서는 오버레이 처리 로직(닫기 버튼 클릭 등)을 코드로 구현하지 않고 문서화만 수행**함(섹션 5 신규 블로커, 섹션 7 다음 액션 참고) — **[v1.11 코드 수정 및 최종 CI 검증]** 이번 세션에서 위 오버레이 문제를 실제로 코드로 수정함. 커밋 1(`fa91e03`): `pages/base_page.py`의 `click_when_clickable()`을 수정해 클릭 직전 `document.elementFromPoint`(JS)로 클릭 좌표를 실제로 가로채는 요소가 있는지 확인(타겟 자신/조상/자손이면 무시하는 안전장치 포함)하고, 가로채는 요소가 있으면 `execute_script("arguments[0].click();", element)`로 JS 클릭 우회, 없으면 기존처럼 네이티브 클릭을 시도하되 `ElementClickInterceptedException` 발생 시에도 동일하게 JS 클릭 1회 재시도하도록 변경(신규 private 메서드 `_get_intercepting_element()` 추가). 로컬 합성 검증(가짜 전체화면 오버레이를 JS로 주입 후 클릭 성공 확인)까지 완료 후 push해 CI로 검증(run_id `30540357346`): Chrome은 **2 failed, 18 passed**로 `test_signup.py` 2건은 여전히 실패했으나, 실패 지점이 `account_created_page.py::click_continue_button()` 내부의 `wait_for_element_presence(HomePage.LOGOUT_LINK, timeout=20)` 타임아웃으로 이동했고 "JS 클릭으로 우회함" 경고 로그가 없어 클릭 자체는 성공했으나 클릭 *이후* 페이지 전환 대기가 문제임을 확인함. `gh run download`로 받은 실패 스크린샷/페이지소스를 직접 확인한 결과, `</body>` 직후에 삽입된 Google AdSense "Vignette"(전면 삽입형) 광고(`position: fixed !important; width: 100vw !important; height: 100vh !important; z-index: 2147483647 !important`)가 원인으로 확인됨. 커밋 2(`5d2d44a`): `wait_for_element_presence()`를 단일 `WebDriverWait(timeout)`에서 전체 타임아웃 예산을 유지하며 5초 단위 청크로 나누어 폴링하고, 각 청크 타임아웃마다 신규 메서드 `_dismiss_full_page_overlay()`로 화면의 50% 이상을 덮는 `position: fixed`/`absolute` 요소를 JS로 제거한 뒤 다음 청크를 재시도하도록 수정(`time.sleep()` 없이 `WebDriverWait` 자체의 폴링만 사용, 전체 타임아웃 예산 초과 없음). 로컬 합성 검증(CI에서 확인된 Vignette 마크업을 그대로 재현해 주입 후 오버레이 제거 로직 자체의 동작만 검증, 로그인 상태 지연 시나리오까지 완전 재현한 것은 아님)까지 완료 후 push해 CI로 검증(run_id `30543091405`): 최초 실행은 Chrome **6 failed, 13 passed, 1 error**로 `test_login.py`/`test_product_search.py`/`test_shopping_cart.py`/`test_logout.py`까지 광범위하게 실패했으나(오버레이 감지 로그 없이 기본 요소 타임아웃), 이 시점 로컬 회귀 테스트에서도 automationexercise.com 실사이트 자체가 일시적으로 광범위하게 불안정한 것을 확인(수정 전 커밋으로 되돌려도 동일 현상 재현)해, 실사이트 측 일시적 문제로 판단하고 `gh run rerun 30543091405`로 재실행한 결과 **2 failed, 18 passed**로 광범위 실패는 사라짐(불안정성이 일시적이었음이 재확인됨). 다만 `test_signup.py` 2건은 여전히 실패(동일하게 `h2[data-qa='account-deleted']`/`a[href='/logout']` 타임아웃)했고, 이번엔 `_dismiss_full_page_overlay()`가 실행되긴 했으나 제거된 오버레이가 0건(매칭되는 전면 오버레이가 존재하지 않았던 것으로 추정)이라 순수 페이지 로딩 지연(광고 네트워크 요청이 많아 20초 안에 로그인 상태 렌더링이 끝나지 않음) 가능성 쪽으로 판단함. 커밋 3(`67249c2`): `config/settings.py`의 `PAGE_LOAD_TIMEOUT`을 20초 → 35초로 상향(반복되는 CI 타임아웃에 여유 확보, `docs/prd/project-prd.md` v1.4 §11.4에 이미 반영됨)해 push한 뒤 최종 CI 검증(run_id `30544240887`): **Chrome은 1 failed, 19 passed**로 `test_signup_then_logout_and_relogin`이 이제 안정적으로 통과했고, 로그에서 클릭 우회 로직이 실전에서 정상 동작함도 직접 확인됨(`[WARNING] 클릭 대상을 다른 요소가 가리고 있어 JS 클릭으로 우회함: ('css selector', "a[href='/delete_account']"), blocker=<iframe class=''>` — 실제 iframe 광고가 Delete Account 링크를 가로채는 것을 감지해 JS 클릭으로 성공적으로 우회함). 다만 `test_signup_with_valid_info` 1건은 35초로 늘렸음에도 여전히 `h2[data-qa='account-deleted']` 대기에서 타임아웃되어 **완전히 해결되었다고 표기하지 않음**("2/2 실패 → 실전 우회 성공 사례 확인 + 최근 실행 기준 1/2 실패로 감소"가 정확한 표현). 같은 최종 실행에서 **Firefox는 7 failed, 13 passed**(실행 시간 19분 9초, 평소 3분 내외)였으나, 실패 원인은 전부 `urllib3.exceptions.ReadTimeoutError: ... Read timed out. (read timeout=120)` 등 WebDriver 프로토콜 레벨 무응답으로 페이지 콘텐츠·광고·테스트 로직과 무관했고, Task3에서 이미 문서화된 "로컬 geckodriver 120초 무응답" 인프라 불안정성과 동일 계열의 문제가 CI에서도 나타난 것으로 판단됨. 이를 근거로 사용자가 **Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정함(`docs/prd/project-prd.md` v1.4 §11.2·§15.3·§16.2·§17.1·§18.2에 반영, 섹션 1 v1.11 요약 참고). Firefox 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job) 자체는 이번 세션에서 아직 제거하지 않음(문서만 먼저 갱신, 코드 정리는 별도 작업으로 남음 — 섹션 7 참고). `SLACK_WEBHOOK_URL` 미등록, README.md 부재는 이번 세션에서 변경 없이 그대로 유지됨 — **[v1.12 Firefox 코드 정리 및 최종 CI 검증]** 사용자가 "Firefox가 검증 범위에서 제외되게 관련 코드도 모두 수정해달라"고 명시적으로 요청해 이번 세션에서 실제로 정리함. 커밋 `c473106`("🔥 chore: Firefox 크로스 브라우저 코드 제거, Chrome 단일 브라우저로 정리"): `conftest.py`에서 `pytest_addoption`으로 등록했던 `--browser` 옵션 전체 삭제, `FirefoxOptions` import 삭제, `driver` fixture를 Chrome 전용으로 단순화(더 이상 필요 없는 `request` 파라미터도 제거). `.github/workflows/test.yml`에서 `strategy.matrix.browser=[chrome, firefox]` 및 `fail-fast: false`를 전부 삭제해 matrix 없는 단일 `test` job으로 정리하고, 아티팩트명(`reports-${{ matrix.browser }}` → `reports`, `screenshots-${{ matrix.browser }}` → `screenshots`)과 Slack 알림 문구(`"[${{ matrix.browser }}] 테스트 실패"` → `"테스트 실패"`)에서도 브라우저 접미사를 제거함. 로컬 검증: `python -m py_compile conftest.py` 통과, `pytest --collect-only` 20개 정상 수집, `pytest tests/test_smoke.py` 2개 PASS로 단순화된 fixture 정상 동작 확인, YAML 파서로 workflow 문법 검증 완료. `requirements.txt` 등 다른 파일에는 Firefox/gecko 관련 참조가 원래 없었음을 `grep`으로 재확인함. 이 코드 정리 커밋(`c473106`)을 v1.11 반영분 docs 커밋(`ad6fae0`)과 함께 push한 뒤 실제 GitHub Actions로 최종 검증(run_id `30549234555`)함: job 이름이 `test (chrome)`/`test (firefox)`에서 단일 `test`로 바뀌었고, 결과는 **`20 passed in 111.61s`, 실패 0건**. 전체 job 소요 시간도 2분 3초로 안정화됨(기존 Firefox 포함 시 최대 19분+까지 걸리던 것과 대조적). Slack 실패 알림 스텝은 실패가 없어 스킵됨(정상 동작이나, `SLACK_WEBHOOK_URL` 미등록 문제 자체는 실패 시에만 드러나므로 여전히 잠재적으로 남아있음 — 변경 없음). 이로써 v1.10에서 "신규, 최우선"으로 발견됐던 광고/설문 오버레이 블로커가 **완전히 해결**되었다고 확정함(v1.11의 "부분 해결(1/2 실패로 감소)"에서 이번 최종 실행으로 `test_signup.py` 2건 포함 20/20 전체 통과 확인). README.md 부재는 여전히 변경 없음(섹션 5·6·7 참고) — **[v1.13 Slack 알림 개선 및 신규 회귀 발견]** `SLACK_WEBHOOK_URL` 시크릿 미등록 문제를 실제로 해소함: 사용자 승인 하에 `gh secret set SLACK_WEBHOOK_URL --repo erik042156/AT-EX`로 등록 완료(`gh api .../actions/secrets` → `total_count: 1`). 이어서 사용자가 "실패 메시지가 커밋 메시지만 보여줄 뿐 실패 지점을 알 수 없다"고 지적해 `Notify Slack on failure` 스텝을 개선함: "Run pytest" 스텝 출력을 `reports/pytest_output.log`로 저장하고, 실패 시(`if: failure()`)에만 실행되는 "Extract failed test summary" 신규 스텝에서 `FAILED ` 로 시작하는 pytest short summary 줄만 추출해 `GITHUB_ENV`의 `FAILED_TESTS`에 저장, `Notify Slack on failure`의 `text`에 포함시킴(커밋 `023dd4e`). 검증 위해 항상 실패하는 임시 테스트(`tests/test_slack_notification_check.py`, 커밋 `480d218`)를 push했다가, GitHub Actions의 단일 라인 `run:`이 `bash -e {0}`로만 실행되어(`pipefail` 미적용) `pytest | tee`의 종료 코드가 `tee`의 0으로 대체되는 버그를 발견함(run_id `30691136890` — pytest는 `1 failed, 20 passed`였는데 스텝이 "성공"으로 처리되어 알림이 전혀 실행되지 않음). `set -o pipefail`을 명시한 멀티라인 `run:`으로 즉시 수정(커밋 `7c1a74c`)했고, 재검증(run_id `30691221750`)에서 `Extract failed test summary`/`Notify Slack on failure` 모두 정상 실행되며 `FAILED_TESTS`에 실패 테스트명이 정확히 담기는 것을 로그로 확인함(**기능 검증 완료**). 임시 테스트 제거(커밋 `6e3fd80`) 후에도 CI가 다시 실패했는데, 이번엔 `tests/test_signup.py::test_signup_with_valid_info`/`test_signup_then_logout_and_relogin`이 `home_page.click_delete_account_link()`(51번 줄)에서 `TimeoutException`으로 실패했고 `gh run rerun --failed` 2회 재실행에도 동일 지점에서 **3회 연속** 재현됨(run_id `30691294540`). v1.12에서 "완전 해결"로 확정했던 것과 같은 계열의 문제가 이번엔 간헐적이 아니라 연속 재현되는 새 회귀로 판단되나, 사용자가 "지금은 문서화만" 결정해 원인 조사·수정은 착수하지 않음. **현재 main 브랜치 HEAD(`6e3fd80`)의 CI는 실패 상태임**(섹션 5 신규 블로커, 섹션 7 참고) — **[v1.14 Slack 메시지 포맷 개선]** 사용자가 Slack 실패 메시지를 `FAILED`/`실패위치 : <파일> ( "<줄>줄" )`/`실패원인 : <예외클래스>` 구체 포맷으로 지정함에 따라, v1.13의 `grep "^FAILED "` 방식을 `.github/scripts/extract_failed_tests.py`(신규, 커밋 `a6ccf54`)로 교체함. pytest 트레이스백을 테스트별 블록으로 나눠 테스트 파일 자신의 호출 지점(정규식 `^(tests/\S+\.py):(\d+):`)과 첫 예외 표현 줄의 클래스명(정규식 `^E\s+(\S+):`, 모듈 경로 제외)을 각각 추출. 실제 CI 실패 아티팩트 2종(중첩 호출로 발생한 `TimeoutException` 케이스, 테스트 함수 내부 직접 `assert` 실패 `AssertionError` 케이스)을 `gh run download`로 받아 로컬에서 스크립트를 미리 검증한 뒤 push했고, 실제 CI(run_id `30691891358`)에서 Slack `text`에 `FAILED\n실패위치 : tests/test_signup.py ( "42줄" )\n실패원인 : TimeoutException`이 두 번째 실패와 빈 줄로 구분되어 정확히 포함됨을 로그로 확인함(요청 포맷과 완전히 일치). 이 변경은 알림 메시지의 표현 방식만 개선한 것으로, `test_signup.py` 오버레이 회귀 자체는 여전히 미해결 상태임. **[v1.16 CI 간헐적 대량 실패 근본 원인 규명 및 job 체이닝/fail-fast 대응]** v1.8에서 진단 장치만 추가하고 "원인 미확정"으로 남겨뒀던 CI의 간헐적 대량 실패(16 failed/2 passed/2 errors)의 정체를 `gh run rerun` 10회 반복 관찰로 실측 확정함: 3회차(run_id `30744291712`)에서 리치빌리티 체크의 `::warning::`이 실제 트리거되고(`##[warning]` 어노테이션), `gh run download`로 받은 실패 스크린샷(전부 동일 파일 크기 26313 bytes)이 `Please wait while your request is being verified...`(Cloudflare 챌린지)였음을 직접 확인함. 이 차단은 job 시작~종료까지 약 10분간 지속되어 같은 job(고정 IP) 안에서는 기존 pytest 재시도 3회(`nick-fields/retry`, 30초 간격)로 극복되지 않음(3회 전부 동일하게 16 failed/2 passed/2 errors)을 실증함. 근본 원인(같은 job=고정 IP)을 겨냥해 `.github/workflows/_run-e2e-tests.yml`(신규, 재사용 워크플로우)로 기존 스텝을 분리하고 `.github/workflows/test.yml`을 `attempt-1`→`attempt-2`(`needs.attempt-1.result == 'failure'`)→`attempt-3`(notify_on_failure) job 체이닝 구조로 재편(매 attempt마다 새 러너=새 IP), `conftest.py`에 `pytest_sessionstart` 훅으로 세션 시작 전 챌린지 문구 감지 시 `pytest.exit(returncode=75)`하는 fail-fast 사전 체크를 추가함(커밋 `2fba20a`). 로컬 YAML 문법 검증·`pytest --collect-only`·`unittest.mock` 기반 훅 단위 검증·스모크 테스트 통과를 거쳐 push한 뒤, 실제 CI(run_id `30747736911`)로 happy path(attempt-1 성공 시 attempt-2/3 정확히 skip, 전체 success)를 확인함. 이어서 10회 더 재시도(누적 20회)했으나 이번엔 차단이 재현되지 않아 "attempt-1 실패 → attempt-2 새 IP로 실제 복구"라는 end-to-end 경로는 라이브 검증하지 못한 채 사용자가 "현재 검증 수준으로 마무리"를 결정함
- [x] 성능 테스트 요구사항 정의 문서화 (실제 성능 테스트 구현은 범위 밖 유지, project-prd.md §8.3, §6.1) — shrimp-task-manager **Task5(96점, automation-prd-writer 위임)** 완료 확인됨. `docs/prd/project-prd.md` v1.2 → v1.3 갱신. §8.3(Phase 3) 산출물에 "성능 테스트 요구사항 정의 문서(§8.3.1, 참고용)" 추가. 신설된 §8.3.1 "성능 테스트 요구사항 정의(참고용 — MVP 범위 외)"에 측정 지표 범주 4종, 측정 후보 대상 6개(기존 자동화 흐름과 연결), 구현 도구 후보(Locust/k6/Lighthouse 등, 미확정) 기술. 측정 임계치는 전부 "미확정 — 확인 필요"로 명시(임의 수치 생성 없음). §6.1의 기존 "성능 테스트 MVP 제외" 서술은 한 글자도 수정하지 않고 유지, §8.3.1 본문에 두 섹션이 시점상 충돌하지 않음을 명시적으로 설명. 실제 성능 테스트 코드는 작성하지 않음(문서화만)

**참고(Task6, 최종 점검)**: shrimp-task-manager Task6은 별도의 구현 산출물을 만드는 Task가 아니라 project-prd.md §14 성공 기준 전체를 저장소 실제 상태와 대조하는 최종 점검이었습니다. 이 점검에서 새로 발견된 사실(CI 대량 실패, README.md 부재, SLACK_WEBHOOK_URL 미등록, `logs/` 파일명 문서-구현 불일치)은 위 8개 항목에는 직접 포함되지 않으며, 섹션 5(블로커 트래킹)와 섹션 6(Phase 3 완료 조건)에 반영되어 있습니다.

**Phase 3 산출물**: `conftest.py`(확장, `--browser` 옵션·`pytest_addoption`·창 크기 1920x1080 설정 + **[v1.8]** `pytest_runtest_makereport` 훅을 `report.when in ("call", "setup")`으로 확장해 픽스처 setup 단계 실패도 스크린샷 캡처 대상에 포함, 실패 시점 `driver.page_source`를 동일 베이스 파일명의 `.html`로 함께 저장 + **[v1.9]** `valid_signup_data`/`relogin_signup_data` fixture가 `email` 필드를 매 실행마다 동적 생성하도록 변경하고, `yield` 이후 `_cleanup_signup_account(driver)` teardown(계정 정리 안전장치, `TimeoutException`/`ElementClickInterceptedException`만 특정 처리)을 추가 + **[v1.12, 단순화]** `pytest_addoption`으로 등록했던 `--browser` 옵션 전체 삭제, `FirefoxOptions` import 삭제, `driver` fixture를 Chrome 전용으로 단순화(더 이상 필요 없는 `request` 파라미터도 제거, 커밋 `c473106`)), `utils/helpers.py`(확장, **[v1.9]** `generate_random_email()` 신규 추가 — uuid4 기반 고유 이메일 생성), `pages/base_page.py`(확장, `scroll_into_view()`/`click_element()` 추가 + **[v1.11]** `click_when_clickable()`을 `document.elementFromPoint` 기반 오버레이 감지 + JS 클릭 우회로 수정, 신규 private 메서드 `_get_intercepting_element()` 추가(커밋 `fa91e03`); `wait_for_element_presence()`를 5초 청크 폴링 + 청크 타임아웃마다 전면 오버레이 제거를 시도하는 방식으로 수정, 신규 private 메서드 `_dismiss_full_page_overlay()` 추가(커밋 `5d2d44a`)), `config/settings.py`(확장, `PAGE_LOAD_TIMEOUT` 신설 + **[v1.11]** 20초 → 35초 상향(커밋 `67249c2`, `docs/prd/project-prd.md` v1.4 §11.4와 동기화)), `pages/account_created_page.py`/`home_page.py`/`signup_page.py`/`login_page.py`/`products_page.py`(대기 지점 `PAGE_LOAD_TIMEOUT` 적용), `.github/workflows/test.yml`(matrix `[chrome, firefox]`·`HEADLESS=true`·브라우저별 리포트 분리·Slack 알림 스텝 추가 + **[v1.8]** 진단 전용 스텝 2개 추가 — "Check site reachability(diagnostic only)"(`curl` HTTP 상태 코드 확인, 실패해도 빌드 유지) / "Upload failure screenshots and page source"(`screenshots/` 디렉터리를 `screenshots-${{ matrix.browser }}`로 아티팩트 업로드, `if: always()`) + **[v1.12, matrix 제거]** `strategy.matrix.browser=[chrome, firefox]`·`fail-fast: false` 전부 삭제, matrix 없는 단일 `test` job으로 정리, 아티팩트명(`reports-${{ matrix.browser }}` → `reports`, `screenshots-${{ matrix.browser }}` → `screenshots`)과 Slack 알림 문구에서 브라우저 접미사 제거(커밋 `c473106`)), `docs/prd/project-prd.md`(v1.2 → v1.3, §8.3.1 신설, automation-prd-writer 위임 산출물). **주의**: 회원가입/로그인/상품검색/장바구니 4개 검토 항목은 착수하지 않기로 결론이 났으므로 해당 기능별 PRD(`signup.md`, `login.md`, `product_search.md`, `shopping_cart.md`)는 이번 Phase 3에서 수정되지 않았습니다. README.md는 아직 산출물 목록에 없습니다(섹션 5 신규 블로커 참고). **[v1.9]** 회원가입 테스트 이메일 중복 문제는 애초 예상했던 `test_data/accounts.json` 수정 방식이 아니라 `conftest.py`(동적 이메일 생성 + teardown)와 `utils/helpers.py`(`generate_random_email()`) 수정으로 해결되어 이번 Phase 3 산출물에 포함되었습니다. `SLACK_WEBHOOK_URL` 시크릿은 2026-07-28 재확인 결과 여전히 미등록 상태입니다(변경 없음). **[v1.10]** 위 수정을 push 후 GitHub Actions 재실행(run_id `30346729676`)으로 이메일 중복 자체는 재발하지 않음을 확인했으나, chrome/firefox 모두 `test_signup.py` 2건이 광고/설문 오버레이(chrome: Google 설문, firefox: Take Profit Trader 팝업)에 버튼·링크가 가려져 여전히 실패 중입니다. 이 신규 문제의 코드 수정(오버레이 닫기 로직 등)은 사용자 결정에 따라 이번 Phase 3 산출물에 포함되지 않았습니다(섹션 5 신규 블로커 참고). **[v1.11]** 위에서 보류되었던 오버레이 처리 로직을 실제로 구현해 `pages/base_page.py`(`click_when_clickable()`/`wait_for_element_presence()` 수정)와 `config/settings.py`(`PAGE_LOAD_TIMEOUT` 35초 상향)로 이번 Phase 3 산출물에 새로 포함시켰습니다(커밋 `fa91e03`/`5d2d44a`/`67249c2`). 최종 CI 검증(run_id `30544240887`) 결과 Chrome 기준 `test_signup.py` 실패가 2/2 → 1/2로 감소했으나 완전히 해소되지는 않았습니다. 같은 검증 과정에서 사용자가 Firefox 검증을 테스트 범위에서 제외하기로 결정해 `docs/prd/project-prd.md`가 v1.3 → v1.4로 갱신되었습니다(섹션 1 v1.11 요약 참고). `SLACK_WEBHOOK_URL` 미등록, README.md 부재는 이번 세션에서도 변경 없이 그대로 유지됩니다. **[v1.12]** 사용자가 Firefox 관련 코드까지 완전히 제거해달라고 요청해 `conftest.py`(단순화)와 `.github/workflows/test.yml`(matrix 제거)이 이번 Phase 3 산출물에 새로 반영되었습니다(커밋 `c473106`). 이 커밋을 push한 뒤 최종 CI 검증(run_id `30549234555`)에서 **20 passed, 실패 0건**을 확인해, v1.11까지 "1/2 잔여 실패"로 남아있던 `test_signup.py`가 이번 실행에서 완전히 PASS했습니다. 이로써 광고/설문 오버레이 블로커는 완전히 해결된 것으로 확정합니다. `SLACK_WEBHOOK_URL` 미등록, README.md 부재는 이번 세션에서도 변경 없이 그대로 유지됩니다. **[v1.13]** `.github/workflows/test.yml`이 이번 Phase 3 산출물에 추가로 갱신되었습니다: `Run pytest` 스텝에 `set -o pipefail`과 `reports/pytest_output.log` 저장 추가, `Extract failed test summary` 신규 스텝(실패한 `FAILED ` 줄만 추출해 `GITHUB_ENV`의 `FAILED_TESTS`로 저장), `Notify Slack on failure`의 `text`에 `FAILED_TESTS` 포함(커밋 `023dd4e`/`7c1a74c`). `SLACK_WEBHOOK_URL` 저장소 시크릿은 실제로 등록 완료되었습니다(`gh secret set`). 다만 검증 과정에서 `test_signup.py`의 광고 오버레이 타임아웃이 3회 연속 재현되는 새 회귀를 발견했고(run_id `30691294540`), 사용자 결정으로 이번 세션에서는 수정하지 않아 **main HEAD(`6e3fd80`) 기준 CI는 실패 상태로 남아 있습니다**(섹션 5 신규 블로커 참고). **[v1.14]** `.github/scripts/extract_failed_tests.py`(신규)가 이번 Phase 3 산출물에 추가되었습니다: pytest 트레이스백에서 테스트별 실패 위치(파일:줄)와 예외 클래스명을 추출해 사용자가 지정한 `FAILED`/`실패위치`/`실패원인` 포맷으로 출력하며, `.github/workflows/test.yml`의 "Extract failed test summary" 스텝이 이 스크립트를 호출하도록 변경되었습니다(커밋 `a6ccf54`). 실제 CI 실패(run_id `30691891358`)로 요청 포맷과 완전히 일치함을 확인했습니다. **[v1.16]** `.github/workflows/_run-e2e-tests.yml`(신규, 재사용 워크플로우 — checkout~pytest~아티팩트 업로드 스텝을 분리)과 `.github/workflows/test.yml`(전면 재작성 — `attempt-1`/`attempt-2`/`attempt-3` job 체이닝, 매 attempt마다 새 러너로 재시도해 Cloudflare 봇차단처럼 같은 job 안에서는 극복 불가능한 지속형 IP 차단에 대응)이 이번 Phase 3 산출물에 새로 반영되었습니다. `conftest.py`에도 `pytest_sessionstart` 훅(BASE_URL을 가볍게 확인해 Cloudflare 챌린지 문구 감지 시 `pytest.exit`로 즉시 세션 중단하는 fail-fast 사전 체크)이 추가되었습니다(커밋 `2fba20a`). 실제 CI(run_id `30747736911`)로 job 체이닝의 정상 skip 동작까지 확인했으나, 실제 차단 발생 시의 attempt 전환 자체는 이번 세션에서 재현되지 않아 end-to-end로는 검증하지 못했습니다(섹션 5 블로커 트래킹 표 참고).

---

## 4. 기능 의존성 개요

| 선행 기능 | 후행 기능 | 의존 내용 |
|-----------|-----------|-----------|
| 회원가입 (Signup) | 로그인 (Login) | 로그인 전에 계정이 먼저 생성되어야 함. 특히 로그인 성공 후 헤더에 노출되는 "Logged in as {username}"의 검증값은 **회원가입 시 입력한 Name 필드 값**과 정확히 일치해야 함 (login.md §18, signup.md §16) |
| 로그인 (Login) | 로그아웃 (Logout) | 로그인 상태여야 헤더에 "Logout" 링크가 노출되며, 로그아웃 시나리오 자체가 로그인 상태를 전제로 함 (logout.md §18) |
| 로그인 (Login) | 로그아웃 (Logout), 재사용 | `pages/home_page.py`(헤더 공통 컴포넌트)와 `pages/login_page.py`는 로그인·로그아웃 기능이 공동으로 소유/재사용함 (logout.md §19 권장) |
| 상품 검색 (Product Search) | 없음 (독립) | 로그인 여부와 무관하게 이용 가능함이 확인됨 (product_search.md §18) — Phase 1 내 실행 순서는 편의상 인증 3종 이후로 배치 |
| 상품 검색/목록 조회 (Product Search) | 장바구니 (Shopping Cart) | 장바구니에 담을 상품을 먼저 찾아야 함. `pages/products_page.py`는 Phase 1(검색)에서 먼저 구현된 후 Phase 2(장바구니)에서 담기 메서드로 확장됨 (shopping_cart.md §18) |
| 장바구니 (Shopping Cart) | 결제(Checkout, MVP 범위 밖) | 비로그인 상태에서 "Proceed to Checkout" 클릭 시에만 로그인이 요구됨(로그인 모달 노출까지만 Phase 2 범위). 결제 프로세스 자체는 MVP 제외 범위 (shopping_cart.md §3, §12) |

---

## 5. 현재 블로커 / 확인 필요 항목 트래킹

| 항목 | 막고 있는 Task | 확인/해소 주체 및 시점 |
|------|----------------|------------------------|
| 로그인 테스트 계정(이메일/비밀번호/Name) 사전 생성 필요 | 1-B 로그인, 1-C 로그아웃 테스트 실행 | 테스트 실행 전 준비 필요 (login.md §17, §22 — 준비 방식은 "사전 생성"으로 이미 확정됨, 실제 계정 생성 작업만 남음) |
| **(해결 완료, v1.18)** 세션 타임아웃 시간 미확정 | Phase 3 "로그아웃 세션 만료 시나리오" 검토 Task | **해결 완료로 확정**: Playwright MCP로 `automationexercise.com`을 실측(로그인 전후 `sessionid` 쿠키 만료 시각 비교)해 **14일(1,209,600초)**로 확정함 — Django 기본 `SESSION_COOKIE_AGE`와 소수점 단위까지 일치. 사용자가 "값만 확정, 자동화는 영구 제외"를 선택해 `docs/prd/project-prd.md`(v1.5)·`docs/prd/features/logout.md`(v1.5)에 반영함. 추가 액션 불필요(섹션 7 다음 액션 참고) |
| `shopping_cart.md` §22 "project-prd.md 정합성 검토(사용자 승인 필요)" 항목 | 로드맵 진행에는 직접 영향 없음 (참고) | `project-prd.md`는 이미 v1.2에서 "장바구니 수량 변경" 서술을 "장바구니 상품 삭제"로 수정하여 `shopping_cart.md` v1.4와 동기화된 것으로 확인됨(§5.1, §8.2). 다만 `shopping_cart.md` 자체의 "다음 단계" 섹션에는 아직 미해결 항목으로 남아 있어, feature PRD 담당(automation-prd-writer)이 해당 문서의 해당 항목을 정리하는 것을 권장함 |
| 로그인 상태에서 "Proceed to Checkout" 클릭 시 결제 페이지의 상세 동작 | 결제(Checkout) 기능 PRD 착수 (MVP 범위 밖) | MVP 이후 별도 checkout PRD 작성 시점에 확인 (shopping_cart.md §20, §22) |
| **(근본 원인 규명 + 대응 구현 완료, v1.8 조사 → v1.15 재관찰 → v1.16 확정)** CI 환경(GitHub Actions)에서 automationexercise.com 접근 시 대량 테스트 실패(16 failed/2 passed/2 errors 패턴)가 간헐적으로 재발하던 문제(v1.8 최초 관찰, v1.15에서 재관찰) — **v1.16에서 `gh run rerun` 10회 반복 관찰로 정체를 확정함**: 3회차(run_id `30744291712`)에서 실제 Cloudflare 챌린지(`Please wait while your request is being verified...`)가 job 시작부터 끝까지(~10분) 지속되는 것을 스크린샷으로 직접 확인했고, 같은 job(고정 IP) 안에서는 pytest 재시도 3회로도 극복되지 않음(3회 전부 동일하게 16 failed/2 passed/2 errors)을 실증함 | Phase 3 "CI 최종 점검" Task의 실질적 완료, project-prd.md §14.2(테스트 성공률)·§14.5(자동화 인프라 CI/CD 정상 동작) 성공 기준 최종 충족 | **대응 구현 완료, end-to-end 복구는 미검증**: 근본 원인(고정 IP)을 겨냥해 (A) `.github/workflows/test.yml`을 `attempt-1`→`attempt-2`→`attempt-3` job 체이닝 구조(재사용 워크플로우 `_run-e2e-tests.yml`, 매 attempt마다 새 러너/새 IP)로 재편하고, (B) `conftest.py`에 `pytest_sessionstart` 훅으로 세션 시작 전 챌린지 문구를 감지하면 즉시 `pytest.exit`하는 fail-fast 사전 체크를 추가함(커밋 `2fba20a`). 실제 CI(run_id `30747736911`)에서 happy path(attempt-1 성공 시 attempt-2/3가 정확히 skip)는 확인했으나, 추가 10회 재시도(누적 20회)에서 차단이 재현되지 않아 "attempt-1 실패 → attempt-2 새 IP로 실제 복구"는 라이브로 검증하지 못함. 사용자가 "현재 검증 수준으로 마무리"를 결정해 다음 재발 시 자연 검증되는 채로 남김. 담당/시점 미정 |
| **(해결 완료, v1.9 → v1.10 CI 검증 완료)** 회원가입 테스트(`tests/test_signup.py`) 고정(하드코딩) 이메일(`newuser011@example.com`, `newuser012@example.com`, `test_data/accounts.json`)이 실제 공개 사이트(automationexercise.com)에 이미 등록되어 있어 `Email Address already exist!` 충돌로 실패 — CI 재실행(run_id `30245294664`)에서 chrome/firefox 둘 다 `test_signup_with_valid_info`, `test_signup_then_logout_and_relogin` 2건씩 실패로 재현됨(스크린샷/페이지소스로 실제 인라인 에러 메시지 확인). 계정 정리(`pages/delete_account_page.py`)는 테스트가 끝까지 성공한 경우에만 같은 테스트 내에서 수행되는 구조라, 과거 다른 이유로 중간에 실패한 실행들이 만든 계정이 정리되지 못하고 실사이트에 남아 중복 충돌을 일으킨 것 | `tests/test_signup.py` 2개 테스트의 CI/재실행 완전 PASS, project-prd.md §14.2 "실패율 0%" 성공 기준 | **수정 완료 + CI로 재발 안 함을 확인**: `utils/helpers.py`에 `generate_random_email()`(uuid4 기반) 추가, `conftest.py`의 `valid_signup_data`/`relogin_signup_data` fixture가 JSON의 name/password 등은 유지하되 `email`만 매 실행마다 새로 생성하도록 변경 — 이제 매 실행이 항상 "미등록 이메일"이라는 signup.md의 전제조건(§SIGNUP-REQ-002 시작 조건)을 자동으로 만족하여 구조적으로 재발 불가능해짐. 추가로 fixture teardown에 `_cleanup_signup_account(driver)`를 추가해, 테스트가 중간 실패로 계정 삭제 단계까지 못 가고 로그인 상태로 남아도 teardown에서 계정 삭제를 한 번 더 시도(`TimeoutException`/`ElementClickInterceptedException`만 특정 처리, 실패해도 원 테스트 실패를 가리지 않도록 방어)하도록 이중 안전장치를 마련함. 기존에 막혀 있던 두 이메일은 더 이상 사용하지 않으므로 실사이트에서 별도로 삭제할 필요 없음. **로컬 검증**: `test_signup.py` 단독 3회 연속 2/2 PASS(수정 전 100% 확정적 실패였음), 전체 테스트(`tests/`) 20개 회귀 없음. **[v1.10]** 커밋(`30a3db7`)·push 후 GitHub Actions 재실행(run_id `30346729676`)으로 이메일 중복이 더 이상 재현되지 않음을 확인함(실패 지점이 회원가입 성공 이후 단계로 이동, 페이지소스에 `already exist` 문자열 0건). **이 블로커는 해결 완료로 확정**하며, 같은 CI 실행에서 발견된 별개의 새 문제는 아래 새 행("광고/설문 오버레이 차단")으로 별도 추적함 |
| **(해결 완료, v1.10 발견 → v1.11 개선 → v1.12 최종 확인)** CI(GitHub Actions)에서 `test_signup.py`가 실패 — 원인은 이메일 중복이 아니라 제3자 광고/설문 오버레이가 페이지 전면을 덮어 버튼·링크 클릭 및 대기가 타임아웃되는 것(v1.10, run_id `30346729676`에서 최초 확인. chrome: "Powered by Google" 설문조사 전면 모달이 `h2[data-qa='account-deleted']`/`a[href='/logout']` 대기 영역을 가림. firefox: "Take Profit Trader" 광고 팝업이 Continue 버튼을 가림). **[v1.11 개선]** 코드 수정 3커밋(`fa91e03`: `click_when_clickable()`에 `document.elementFromPoint` 기반 오버레이 감지 + JS 클릭 우회 추가 / `5d2d44a`: `wait_for_element_presence()`를 5초 청크 폴링 + 청크 타임아웃마다 전면 오버레이 JS 제거로 수정 / `67249c2`: `PAGE_LOAD_TIMEOUT` 20초 → 35초 상향)을 각각 push해 CI로 순차 검증(run_id `30540357346` → `30543091405`(+rerun) → `30544240887`)함. 최종 CI(run_id `30544240887`)에서 **Chrome 기준 test_signup.py 실패가 2/2 → 1/2로 감소**했고, 실전 오버레이 우회 성공 사례(`blocker=<iframe>`가 Delete Account 링크를 가리는 것을 JS 클릭으로 우회, 로그로 직접 확인됨)도 확보됨. **[v1.12 최종 확인]** 이후 세션에서 Firefox 관련 코드를 완전히 제거(커밋 `c473106`)한 뒤 docs 커밋(`ad6fae0`)과 함께 push하고 실제 GitHub Actions로 최종 검증(run_id `30549234555`)한 결과 **20 passed, 실패 0건**으로 `test_signup.py` 2건을 포함한 전체 테스트가 모두 통과함을 확인함. v1.11에서 잔여했던 1건(`test_signup_with_valid_info`의 `h2[data-qa='account-deleted']` 간헐적 타임아웃)도 이번 실행에서는 재현되지 않음 | `test_signup.py` 2개 테스트의 CI 완전 PASS, project-prd.md §14.2 "실패율 0%" 성공 기준 | **해결 완료로 확정**: CI run_id `30549234555`에서 20/20 PASS로 재발하지 않음을 확인함. 다만 이번 확인은 단일 실행 1회 기준이며, 광고 노출 성격상 향후 CI 실행에서 유사 현상이 완전히 재발하지 않는다고 100% 보장할 수는 없음(간헐적 재발 가능성은 배제하지 않되, 코드 수정(`fa91e03`/`5d2d44a`/`67249c2`)이 유지되는 한 재발해도 즉시 대응 가능한 우회·재시도 로직이 이미 반영되어 있음). 추가 액션 불필요(섹션 7 다음 액션 참고) |
| **(해결 완료, v1.13)** `SLACK_WEBHOOK_URL` 저장소 시크릿 미등록 | `.github/workflows/test.yml`의 Slack 실패 알림 스텝("Specify secrets.SLACK_WEBHOOK_URL" 오류로 실패하던 문제) | **해결 완료로 확정**: 사용자 승인 하에 `gh secret set SLACK_WEBHOOK_URL --repo erik042156/AT-EX`로 등록 완료(`gh api repos/erik042156/AT-EX/actions/secrets` → `total_count: 1`로 확인). 추가로 `Notify Slack on failure` 스텝을 개선해 실패한 pytest 테스트 목록(`FAILED <파일::함수> - <사유>`)이 Slack 메시지 `text`에 포함되도록 수정(커밋 `023dd4e`, `set -o pipefail` 누락 버그 수정 `7c1a74c`)하고, 의도적 실패 테스트로 3회 CI 실행(run_id `30690743669`/`30691136890`/`30691221750`)을 통해 시크릿 인식·실패 테스트 상세 메시지 포함까지 모두 실제 동작함을 로그로 확인함. 추가 액션 불필요(섹션 1 v1.13 요약 참고) |
| **(해결 완료, v1.13/v1.14 발견 → v1.15 근본 원인 규명 및 수정)** `test_signup.py`의 광고/설문 오버레이 타임아웃이 연속 재현됨 | `tests/test_signup.py::test_signup_with_valid_info`, `test_signup_then_logout_and_relogin` — `home_page.click_delete_account_link()` 주변 대기에서 `TimeoutException` | v1.12에서 "완전 해결"로 확정했던 광고/설문 오버레이 문제와 같은 계열이나, v1.13에서는 간헐적이 아니라 동일 지점에서 **3회 연속**(run_id `30691294540`, 실패 줄 51번) 재현됨. **[v1.14 추가 관찰]** 재push(run_id `30691891358`)에서도 재현되었으나 실패 줄이 **42번**으로 달라져, 매번 정확히 같은 지점이 아니라 실패 assertion 지점 자체가 달라질 수 있음을 시사함. **[v1.15 해결]** 실패 시점 스크린샷(`test_signup_with_valid_info_failed_2026-08-02_18-26-37.png`)을 직접 분석해 근본 원인을 규명함: 화면은 여전히 홈페이지였고(`/delete_account`로 이동하지 않음) 그 위를 신규 전면 광고(Sudden Attack 게임 영상 팝업, `position:absolute; width:100vw; height:100vh` iframe)가 덮고 있었음 — 즉 Delete Account 클릭이 광고에 가로채여 **페이지 이동 자체가 발생하지 않은 것**이며, 오버레이를 제거해도 이미 유실된 클릭은 복구되지 않는다는 것이 확인됨. `pages/base_page.py`의 오버레이 완화 로직을 BasePage 5개 대기 메서드 전체로 확대(최대 20회 재시도, z-index>999/뷰포트 30% 이상 iframe/close·×·skip·닫기 텍스트 클릭 휴리스틱 추가)한 데 이어, `click_and_wait` 공용 메서드로 "오버레이 제거 → 링크 재클릭 → 재대기"를 최대 20회 반복하도록 수정함(커밋 `2a1d1bf`, `account_created_page.click_continue_button`/`home_page.click_delete_account_link`/`home_page.click_logout_link` 3곳 적용). 로컬 `pytest tests/ -v` 20개 전체 PASS 및 `test_signup.py` 6회 연속 전부 PASS(수정 전 8회 중 3회 실패와 대비), 실제 GitHub Actions 재검증(run_id `30742305431`, `test` job 성공)까지 확인함. 다만 광고 노출이라는 외부 요인에 근본적으로 기인하므로 향후 완전 재발하지 않는다고 100% 단정하지는 않되, 재시도 폭과 재클릭 대응이 구조적으로 크게 강화되어 유사 상황 발생 시 자동 복구 가능성이 높아짐 |
| **(해결 완료, v1.17)** 저장소 루트에 `README.md` 파일이 존재하지 않음 (Task6에서 `ls` 명령으로 직접 확인) | project-prd.md §14.4 "README.md 포함(프로젝트 개요, 실행 방법)" 성공 기준 충족 | **해결 완료로 확정**: 사용자가 남은 두 항목(README.md 작성 vs 세션 타임아웃 확정) 중 README.md 작성을 먼저 진행하기로 선택해, 프로젝트 개요·기술 스택·디렉터리 구조·설치·환경변수·pytest 실행 방법·테스트 범위·CI/CD 요약·관련 문서 링크를 포함한 `README.md`를 신규 작성함. 문서 신규 작성이라 코드 회귀 위험이 없어 별도 pytest 재실행은 수행하지 않음. 추가 액션 불필요(섹션 7 다음 액션 참고) |

**참고(Phase 0 산출물, 이번 Phase 3 범위 밖이나 Task6에서 재확인됨)**: `utils/logger.py`의 실제 로그 파일은 `logs/test.log`(고정 파일명)인데 project-prd.md §13.3은 `logs/automation_test_*.log`(타임스탬프 패턴)로 서술하고 있어 문서-구현 간 사소한 불일치가 있습니다. 로드맵 진행에 직접 영향은 없어 이 표에 블로커 행으로 추가하지 않고 참고로만 기록합니다.

**참고(정책 변경으로 표에서 제외된 항목)**: 기존에는 "전 기능 공통 UI 요소의 실제 id/name/data-* 속성 미공유"를 이 표에 블로커로 포함했으나, `CLAUDE.md` §6·`shrimp-rules.md` §5의 Playwright MCP 조사 절차 도입에 따라 더 이상 이 표에서 다루지 않습니다. 실제 Locator 미공유 자체는 Page Object 구현 착수를 막지 않으며, 1-D 상품 검색(`pages/products_page.py`)에서 이미 이 절차로 해소된 선례가 있습니다. Playwright MCP로도 확인이 불가능한 개별 요소(CLAUDE.md §6.4, shrimp-rules.md §5의 "되돌리기 어려운 동작" 등의 사유)가 실제로 발견되는 시점에만 그 항목을 이 표에 새 행으로 추가합니다.

---

## 6. Definition of Done (Phase별 완료 조건)

### Phase 0 완료 조건
- [x] `pages/`, `tests/`, `utils/`, `config/`, `test_data/`, `screenshots/`, `reports/` 디렉터리 존재 — 확인됨
- [x] `conftest.py`의 `driver` fixture가 정상 동작 (브라우저 실행/종료 확인) — `tests/test_smoke.py::test_driver_opens_and_closes` PASS로 확인됨
- [x] `base_page.py`의 4개 공통 Wait 메서드(`find_element_visible`, `click_when_clickable`, `wait_for_element_presence`, `wait_for_text`) 구현 및 단순 호출 테스트로 동작 확인 — `tests/test_smoke.py::test_base_page_wait_methods` PASS로 확인됨
- [x] `requirements.txt` 기준으로 `pip install -r requirements.txt` 정상 완료 — 확인됨
- [x] `.env`, `screenshots/`, `reports/` 등이 `.gitignore`에 등록되어 git 미추적 확인 — 확인됨(`logs/`도 함께 등록됨)

### Phase 1 완료 조건
- [x] signup, login, logout, product_search 4개 기능 PRD의 자동화 대상 정상/비정상 시나리오(NOR-001~002, NOR-001~002/ABN-001~002, NOR-001~002, NOR-001~003/ABN-001) 100% 테스트 케이스화 — **완료 확인됨**: T21에서 `tests/test_product_search.py`가 신규 작성되어 4개 기능 전부 커버됨. signup NOR-001~002 → `test_signup_with_valid_info`, `test_signup_then_logout_and_relogin` / login NOR-001~002·ABN-001~002 → `test_login_with_valid_credentials`, `test_relogin_after_logout`, `test_login_with_invalid_password`, `test_login_with_nonexistent_email` / logout NOR-001~002 → `test_logout_from_logged_in_state`, `test_logout_then_relogin`(ABN-001~002는 logout.md §12에서 자동화 범위 제외로 이미 확정되어 미구현이 정상) / product_search NOR-001~003·ABN-001 → `test_search_with_valid_keyword`, `test_view_all_products_without_keyword`, `test_search_case_insensitive`, `test_search_with_nonexistent_keyword`. 총 12개 시나리오 = 12개 테스트 함수로 1:1 매칭 확인됨
- [x] 각 Page Object(SignupPage, LoginPage, HomePage, ProductsPage 등) 구현 직후 Playwright MCP로 주요 화면/시나리오의 실제 동작을 확인 (pytest 실행 전 보조 검증, pytest를 대체하지 않음) — **완료 확인됨**: T11~T21 전 구간에서 SignupPage, LoginPage, AccountCreatedPage, HomePage, DeleteAccountPage, ProductsPage 구현 시마다 Playwright MCP(`browser_navigate`, `browser_snapshot`, `browser_evaluate`, `browser_click`, `browser_fill_form` 등)로 로그인/회원가입/로그아웃/상품검색 흐름을 사전 또는 사후 검증함(각 shrimp-task-manager Task의 verify_task 요약에 기록됨). 예: 로그인 성공/실패 케이스, 로그아웃 후 헤더 상태, 상품 검색 결과 개수(Blue=7건, 전체=34건, TOP/Top=14건 동일, 무효 키워드=0건)를 실제 사이트에서 직접 확인함
- [x] `pytest tests/test_signup.py tests/test_login.py tests/test_logout.py tests/test_product_search.py` 전체 PASS (실패율 0%, pytest 기준) — **완료 확인됨**: 2026-07-25 `pytest tests/test_signup.py tests/test_login.py tests/test_logout.py tests/test_product_search.py --html=reports/report.html` 실행 결과 `12 passed, 1 warning in 65.84s`로 실패 0건 확인됨
- [x] 로그인 테스트 계정이 사전 생성되어 있고, 회원가입 시 입력한 Name 값과 로그인 후 "Logged in as {username}"이 일치함을 확인 — 이번 전체 실행에서도 재확인됨(`test_login_with_valid_credentials`, `test_relogin_after_logout`, `test_signup_then_logout_and_relogin`의 `get_logged_in_username() == expected_username` assertion 전부 PASS)
- [x] 테스트 실패 시 `screenshots/`에 스크린샷 자동 저장 확인 — **완료 확인됨**: 이번 세션 진행 중 실제 발생한 실패(사이트 광고 인터스티셜로 인한 계정 삭제 확인 페이지 로딩 지연 타임아웃 등, 예: `test_signup_with_valid_info`)에서 `screenshots/` 디렉터리에 `test_signup_with_valid_info_failed_2026-07-25_19-31-36.png` 등 총 6개 스크린샷이 파일명 규칙(CLAUDE.md §14.2)에 맞게 정상 저장된 것을 확인함 — 실전 발생 사례로 hook 정상 동작이 실증됨
- [x] `pytest-html` 리포트(`reports/report.html`) 생성 확인 — **완료 확인됨**: 2026-07-25 실행 직후 파일 생성 확인됨(`/Users/leeseunghwan/automation/AT-EX/reports/report.html`, 46,811 bytes, 최신 타임스탬프), pytest-html 플러그인의 "Generated html report" 로그가 매 실행마다 정상 출력됨

### Phase 2 완료 조건
- [x] shopping_cart.md의 자동화 대상 정상/비정상 시나리오(NOR-001~005, ABN-001~002) 100% 테스트 케이스화 — **완료 확인됨**: `tests/test_shopping_cart.py`의 6개 테스트 함수로 7개 시나리오가 전부 매핑됨(NOR-005/ABN-001은 shopping_cart.md §13 NOR-005 비고의 권장에 따라 `test_guest_checkout_shows_login_modal` 하나로 통합 구현)
- [x] `pages/products_page.py`(확장 메서드)와 신규 `pages/cart_page.py` 구현 직후 Playwright MCP로 담기/삭제/결제 진입 등 주요 시나리오의 실제 동작을 확인 (pytest 실행 전 보조 검증, pytest를 대체하지 않음) — **완료 확인됨**: Locator 조사 Task(Task1)에서 Playwright MCP 실클릭으로 담기/성공 모달/삭제/결제 진입/빈 장바구니 전체 흐름을 확인했고, ProductsPage 확장 Task(Task4)에서 ProductsPage+CartPage 연동 흐름을 Selenium 스모크로 추가 확인함. 삭제·결제 진입처럼 되돌리기 어려운 동작은 실클릭 전 사용자 확인(AskUserQuestion)을 거쳐 안전하게 검증됨
- [x] `pytest tests/test_shopping_cart.py` PASS, Phase 1 테스트와 함께 실행 시에도 누적 성공률 100% (pytest 기준) — **완료 확인됨**: `pytest tests/test_shopping_cart.py -v` 단독 실행 결과 6 passed(26.62s). `pytest tests/test_signup.py tests/test_login.py tests/test_logout.py tests/test_product_search.py tests/test_shopping_cart.py --html=reports/report.html` 통합 실행 결과 **18 passed, 0 failed(93.61s)**. 1차 시도에서 `test_signup_then_logout_and_relogin` 1건이 사이트 광고 인터스티셜로 인한 계정 삭제 확인 페이지 로딩 지연 타임아웃으로 실패했으나, Phase 2 변경과 무관한 Phase 1 기존 코드의 플레이키 이슈로 판명되어 단독 재실행 시 즉시 PASS로 재현됨을 확인함(위 Phase 1 완료 조건 스크린샷 항목에 이미 기록된 것과 동일 종류의 현상). `reports/report.html` 최신 타임스탬프로 재생성 확인됨
- [x] `pages/products_page.py`와 `pages/cart_page.py` 간 책임 분리가 shopping_cart.md §19 기준과 일치 (담기 진입은 ProductsPage, 조회/삭제/결제진입은 CartPage) — **완료 확인됨**: 설계 그대로 구현되어 ProductsPage는 담기 진입 메서드만, CartPage는 조회/삭제/결제진입 메서드만 보유함

### Phase 3 완료 조건
- [x] Phase 3 "검토" Task 5건(회원가입/로그인/상품검색/장바구니 자동화 확장 검토 4건 + 로그아웃 세션 만료 검토 1건)에 대한 착수 여부 결정 및 결과 문서화(착수하지 않기로 한 경우 사유 기록) — **완료 확인됨**: shrimp-task-manager Task1(95점)에서 4건 모두 각 기능 PRD 원문(signup.md §11·§20, login.md §12·§22, product_search.md §12, shopping_cart.md §12·§20)을 근거로 "착수하지 않음"으로 재검증·문서화됨. Task2(90점)에서 로그아웃 세션 만료 1건은 세션 타임아웃 미확정(project-prd.md §19.1)과 사용자의 명시적 제외 지시를 근거로 "착수하지 않음(보류)"으로 문서화됨. 5건 전부 결정과 사유가 문서화되었다는 의미에서 이 DoD 항목 자체는 충족되었으나, 보류된 1건(세션 만료)은 여전히 미해결 상태로 남아 있음(섹션 5 블로커 참고)
- ⚠️ ~~Firefox 브라우저로 Phase 1~2 정상 시나리오 재실행 PASS~~ → **해당 없음(범위 제외로 확정, v1.11)**: Task3(85점)에서 `--browser` 옵션 및 크로스브라우저 대응 코드(스크롤 후 클릭, 1920x1080 창 크기, `PAGE_LOAD_TIMEOUT`)는 완성되어 Chrome은 18 passed로 재확인되었으나, Firefox는 로컬 7회 이상 반복 검증에서도 100% 안정적 통과를 달성하지 못했고(geckodriver 인프라 불안정성으로 판명), Task4(82점)의 실제 GitHub Actions 실행(run_id 30201769656)에서도 Firefox는 16 failed/2 passed/2 errors로 PASS 기준을 충족하지 못함. **[v1.8 추가]** 이후 진단 장치를 추가하고 재실행한 GitHub Actions(run_id `30245294664`)에서는 Firefox가 **18 passed, 2 failed**(192.90s)로 크게 개선되었고, chrome과 정확히 동일하게 `test_signup.py`의 이메일 중복 실패 2건(섹션 5 신규 블로커 참고) 외에는 실패가 없어 CI 환경에서는 Firefox 고유의 추가 불안정성이 관찰되지 않음. 다만 여전히 2건 실패가 남아 있어 "100% PASS"는 아직 충족하지 못하며, 로컬 geckodriver 무응답 문제 자체가 해소되었다고 단정할 근거는 없음(로컬 재검증은 이번 세션 범위 밖). **[v1.11 확정]** 이번 세션의 최종 CI 검증(run_id `30544240887`)에서 Firefox가 7 failed·19분 이상 소요(전부 `ReadTimeoutError(read timeout=120)` 등 WebDriver 프로토콜 레벨 무응답, 페이지·오버레이와 무관)로 재확인되어, 로컬·CI 양쪽에서 동일한 성격의 인프라 불안정성이 반복 재현됨. 이를 근거로 사용자가 **Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정함(`docs/prd/project-prd.md` v1.4 §11.2·§15.3·§16.2·§17.1·§18.2에 반영). 이에 따라 이 DoD 항목은 "미충족"이 아니라 **"해당 없음(범위 제외로 인해 이 프로젝트의 완료 기준에서 제외됨)"**으로 처리하며, 이후 Phase 3 완료 조건 판정에는 이 항목을 카운트하지 않음. v1.11 시점에는 Firefox 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job)가 아직 제거되지 않아 별도 정리 작업이 남아 있었음. **[v1.12 코드 정리 완료]** 사용자 요청으로 커밋 `c473106`을 통해 해당 코드를 완전히 제거함(`conftest.py`를 Chrome 전용으로 단순화, `.github/workflows/test.yml`의 matrix 삭제). 이로써 "범위 제외" 결정과 실제 코드 상태가 완전히 일치하게 되어 더 이상 남은 정리 작업이 없음(섹션 3 Task4, 섹션 7 참고)
- [x] `.github/workflows/test.yml`이 매 커밋마다 정상 실행되고 리포트 아티팩트가 저장됨을 확인 — **[v1.12 충족으로 갱신]** 아래 이력의 최종 상태(run_id `30549234555`, 20 passed·실패 0건, `reports`/`screenshots` 아티팩트 정상 업로드, job 소요 시간 2분 3초로 안정화)를 근거로 "정상 실행 + 리포트 아티팩트 저장"이라는 이 항목의 문면상 기준은 충족된 것으로 판단해 체크함. 다만 Slack 실패 알림이 실제로 발송되는지는 `SLACK_WEBHOOK_URL` 미등록으로 여전히 검증되지 않았으므로, "알림"까지 포함한 CI/CD 완전성은 섹션 6 마지막 항목(§14 성공 기준 최종 점검)에서 별도로 미충족으로 계속 추적함. 이하는 이 항목이 미충족이었던 과거 이력임(변경하지 않고 그대로 보존): 원래 **미충족**: Task4(82점)에서 워크플로우 트리거·HEADLESS 적용·브라우저별 리포트 아티팩트 2종 생성/다운로드 등 **메커니즘 자체는 정상 동작**함을 실제 GitHub Actions 실행(run_id 30201769656)으로 확인했으나, 그 실행에서 나온 실제 테스트 결과가 chrome/firefox 둘 다 16 failed/2 passed/2 errors로 대량 실패해 "정상 실행"의 실질적 의미(테스트가 정상적으로 통과하며 실행됨)는 충족되지 않음. Slack 실패 알림 스텝도 `SLACK_WEBHOOK_URL` 시크릿 미등록으로 실패함. **[v1.8 추가]** 진단 장치(curl 리치빌리티 체크, screenshots/page_source 아티팩트 업로드)를 추가해 재실행한 결과(run_id `30245294664`), curl 스텝은 chrome/firefox 모두 HTTP 200으로 정상, pytest는 chrome/firefox 모두 **18 passed, 2 failed**로 원래의 대량 실패(16 failed/2 passed/2 errors)는 재현되지 않아 워크플로우 실행 자체는 실질적으로 정상 동작하는 것으로 판단됨. 그러나 남은 2건 실패(`test_signup.py`, 이메일 중복 — 섹션 5 신규 블로커 참고)가 있어 "실패율 0%"는 여전히 미충족이며, `SLACK_WEBHOOK_URL` 시크릿 미등록 문제도 변경 없음. 이번 재실행 1회만으로 매 커밋 안정성을 100% 보장한다고 단정하기는 이르므로 몇 차례 더 관찰 필요. **[v1.11 추가]** 이메일 중복 해결 이후 새로 발견된 광고/설문 오버레이 문제를 코드 3커밋(`fa91e03`/`5d2d44a`/`67249c2`)으로 수정하고 최종 CI 검증(run_id `30544240887`)까지 완료함: **Chrome은 1 failed, 19 passed**로 실패가 1건으로 줄었으나 여전히 0건은 아니라 "실패율 0%"는 미충족. **Firefox는 7 failed, 13 passed**(19분 이상 소요, WebDriver 프로토콜 레벨 무응답)로 재확인되어 사용자가 Firefox 검증을 테스트 범위에서 명시적으로 제외하기로 결정함(`docs/prd/project-prd.md` v1.4 §11.2 참고). 이에 따라 이 DoD 항목의 "정상 실행" 판정 기준은 **Chrome 단일 브라우저 기준으로 재정의**되며, Chrome 기준으로도 19/20까지 개선되었으나 아직 100% PASS는 아니므로 **여전히 미충족**으로 유지함. `SLACK_WEBHOOK_URL` 시크릿 미등록 문제는 변경 없음. Firefox matrix job 자체는 아직 워크플로우 파일에서 제거되지 않아 코드 정리가 별도로 필요함(섹션 7 참고) **[v1.12 추가]** 사용자 요청으로 Firefox 관련 코드를 커밋 `c473106`으로 완전히 제거함(`conftest.py`의 `--browser` 옵션·`FirefoxOptions` import 삭제, `driver` fixture Chrome 전용 단순화; `.github/workflows/test.yml`의 matrix·`fail-fast` 삭제, 단일 `test` job으로 정리, 아티팩트명·Slack 알림 문구의 브라우저 접미사 제거). 이 코드 정리 커밋과 v1.11 반영분 docs 커밋(`ad6fae0`)을 함께 push한 뒤 실제 GitHub Actions로 최종 검증(run_id `30549234555`)한 결과 **`20 passed in 111.61s`, 실패 0건**을 확인함. job 이름이 `test (chrome)`/`test (firefox)`에서 단일 `test`로 바뀌었고, 전체 job 소요 시간도 2분 3초로 안정화됨(기존 Firefox 포함 시 최대 19분+까지 걸리던 것과 대조적). **테스트 실행 자체의 "매 커밋 정상 실행" 요건은 이번 실행으로 실질적으로 충족되었다고 판단함.** 다만 Slack 실패 알림 스텝은 이번 실행에 실패가 없어 스킵되었을 뿐이며, `SLACK_WEBHOOK_URL` 시크릿이 여전히 미등록 상태라 "실패 시 알림이 실제로 발송되는지"는 이번 검증으로 확인되지 않음(변경 없이 잠재적 문제로 남아 있음). README.md 부재도 변경 없음. 이 항목 자체의 문면 기준("매 커밋마다 정상 실행 + 리포트 아티팩트 저장")은 위 20/20 PASS 결과로 충족되어 체크 완료로 갱신하되, Slack 알림 미검증·README.md 부재는 별도 항목(다음 §14 성공 기준 최종 점검, 섹션 5·7)에서 계속 추적함 — **[v1.13 추가]** Slack 실패 알림 미검증 문제를 실제로 해소함: `SLACK_WEBHOOK_URL` 저장소 시크릿을 `gh secret set`으로 등록 완료(`total_count: 1`로 확인)했고, `Notify Slack on failure`가 실패한 pytest 테스트 목록까지 메시지에 포함하도록 개선(커밋 `023dd4e`/`7c1a74c`)한 뒤 의도적 실패 테스트로 3회 CI 실행(run_id `30690743669`/`30691136890`/`30691221750`)을 통해 시크릿 인식과 상세 메시지 포함 모두 실제 동작함을 확인함(**Slack 알림 자체는 이제 미검증이 아니라 검증 완료 상태**). 다만 검증 마무리 과정에서 `test_signup.py`의 광고 오버레이 타임아웃이 3회 연속 재현되는 새 회귀를 발견함(run_id `30691294540`). 이 항목("매 커밋마다 정상 실행 + 리포트 아티팩트 저장")의 문면 기준 자체(워크플로우 메커니즘, 아티팩트 업로드, 이제는 알림까지)는 여전히 충족되어 체크 상태를 유지하되, "테스트가 정상적으로 통과하며 실행됨"이라는 실질적 의미는 이 신규 회귀로 다시 미충족 상태가 되었음을 아래 §14 최종 점검 항목에 반영함
- [x] 프로젝트 전체 성공 기준(project-prd.md §14) 충족 여부 최종 점검 — **점검은 수행했으나 결과는 미충족**: shrimp-task-manager Task6에서 §14 전 항목을 실제 저장소 상태와 대조 점검함. §14.1(테스트 코드 완성도)·§14.3(코드 품질)은 기존과 동일하게 충족 유지. §14.2(테스트 성공률, "마지막 실행 기준 실패율 0%")는 로컬 Chrome 기준으로는 충족(18 passed)되나 **실제 GitHub Actions CI 실행 기준으로는 chrome/firefox 모두 대량 실패해 미충족**. §14.4(문서 완성도, "README.md 포함")는 **저장소 루트에 README.md 파일이 존재하지 않음을 `ls` 명령으로 직접 확인해 미충족**. §14.5(자동화 인프라)는 pytest 실행·pytest-html 리포트 생성·로깅/스크린샷 자동 저장은 충족되나 "GitHub Actions CI/CD 구성 완료"는 구성 자체는 완료되었어도 정상 동작까지는 §14.2와 동일한 사유로 미확인. 종합적으로 **project-prd.md §14 전체 성공 기준은 현재 미충족**(README.md 부재, CI 대량 실패가 핵심 근거). **[v1.8 추가]** §14.2(실패율 0%) 미충족의 구체적 근거가 갱신됨: 원래 우려했던 "CI 환경 자체의 대량 실패(원인 미상)"는 재조사 결과(run_id `30245294664`, chrome/firefox 모두 18 passed/2 failed) 재현되지 않아 근거가 약화되었으나, 대신 회원가입 테스트 고정 이메일 중복이라는 확정적으로 재발하는 새로운 원인이 파악됨(원인 파악됨, 수정은 사용자 결정으로 보류 — 섹션 5 신규 블로커 참고). §14.4(README.md 부재)는 변경 없이 그대로 미충족. 종합적으로 **project-prd.md §14 전체 성공 기준은 여전히 미충족**(README.md 부재, `test_signup.py` 이메일 중복으로 인한 실패 2건이 핵심 근거로 갱신됨). **[v1.9 추가]** §14.2의 핵심 근거였던 이메일 중복 문제를 실제로 수정함(`utils/helpers.py`의 `generate_random_email()` + `conftest.py` fixture 동적 이메일 발급/teardown 정리, 섹션 5 참고). 로컬에서 `test_signup.py` 3회 연속 PASS, 전체 테스트 회귀 없음으로 확인했으나 **아직 커밋/푸시 및 GitHub Actions 재확인 전**이라 §14.2 "CI 기준 실패율 0%"는 이번 갱신 시점에도 여전히 미충족으로 유지함(로컬 검증만으로는 §14.2를 충족 처리하지 않음). §14.4(README.md 부재)는 변경 없음. `SLACK_WEBHOOK_URL`도 2026-07-28 `gh api` 재확인 결과 미등록 그대로(§14.5 관련, 변경 없음). **[v1.10 추가]** push 후 실제 GitHub Actions 재실행(run_id `30346729676`)으로 이메일 중복 자체는 재발하지 않음이 확인되어 §14.2 미충족의 "이메일 중복" 근거는 해소됨. 다만 같은 실행에서 chrome/firefox 모두 `test_signup.py` 2건이 광고/설문 오버레이 차단이라는 새로운 원인으로 여전히 실패해(섹션 5 신규 블로커 참고) **§14.2 "실패율 0%"는 여전히 미충족**으로 유지함(근거만 이메일 중복 → 광고 오버레이 차단으로 교체됨). §14.4(README.md 부재)·§14.5(`SLACK_WEBHOOK_URL` 미등록) 변경 없음. **[v1.11 추가]** 광고/설문 오버레이 문제를 코드 3커밋으로 수정하고 최종 CI(run_id `30544240887`)로 검증한 결과, **§14.2(실패율 0%)는 Chrome 기준 19/20까지 개선되었으나 여전히 100%는 아니므로 미충족으로 유지**함(근거: `test_signup_with_valid_info` 1건 잔여 실패, 근본 원인은 광고 많은 페이지의 로딩 지연으로 추정되나 100% 확정은 아님). 같은 검증에서 Firefox의 지속적인 WebDriver 프로토콜 레벨 무응답(7 failed, 19분 이상)이 재확인되어 사용자가 Firefox 검증을 테스트 범위에서 명시적으로 제외하기로 결정했고, 이 결정이 `docs/prd/project-prd.md` v1.4(§11.2, §15.3, §16.2, §17.1, §18.2)에 반영됨에 따라 §14.2·§15.3의 "브라우저 검증" 판정 기준 자체가 **Chrome 단일 브라우저 기준**으로 재정의됨(Firefox 결과는 더 이상 §14.2 미충족의 근거로 카운트하지 않음). §14.4(README.md 부재)·§14.5(`SLACK_WEBHOOK_URL` 미등록)는 이번 세션에서도 변경 없이 그대로 미충족 유지. 종합적으로 **project-prd.md §14 전체 성공 기준은 여전히 미충족**이나(README.md 부재, `SLACK_WEBHOOK_URL` 미등록, `test_signup_with_valid_info` 잔여 실패 1건이 핵심 근거), Chrome 기준 실패 건수가 v1.10 대비 크게 줄어 전체적으로 개선된 상태임. **[v1.12 추가]** Firefox 코드 정리 커밋(`c473106`)을 push한 뒤 최종 CI(run_id `30549234555`)로 검증한 결과 **`test_signup_with_valid_info`를 포함해 20 passed, 실패 0건**을 확인함에 따라, **§14.2(테스트 성공률, "마지막 실행 기준 실패율 0%")는 이번 실행을 근거로 "충족"으로 갱신**함(단일 실행 1회 기준이며, 향후 CI에서 재발 가능성을 100% 배제하지는 않음을 전제로 함). §14.5(자동화 인프라)도 "GitHub Actions CI/CD 구성 완료 및 정상 동작"이라는 문면상 핵심 요건(매 커밋 실행, pytest/리포트 정상 생성)은 이번 결과로 충족되었다고 판단하나, §14.5가 요구하는 범위에 Slack 알림 등 실패 대응 체계까지 포함된다고 보면 `SLACK_WEBHOOK_URL` 미등록으로 실패 알림 자체는 검증되지 않아 **부분 충족**으로 유지함(완전 충족으로 표기하지 않음). §14.4(README.md 부재)는 이번 세션에서도 변경 없이 **미충족** 그대로 유지함. 이에 따라 §14 개별 항목의 판정은 §14.1·§14.2·§14.3 충족, §14.5 부분 충족, §14.4 미충족으로 갱신되며, **§14.4가 미충족인 이상 project-prd.md §14 전체 성공 기준과 이 DoD 체크박스, 그리고 Phase 3 자체를 "완료"로 표기하지는 않음**. Phase 3의 남은 미해결 사항은 (1) README.md 작성 여부 결정, (2) `SLACK_WEBHOOK_URL` 시크릿 등록, (3) 세션 타임아웃 값 확정(보류 중인 검토 Task 1건) 3가지로 좁혀짐(섹션 5·7 참고) — **[v1.13 추가]** (2) `SLACK_WEBHOOK_URL` 시크릿 등록은 이번 세션에서 실제로 완료되어 더 이상 미해결 사항이 아님(`gh secret set`으로 등록, `Notify Slack on failure`의 실패 테스트 상세 메시지 포함까지 CI로 검증 완료). 이에 따라 **§14.5(자동화 인프라)를 "부분 충족"에서 "충족"으로 상향**함(Slack 실패 알림 체계까지 포함해 정상 동작이 실제로 확인됨). 그러나 검증 마무리 과정에서 `test_signup.py::test_signup_with_valid_info`/`test_signup_then_logout_and_relogin`이 `home_page.click_delete_account_link()` 대기에서 `TimeoutException`으로 3회 연속(run_id `30691294540`, 최초 실행 + 재실행 2회) 재현되는 새로운 회귀를 발견함 — v1.12에서 "충족"으로 갱신했던 **§14.2(테스트 성공률, "실패율 0%")를 다시 "미충족"으로 되돌림**(근거: main HEAD `6e3fd80` 기준 최신 CI 실행이 2건 실패). 종합적으로 **project-prd.md §14 전체 성공 기준은 여전히 미충족**이며, 핵심 근거가 (a) README.md 부재(§14.4, 변경 없음), (b) `test_signup.py` 오버레이 회귀 재발(§14.2, 신규)로 재구성됨. 사용자가 "지금은 ROADMAP에 기록만" 결정해 이번 세션에서는 이 회귀의 원인 조사·수정에 착수하지 않음. Phase 3의 남은 미해결 사항은 (1) README.md 작성 여부 결정, (2) 세션 타임아웃 값 확정, (3) `test_signup.py` 오버레이 회귀 재조사 3가지로 재정렬됨(섹션 5·7 참고) — **[v1.15 추가]** (3) `test_signup.py` 오버레이 회귀를 근본 원인 규명(클릭이 광고에 가로채여 페이지 이동 자체가 발생하지 않음, 섹션 5 참고) 및 `click_and_wait` 재클릭 로직으로 수정 완료함(커밋 `2a1d1bf`). 로컬 6회 연속 PASS + 실제 GitHub Actions 재검증(run_id `30742305431`, 성공)까지 확인해 **§14.2(테스트 성공률, "실패율 0%")를 다시 "충족"으로 갱신**함(단, 광고 노출이라는 외부 요인 특성상 향후 재발 가능성을 100% 배제하지는 않음). §14.4(README.md 부재)는 이번 세션에서도 변경 없이 미충족 유지. 이에 따라 §14 개별 항목 판정은 §14.1·§14.2·§14.3·§14.5 충족, §14.4만 미충족으로 갱신되며, **§14.4가 미충족인 이상 project-prd.md §14 전체 성공 기준과 이 DoD 체크박스, Phase 3 자체는 여전히 "완료"로 표기하지 않음**. Phase 3의 남은 미해결 사항은 (1) README.md 작성 여부 결정, (2) 세션 타임아웃 값 확정 2가지로 좁혀짐(섹션 5·7 참고) — **[v1.17 추가]** (1) README.md를 신규 작성함(섹션 5 참고)에 따라 **§14.4를 "충족"으로 갱신**함. 이에 따라 **§14 개별 항목 판정이 §14.1~§14.5 전 항목 충족으로 전환**되어, project-prd.md §14(전체 성공 기준) 자체는 이번 갱신으로 충족됨. 다만 이 DoD 체크박스는 §14 충족 여부만이 아니라 Phase 3 전체 완료를 전제로 하며, "로그아웃 세션 만료 시나리오 검토"가 세션 타임아웃 값 미확정(project-prd.md §19.1)으로 여전히 보류 상태이므로 **이 체크박스와 Phase 3 자체는 아직 "완료"로 표기하지 않음**. Phase 3의 남은 미해결 사항은 (1) 세션 타임아웃 값 확정 1가지로 좁혀짐(섹션 5·7 참고) — **[v1.18 추가, 최종 완료]** Playwright MCP 실측으로 세션 타임아웃 값을 **14일(1,209,600초)**로 확정하고, "값 확정 + 자동화 영구 제외" 결정을 `docs/prd/project-prd.md`(v1.5)·`docs/prd/features/logout.md`(v1.5)에 반영함(섹션 1 v1.18 요약, 섹션 5 참고). 이로써 Phase 3의 마지막 미해결 사항이 해소되어 **이 DoD 체크박스를 최종 체크 완료로 전환하며, project-prd.md §14 전체 성공 기준과 Phase 3 자체를 "완료"로 확정함**

---

## 7. 다음 액션 (Next Actions)

우선순위 순으로 착수를 권장하는 Task입니다.

Phase 0(기반 설정), Phase 1(1-A 회원가입, 1-B 로그인, 1-C 로그아웃, 1-D 상품 검색), Phase 2(장바구니 및 결제 진입 제약 확인), **Phase 3(검증 및 안정성 강화)까지 전부 완료되었습니다.** v1.17에서 README.md 신규 작성으로 project-prd.md §14.4를 충족시켰고, **v1.18에서 마지막 블로커였던 세션 타임아웃 값을 Playwright MCP 실측(14일/1,209,600초)으로 확정하고 "값 확정 + 자동화 영구 제외" 결정을 PRD에 반영함으로써 Phase 3가 최종 완료되었습니다.** 아래 목록은 대부분 과거 이력이며, 현재 시점에 착수가 필요한 신규 액션은 없습니다. 다음 작업은 신규 Phase(예: 결제/Checkout 기능 등 MVP 이후 확장)를 시작할지, 아니면 현재 상태로 프로젝트를 마무리할지 사용자 결정이 필요합니다.

1. **(해결 완료, 추가 액션 불필요, v1.17)** README.md 작성 여부 및 시점 결정: 저장소 루트에 README.md가 존재하지 않아 project-prd.md §14.4 문서 완성도 기준이 미충족 상태였으나, 사용자가 남은 두 항목 중 이 항목을 먼저 진행하기로 선택해 프로젝트 개요·기술 스택·디렉터리 구조·설치·환경변수·pytest 실행 방법·테스트 범위·CI/CD 요약·관련 문서 링크를 포함한 `README.md`를 신규 작성함. 이로써 project-prd.md §14.1~§14.5 전 항목이 충족 상태로 전환됨(섹션 5·6 참고)
2. **(해결 완료, 추가 액션 불필요, v1.18)** 세션 타임아웃 값 확정 여부 재확인: Playwright MCP로 `automationexercise.com`을 실측(로그인 전후 `sessionid` 쿠키 만료 시각 비교)해 **14일(1,209,600초, Django 기본값과 일치)**로 확정함. 사용자가 "값만 확정, 자동화는 영구 제외"를 선택해 `docs/prd/project-prd.md`(v1.5)·`docs/prd/features/logout.md`(v1.5)에 반영함. Phase 3 완료를 막던 **마지막 블로커가 해소되어 Phase 3가 최종 완료됨** (섹션 5 블로커 트래킹 표 참고)
3. **(해결 완료, 추가 액션 불필요, v1.13 → v1.14 포맷 개선)** `SLACK_WEBHOOK_URL` 저장소 시크릿 등록 및 실패 알림 상세화: 사용자 승인 하에 `gh secret set SLACK_WEBHOOK_URL --repo erik042156/AT-EX`로 등록 완료(`total_count: 1` 확인). `Notify Slack on failure` 스텝도 실패한 pytest 테스트 목록이 메시지에 포함되도록 개선(커밋 `023dd4e`, `set -o pipefail` 누락 버그 수정 `7c1a74c`)하고 의도적 실패 테스트로 3회 CI 실행을 통해 시크릿 인식·상세 메시지 포함까지 모두 실제 동작함을 로그로 확인함. **[v1.14]** 이후 사용자가 `FAILED`/`실패위치 : <파일> ( "<줄>줄" )`/`실패원인 : <예외클래스>` 구체 포맷을 지정해, `.github/scripts/extract_failed_tests.py`(신규, 커밋 `a6ccf54`)로 메시지 생성 방식을 교체함. 실제 CI 실패(run_id `30691891358`)로 요청 포맷과 완전히 일치함을 확인함. 더 이상 추가 조사·수정이 필요하지 않음(섹션 5 블로커 트래킹 표 참고)
4. **(근본 원인 규명 + 대응 구현 완료, v1.8 조사 → v1.15 재관찰 → v1.16 확정, end-to-end 복구만 미검증)** CI 환경(GitHub Actions)에서의 대량 테스트 실패(16 failed/2 passed/2 errors 패턴) 원인은 **Cloudflare 봇차단(챌린지 페이지, 수분간 지속)**으로 확정됨. `gh run rerun` 10회 반복 관찰로 3회차(run_id `30744291712`)에서 실제 재현을 스크린샷으로 확인했고, 같은 job(고정 IP) 안에서는 pytest 재시도로 극복되지 않음을 실증함. 근본 원인(IP 고정)을 겨냥해 job 체이닝(`attempt-1/2/3`, 매 attempt 새 IP, `.github/workflows/_run-e2e-tests.yml` 신규) + fail-fast 사전 체크(`conftest.py`의 `pytest_sessionstart`, 챌린지 감지 시 즉시 `pytest.exit`)를 구현함(커밋 `2fba20a`). 실제 CI(run_id `30747736911`)로 happy path(attempt-1 성공 시 2/3 skip)는 확인했으나, 추가 재시도에서 차단이 재현되지 않아 attempt-1 실패→attempt-2 복구의 end-to-end 검증은 다음 재발 시로 남음(사용자가 "현재 검증 수준으로 마무리" 결정, 섹션 5 블로커 트래킹 표 참고)
5. **(해결 완료, 추가 액션 불필요, v1.9~v1.10)** 회원가입 테스트(`tests/test_signup.py`) 이메일 중복 문제 해결: `utils/helpers.py`에 `generate_random_email()` 추가, `conftest.py`의 `valid_signup_data`/`relogin_signup_data` fixture가 매 실행마다 새 이메일을 동적 발급하도록 변경 + teardown 계정 정리 안전장치 추가. 로컬 3회 연속 2/2 PASS에 이어 실제 GitHub Actions 재실행(run_id `30346729676`)에서도 이메일 중복이 재현되지 않음을 확인함(섹션 5 블로커 트래킹 표 참고)
6. **(해결 완료, 추가 액션 불필요, v1.10 발견 → v1.11 개선 → v1.12 완전 해결 → v1.13 재발 → v1.15 근본 원인 규명 및 최종 해결)** CI에서 광고/설문 오버레이가 버튼·링크를 가려 발생하던 `test_signup.py` 실패: 코드 3커밋(`fa91e03`/`5d2d44a`/`67249c2`)으로 Chrome 기준 2/2 → 1/2 실패로 감소한 데 이어, Firefox 코드 정리 커밋(`c473106`)과 함께 push한 CI(run_id `30549234555`)에서 20 passed, 실패 0건으로 해결됨을 확인했으나, v1.13의 Slack 알림 검증 과정에서 동일 계열의 타임아웃이 다시 3회 연속 재현됨(run_id `30691294540`). **v1.15에서 실패 시점 스크린샷을 분석해 근본 원인(광고가 클릭 자체를 가로채 페이지 이동이 발생하지 않음)을 규명하고, `pages/base_page.py`의 오버레이 완화 로직을 BasePage 전체로 확대(최대 20회 재시도, z-index/iframe/닫기텍스트 휴리스틱)한 데 이어 `click_and_wait` 공용 메서드로 "오버레이 제거 → 재클릭 → 재대기"를 반복하도록 수정함(커밋 `2a1d1bf`)**. 로컬 6회 연속 전부 PASS, 실제 GitHub Actions 재검증(run_id `30742305431`, 성공)까지 확인해 더 이상 추가 조사가 필요하지 않음(섹션 5 블로커 트래킹 표 참고)
7. **(완료, 추가 액션 불필요, v1.12)** Firefox 관련 코드 정리: 사용자가 "Firefox가 검증 범위에서 제외되게 관련 코드도 모두 수정해달라"고 명시적으로 요청해, `conftest.py`의 `pytest_addoption`/`--browser` 옵션·`FirefoxOptions` import 삭제 및 `driver` fixture 단순화, `.github/workflows/test.yml`의 `strategy.matrix.browser=[chrome, firefox]`·firefox job 삭제(단일 `test` job으로 정리)를 커밋 `c473106`으로 완료함. 문서(`docs/prd/project-prd.md` v1.4의 Firefox 범위 제외 결정)와 실제 코드 상태가 이제 완전히 일치함(섹션 3 Task4, 섹션 5 블로커 트래킹 표 참고)
8. **(확정, 추가 액션 불필요)** Firefox 검증 자체(로컬·CI 모두)는 v1.11에서 사용자가 명시적으로 범위 제외를 결정했고, v1.12에서 관련 코드까지 완전히 정리되어 더 이상 "미달성 블로커"도 "코드 정리 필요 항목"도 아님

---

## 8. 변경 이력

- **v1.0 (2026-07-24)**: 최초 작성. `docs/prd/project-prd.md`(v1.2)의 Phase 1/2/3 구조를 뼈대로 하여, `signup.md`(v1.4), `login.md`(v1.4), `logout.md`(v1.4), `product_search.md`(v1.6), `shopping_cart.md`(v1.4)에 명시된 정상/비정상 시나리오 ID(NOR-xxx/ABN-xxx), 자동화 제외 범위, 확인 완료/확인 필요 항목, 기능 간 의존성을 실행 Task로 변환. Phase 0(프로젝트 기반 설정)을 신설하여 CLAUDE.md §21.1의 미생성 인프라 항목을 선행 작업으로 명시. 현재 저장소에 실제 코드가 없어 모든 Phase 진행률은 0%에서 시작.
- **v1.1 (2026-07-24)**: roadmap-writer 서브에이전트 정의(`.claude/agents/dev/roadmap-writer.md`)에 추가된 규칙("구현 Task가 포함된 Phase의 DoD에는 Playwright MCP 실동작 확인 항목을 pytest PASS 항목과 별도 체크박스로 포함")을 반영. Page Object 구현 Task가 포함된 Phase 1, Phase 2의 완료 조건에 "구현 직후 Playwright MCP로 주요 화면/시나리오의 실제 동작을 확인 (pytest 실행 전 보조 검증, pytest를 대체하지 않음)" 항목을 추가. 인프라 설정 위주인 Phase 0과 검토/크로스브라우저/CI 위주인 Phase 3에는 해당 항목을 추가하지 않음. Task 목록(섹션 3)과 기존 진행 상태(체크박스)는 변경하지 않음.
- **v1.2 (2026-07-25)**: Locator 확보 정책 변경 반영. `CLAUDE.md` §6 "Playwright MCP 기반 페이지 탐색 규칙"(신설) 및 `shrimp-rules.md` §5 "Playwright MCP 기반 Locator 조사 절차"(갱신)에 따라, 실제 Locator 미공유를 즉시 사용자 확인이 필요한 블로커로 표기하던 항목들(1-A 회원가입, 1-B 로그인, 1-D 상품 검색, Phase 2 장바구니, 섹션 5 블로커 트래킹 표)을 "Playwright MCP로 우선 조사·검증하고, MCP로도 확인 불가능한 경우에만 사용자 확인 요청" 방식으로 재정리. 섹션 5 표에서 "전 기능 공통 UI 요소의 실제 Locator 미공유" 행을 제거하고 정책 변경 취지를 설명하는 참고 문구로 대체. 1-D "`pages/products_page.py` 구현" Task는 실제 저장소에서 구현이 확인되어(Playwright MCP `browser_evaluate`로 `/products` DOM을 직접 조사해 Locator 확보) 체크 완료로 갱신했으며, 이에 맞춰 Phase 1 마일스톤 상태를 ⬜ 예정 → 🟨 진행중으로 조정. Locator 미공유를 사유로 두지 않는 진짜 블로커(로그인 테스트 계정 사전 생성, 세션 타임아웃 미확정 등)는 그대로 유지. Phase 0/3은 이번 정책 변경과 무관하여 수정하지 않음.
- **v1.3 (2026-07-25)**: shrimp-task-manager MCP의 Task 실행 이력(T1~T19, 각 `execute_task` → 구현 → `verify_task` 80점 이상으로 completed 처리)을 저장소 실제 파일 상태와 교차검증하여 진행 상태를 재동기화. Phase 0(디렉터리 구조, `requirements.txt`, `pytest.ini`, `config/settings.py`, `pages/base_page.py`, `conftest.py`, `utils/logger.py`, `utils/helpers.py` 골격, `test_data/accounts.json` 골격, `.github/workflows/test.yml` 초안, `tests/test_smoke.py`)의 모든 Task를 완료로 체크하고 마일스톤 상태를 ⬜ 예정 → ✅ 완료(100%)로 갱신. Phase 1의 1-A 회원가입(`pages/login_page.py`·`signup_page.py`·`account_created_page.py`, `test_data/accounts.json` signup 데이터, `tests/test_signup.py` NOR-001~002 PASS), 1-B 로그인(`pages/login_page.py` 로그인 폼 메서드, `pages/home_page.py`, `test_data/accounts.json` shared_login_accounts, `tests/test_login.py` NOR-001~002/ABN-001~002 PASS), 1-C 로그아웃(`pages/home_page.py`·`login_page.py` 로그아웃 관련 메서드, `conftest.py`의 `logged_in_user` fixture, `tests/test_logout.py` NOR-001~002 PASS)의 Task를 모두 완료로 체크. 회원가입 테스트가 생성한 계정을 정리하기 위해 원래 로드맵에 계획되지 않았던 `pages/delete_account_page.py`가 1-A 작업 중 추가로 구현된 사실을 확인하여 1-A Task 목록과 "Phase 1 산출물" 목록에 반영. 1-D 상품 검색은 `pages/products_page.py` 구현 완료(기존 v1.2에서 이미 체크)만 유지하고, `test_data/search_keywords.json`과 `tests/test_product_search.py`는 저장소에 파일이 존재하지 않음을 확인하여 미체크로 유지. 이에 따라 Phase 1 마일스톤 진행률을 "세부 산정 필요" → "약 80%"로 구체화(상태는 여전히 🟨 진행중, 1-D 미완료로 ✅ 완료 아님). 섹션 6 Definition of Done에서 Phase 0의 5개 항목을 모두 체크(스모크 테스트 PASS 근거)하고, Phase 1은 로그인 테스트 계정·Name 일치 확인 1개 항목만 체크하고 나머지(100% 테스트 케이스화, Playwright MCP 실동작 확인, 4개 파일 전체 PASS, 스크린샷 저장 확인, HTML 리포트 생성 확인)는 실제 확인 근거가 없거나 product_search 누락으로 미체크 유지하며 각 항목에 현재 상태를 설명하는 문구를 추가. 섹션 7 "다음 액션"을 1-D 잔여 Task(`search_keywords.json` 작성, `test_product_search.py` 작성·실행) 및 Phase 1 통합 검증 중심으로 갱신. 1-B의 "로그인 테스트 계정 사전 생성" 블로커 체크박스, 1-C의 "참고(자동화 대상 아님)" 항목, 섹션 5 블로커/확인 필요 항목 트래킹 표, Phase 2·Phase 3 관련 내용은 이번 재동기화 범위에서 제외하여 그대로 유지.
- **v1.4 (2026-07-25)**: Phase 1 통합 검증(T22) 완료를 반영. 사용자가 직접 실행/확인한 검증 결과(T21에서 `tests/test_product_search.py` 신규 작성 완료로 4개 기능 12개 시나리오 전부 테스트 케이스화, T11~T21 전 구간 Playwright MCP 보조 검증 수행 기록 확인, `pytest tests/test_signup.py tests/test_login.py tests/test_logout.py tests/test_product_search.py --html=reports/report.html` 실행 결과 12 passed·실패 0건, 로그인 계정·Name 일치 재확인, 실제 실패 사례에서 `screenshots/` 자동 저장 6건 확인, `reports/report.html` 생성 확인)를 근거로 섹션 6 "Phase 1 완료 조건" 6개 항목을 모두 미체크 → 체크 완료로 갱신하고 각 항목의 설명 문구를 실제 검증 결과로 교체. 섹션 2 마일스톤 요약 표의 Phase 1 상태를 `🟨 진행중 약 80%` → `✅ 완료 100%`로 갱신. 섹션 7 "다음 액션"을 Phase 1 잔여 Task 중심에서 Phase 2(장바구니) 착수 Task(`pages/products_page.py` 확장, `pages/cart_page.py` 신규 구현, `test_data/products.json` 작성, `tests/test_shopping_cart.py` 작성·실행, Phase 2 통합 검증) 중심으로 전면 교체. 이번 갱신은 섹션 2(Phase 1 행)·섹션 6(Phase 1)·섹션 7·본 변경 이력 항목에 한정되며, 섹션 3의 Phase 1 세부 Task 체크박스(1-D의 `search_keywords.json`/`test_product_search.py` 항목, "Phase 1 산출물" 목록의 "미작성" 표기 등)와 Phase 0/2/3, 섹션 4, 섹션 5 블로커 트래킹 표는 이번 갱신 범위에서 제외되어 그대로 유지됨. **참고**: 이로 인해 섹션 3의 1-D 관련 체크박스 및 "Phase 1 산출물" 목록 문구가 섹션 6과 일시적으로 불일치하는 상태이며(예: `tests/test_product_search.py(미작성)` 표기가 아직 남아 있음), 이는 다음 갱신 시 함께 정리가 필요함.
- **v1.5 (2026-07-25)**: v1.4에서 남겨두었던 섹션 3-섹션 6 불일치를 보완. 섹션 3의 "1-D. 상품 검색" Task 목록 중 `test_data/search_keywords.json` 작성, `tests/test_product_search.py` 작성 두 항목을 `[ ]` → `[x]`로 갱신(T21에서 작성 완료 및 PASS 확인, T22 통합 실행에서 재확인된 근거 반영). "Phase 1 산출물" 목록의 `tests/test_product_search.py(미작성)` 표기를 완료된 산출물 표기로 수정. 이번 보완은 섹션 3의 해당 두 체크박스와 산출물 목록 문구에 한정되며, 그 외 섹션은 변경하지 않음.
- **v1.6 (2026-07-26)**: Phase 2(장바구니 및 결제 진입 제약 확인) 완료를 반영. shrimp-task-manager MCP로 Phase 2를 7개 Task로 분해해 순차 실행했고, 7개 Task 모두 `execute_task` → 구현 → `verify_task`(80점 이상, Task1 95점/Task2 100점/Task3 100점/Task4 92점/Task5 93점/Task6 100점/Task7 96점)로 completed 처리된 이력을 근거로 갱신함. 섹션 2 마일스톤 요약 표의 Phase 2 상태를 `⬜ 예정 0%` → `✅ 완료 100%`로 갱신. 섹션 3 "Phase 2" 하위 전 체크박스를 `[x]`로 갱신하고, `tests/test_shopping_cart.py`의 NOR-001~005/ABN-001~002 각 항목에 실제 매핑된 테스트 함수명(`test_add_product_to_cart_from_products_page`, `test_add_product_to_cart_from_detail_page_with_quantity`, `test_delete_product_from_cart`, `test_duplicate_add_increases_quantity`, `test_guest_checkout_shows_login_modal`, `test_empty_cart_shows_message`)을 추가. "Phase 2 산출물" 목록을 실제 산출물(`pages/products_page.py` 확장, `pages/cart_page.py` 신규, `pages/base_page.py` 확장, `utils/helpers.py` 확장, `test_data/products.json` 신규, `conftest.py` 확장, `tests/test_shopping_cart.py` 신규)로 갱신. 섹션 6 "Phase 2 완료 조건" 4개 항목을 모두 체크 완료 처리하고, `pytest tests/test_shopping_cart.py -v`(6 passed, 26.62s) 및 Phase 1~2 통합 실행(`pytest tests/test_signup.py tests/test_login.py tests/test_logout.py tests/test_product_search.py tests/test_shopping_cart.py --html=reports/report.html`, 18 passed·0 failed·93.61s) 결과를 근거로 명시. 섹션 7 "다음 액션"을 Phase 3(검증 및 안정성 강화) 착수 관점으로 전면 교체. **[계획 외 발견 사항]** (1) Task3 실행 중 실제 사이트의 광고 스크립트(`class="google-anno"`)가 상품명 텍스트에 여분의 공백을 삽입하는 문제를 발견하여, 계획에 없던 `utils/helpers.py`의 `normalize_whitespace(text: str) -> str` 함수가 추가로 구현됨(섹션 3 Phase 2 Task 목록·산출물에 반영). (2) 삭제(X 아이콘)·"Proceed To Checkout" 클릭처럼 "되돌리기 어려운 동작"에 대해 CLAUDE.md §6.4·shrimp-rules.md §5에 따라 실클릭 전 AskUserQuestion으로 사용자 확인을 받는 절차가 Task1에서 실제로 정상 동작함을 확인함(사용자가 "둘 다 허용" 응답 후 실클릭으로 검증 완료). (3) 통합 실행 1차 시도에서 `test_signup_then_logout_and_relogin` 1건이 사이트 광고 인터스티셜로 인한 타임아웃으로 실패했으나, Phase 2 변경과 무관한 Phase 1 기존 코드의 플레이키 이슈로 판명되어 단독 재실행 시 즉시 PASS로 재현됨을 재확인함(ROADMAP.md 기존 버전 섹션 6 Phase 1 완료 조건에 이미 기록된 것과 동일 종류의 현상). 섹션 5 블로커 트래킹 표는 이번 갱신으로 정리가 필요한 Phase 2 관련 행이 없음을 확인하여(Locator 미공유 관련 정책 변경은 v1.2에서 이미 반영됨, `shopping_cart.md` §22 project-prd.md 정합성 검토 항목은 Phase 진행에 직접 영향이 없어 그대로 유지) 변경하지 않음. 섹션 3의 Phase 0/1 내용, 섹션 4(기능 의존성 개요), 섹션 5(블로커 트래킹 표)는 이번 갱신 범위에서 제외되어 그대로 유지됨.
- **v1.7 (2026-07-26)**: Phase 3(검증 및 안정성 강화) 진행 결과를 반영. shrimp-task-manager MCP로 Phase 3를 6개 Task로 분해해 순차 실행했고, 6개 Task 모두 `execute_task` → 구현/조사 → `verify_task`(80점 이상, Task1 95점/Task2 90점/Task3 85점/Task4 82점/Task5 96점, Task6은 별도 산출물 없는 최종 점검)로 completed 처리된 이력을 근거로 갱신함. **Phase 3는 완료로 표기하지 않음** — 검증 과정에서 (1) 실제 GitHub Actions CI 실행(run_id 30201769656)에서 chrome/firefox 둘 다 대량 테스트 실패(각 16 failed/2 passed/2 errors), (2) Firefox 로컬 100% 안정적 통과 미달성(geckodriver 인프라 불안정성으로 판명, CI 검증으로 이관), (3) 저장소 루트에 README.md 파일 부재(project-prd.md §14.4 미충족), (4) `SLACK_WEBHOOK_URL` 시크릿 미등록으로 Slack 알림 미동작 등 4건의 미해결 사항이 새로 확인되었기 때문. 섹션 2 마일스톤 요약 표의 Phase 3 상태를 `⬜ 예정 0%` → `🟨 진행중 약 70%`로 갱신하고 근거를 상세 기술. 섹션 3 "Phase 3" 하위 8개 항목을 갱신: 회원가입/로그인/상품검색/장바구니 자동화 확장 검토 4건은 Task1(95점)에서 각 기능 PRD 원문 근거로 재검증되어 착수 불필요로 결론 확정되어 `[x]`로 체크, 로그아웃 세션 만료 검토 1건은 Task2(90점)에서 세션 타임아웃 여전히 미확정 + 사용자의 명시적 제외 지시로 `[ ]` 유지, Firefox 크로스 브라우저 1건은 Task3(85점)에서 코드는 완성되었으나 로컬 100% 안정성 미달성으로 "부분 완료" 표기하며 `[ ]` 유지, CI 최종 점검 1건은 Task4(82점)에서 워크플로우 수정 자체는 완료되었으나 실제 CI 대량 실패가 미해결로 "부분 완료" 표기하며 `[ ]` 유지, 성능 테스트 요구사항 정의 1건은 Task5(96점, automation-prd-writer 위임)로 project-prd.md v1.2 → v1.3 갱신·§8.3.1 신설이 완료되어 `[x]`로 체크. Task3에서 발견·수정된 크로스브라우저 버그 3건(광고 iframe 클릭 가로채기 → `scroll_into_view()`/`click_element()` 추가, 1920x1080 창 크기 미구현 결함 수정, `PAGE_LOAD_TIMEOUT`(20초) 신설 및 5개 페이지 전환 대기 지점 적용)을 "Phase 3 산출물" 목록에 반영. 섹션 5 블로커 트래킹 표에 신규 항목 3건(CI 환경 대량 실패 원인 미확정, `SLACK_WEBHOOK_URL` 시크릿 미등록, README.md 부재)을 추가하고, `logs/test.log` vs project-prd.md §13.3 `logs/automation_test_*.log` 문서-구현 불일치를 참고 각주로 추가(Phase 0 산출물이라 블로커 행으로는 추가하지 않음). 세션 타임아웃 미확정 행은 새로운 사실이 없어 변경하지 않고 그대로 유지. 섹션 6 "Phase 3 완료 조건" 4개 항목을 갱신: "검토 Task 착수 여부 결정·문서화" 항목은 5건 전부 결정·사유 문서화가 완료되어 `[x]`, 나머지 3개 항목("Firefox PASS", "CI 정상 실행 확인", "project-prd.md §14 성공 기준 최종 점검")은 모두 미충족 근거(Firefox CI 결과 16 failed/2 passed/2 errors, README.md 부재, CI 대량 실패)를 명시하며 `[ ]` 유지. 섹션 7 "다음 액션"을 신규 블로커 3건(CI 대량 실패 원인 조사, Slack 시크릿 등록, README.md 작성 여부 결정) 및 기존 블로커(세션 타임아웃 확정) 중심으로 전면 교체. 섹션 1 "기준 문서 및 버전" 표의 `project-prd.md` 버전을 v1.2 → v1.3으로 동기화(project-prd.md가 이번에 Task5로 v1.3으로 갱신되었으므로). 이번 갱신은 섹션 1(project-prd.md 버전)·섹션 2(Phase 3 행)·섹션 3(Phase 3 Task 목록·산출물)·섹션 5(블로커 표)·섹션 6(Phase 3 완료 조건)·섹션 7(다음 액션)·본 변경 이력 항목에 한정되며, Phase 0/1/2 관련 내용과 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨.

- **v1.8 (2026-07-27)**: v1.7 섹션 5에 "(신규)" 원인 미확정 블로커로 남아있던 GitHub Actions CI 대량 테스트 실패(run_id 30201769656, chrome/firefox 각 16 failed/2 passed/2 errors) 이슈에 대한 실제 조사를 완료하고 그 결과를 반영. 이번 세션에서 코드 변경 2건을 커밋·푸시함: (1) `conftest.py`의 `pytest_runtest_makereport` 훅을 `report.when in ("call", "setup")`으로 확장해 픽스처 setup 단계 실패(예: `logged_in_user` 픽스처를 쓰는 `test_logout.py`)도 스크린샷 캡처 대상에 포함시키고, 실패 시점 `driver.page_source`를 동일 베이스 파일명의 `.html`로 함께 저장하도록 개선. (2) `.github/workflows/test.yml`에 진단 전용 스텝 2개 추가 — "Check site reachability(diagnostic only)"(`curl`로 순수 HTTP 레벨에서 `automationexercise.com/login` 응답 코드 확인, `|| true`로 실패해도 빌드를 깨뜨리지 않음), "Upload failure screenshots and page source"(`screenshots/` 디렉터리를 `screenshots-${{ matrix.browser }}`로 CI 아티팩트 업로드, `if: always()`, `if-no-files-found: ignore`). 테스트 로직·타임아웃·로케이터는 변경하지 않음(진단 장치만 추가). 이 변경을 커밋 `1c077b1`(기존 미푸시 상태였던 `bf3dd32`, `39e10ef`과 함께)로 push한 뒤 실제 GitHub Actions를 재실행(run_id `30245294664`)해 확인함: curl 스텝은 chrome/firefox 두 job 모두 HTTP 200(러너 IP가 순수 HTTP 레벨에서 차단되는 것은 아님), pytest 결과는 chrome **18 passed, 2 failed**(128.65s)/firefox **18 passed, 2 failed**(192.90s)로 `test_login.py`·`test_logout.py`·`test_product_search.py`·`test_shopping_cart.py`·`test_smoke.py`는 전부 PASS했고, 원래 관찰됐던 "로그인 폼 입력창부터 못 찾는" 대량 실패 패턴은 전혀 재현되지 않음. 남은 실패는 두 브라우저 모두 `test_signup.py`의 2개 테스트뿐이었고, 다운로드한 스크린샷/페이지소스 아티팩트를 직접 확인한 결과 실패 시점 페이지는 정상적인 Automation Exercise 사이트 화면(실제 Cloudflare Beacon 스크립트, 실제 Google 광고 iframe 포함 — 봇차단/챌린지 페이지 아님)이었고, 화면에 실제 인라인 에러 메시지 `Email Address already exist!`가 있었음. **조사 결론**: curl HTTP 200 + 재실행 18/20 PASS + 봇차단 아닌 정상 페이지 렌더링이라는 세 가지 근거로 "GitHub Actions 클라우드 IP가 구조적으로 차단당한다"는 기존 가설(v1.7에 "CI 환경 고유 이슈로 추정"이라 기술됐던 부분)은 근거가 약화되었고, 오히려 그 시점 automationexercise.com 쪽의 일시적 이슈(부하/장애 등)였을 가능성 쪽으로 무게가 옮겨감. 다만 이번 1회 재실행만으로 재발 가능성을 100% 배제할 수는 없어 "원인 미확정"에서 "재현 안 됨/구조적 문제 가능성 낮음"으로 판단을 조정했을 뿐 완전히 종결하지는 않음. 진단 장치(curl 스텝, screenshots/page_source 아티팩트 업로드)가 코드에 남아있어 재발 시 즉시 재조사 가능. **조사 과정에서 원래 이슈와는 별개인 신규 문제를 발견**: `test_data/accounts.json`의 회원가입 테스트 고정 이메일(`newuser011@example.com`, `newuser012@example.com`)이 과거 실행에서 이미 실제 공개 사이트(automationexercise.com)에 등록되어 남아 있어 `Email Address already exist!` 충돌이 발생하는 테스트 데이터 격리 문제. 계정 정리(`pages/delete_account_page.py`)는 테스트가 끝까지 성공한 경우에만 같은 테스트 내에서 수행되는 구조라, 과거 중간 실패 실행이 만든 계정이 정리되지 못하고 남아 발생한 것으로 원인은 파악되었으나, 사용자가 "추후 수정"으로 명시적으로 결정해 **이번 세션에서는 수정에 착수하지 않음**. 이 근거로 섹션 2(Phase 3 행 진행률·완료조건 요약), 섹션 3(Task4 `.github/workflows/test.yml` 최종 점검 항목 및 "Phase 3 산출물" 목록에 진단 장치 반영), 섹션 5(기존 CI 대량 실패 블로커 행을 조사 결과로 갱신 + 이메일 중복 신규 블로커 행 추가), 섹션 6(Phase 3 완료 조건의 "Firefox PASS"·"CI 정상 실행 확인"·"§14 성공 기준 최종 점검" 3개 항목 설명 갱신, 여전히 미체크 유지), 섹션 7(1번 항목을 조사 완료로 갱신, 이메일 중복 해결을 신규 최우선 액션으로 추가, 기존 항목 번호 재정렬)을 갱신함. **주의**: 신규 발견된 이메일 중복 문제 자체의 코드 수정은 이번 갱신 범위가 아니며, 문서에는 다음 액션/블로커로만 기록함(착수 완료로 표기하지 않음). Phase 0/1/2와 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨.

- **v1.9 (2026-07-28)**: v1.8 섹션 5·7에 "추후 수정으로 보류"로 남아있던 회원가입 테스트(`tests/test_signup.py`) 이메일 중복 문제를 실제로 수정하고 검증한 결과를 반영. 코드 변경 2건: (1) `utils/helpers.py`에 `generate_random_email(prefix: str = "test") -> str` 신규 추가(uuid4 기반, 실행마다 값이 달라짐). (2) `conftest.py`의 `valid_signup_data`/`relogin_signup_data` fixture를 수정 — JSON(`test_data/accounts.json`)의 name/password/address 등 나머지 필드는 그대로 쓰되 `email` 필드만 `generate_random_email()`로 매 실행 시점에 덮어쓰도록 변경했고, `yield` 이후 `_cleanup_signup_account(driver)` teardown을 추가해 테스트가 중간에 실패해 로그인 상태로 남아도(`HomePage.is_delete_account_link_displayed()`로 확인) 계정 삭제를 한 번 더 시도하는 안전장치를 마련함. 이 teardown은 `TimeoutException`/`ElementClickInterceptedException`(기존에 문서화된 광고 iframe 클릭 가로채기와 동일한 구체적 예외)만 잡아 로그만 남기고 넘어가도록 구현했는데, 최초 구현 시 이 예외 처리 범위가 `TimeoutException`만이어서 teardown 자체가 `ElementClickInterceptedException`으로 에러를 내며 원래 테스트 실패를 "FAILED+ERROR" 이중 표시로 가리는 문제가 실제로 재현되었고, 이를 발견해 예외 처리 범위를 넓혀 수정함. **로컬 검증**: `test_signup.py` 단독 실행 3회 연속 2/2 PASS(수정 전에는 매번 100% 확정적으로 2건 실패했음을 재현 후 확인), 전체 테스트(`tests/`) 20개 실행 시 회귀 없음(광고 관련 클릭/로딩 인터셉트로 인한 간헐적 실패 1건이 한 차례 관찰되었으나 Task3/Task4에서 이미 문서화된 것과 동일한 성격의 플레이키니스이며 재실행 시 즉시 PASS로 재현되지 않음을 확인). 기존에 막혀 있던 `newuser011/012@example.com` 두 계정은 더 이상 테스트가 사용하지 않게 되어 실사이트에서 별도로 정리할 필요가 없어짐. 이 근거로 섹션 1(작성/갱신 이력에 v1.9 요약 추가), 섹션 2(Phase 3 행의 "5건" → "4건"으로 갱신, 항목 (5)를 "해결 완료"로 표기), 섹션 3(Phase 3 Task4 문단 및 "Phase 3 산출물" 목록에 `conftest.py`/`utils/helpers.py` 변경 내역 반영), 섹션 5(이메일 중복 블로커 행을 "(해결 완료, v1.9)"로 갱신), 섹션 6(Phase 3 완료 조건의 §14.2 설명에 로컬 검증 결과 반영), 섹션 7(다음 액션 2번 항목을 "해결 완료"로 갱신)을 갱신함. 별도로 `SLACK_WEBHOOK_URL` 저장소 시크릿 등록 여부를 `gh api repos/erik042156/AT-EX/actions/secrets`로 재확인했으나 `{"total_count": 0, "secrets": []}`로 **여전히 미등록 상태**임을 확인하여 관련 블로커·다음 액션 항목은 변경하지 않고 그대로 유지함. **주의**: 이번 수정은 로컬 검증만 완료된 상태이며, 아직 커밋/푸시되지 않았고 GitHub Actions 재실행을 통한 최종 확인(§14.2 "CI 기준 실패율 0%" 충족 여부)은 남아 있어 관련 항목들을 "완료 조건 충족"으로까지는 표기하지 않음. Phase 0/1/2, 섹션 4(기능 의존성 개요), CI 대량 실패 가설·Firefox 로컬 불안정성·README.md 부재 관련 블로커는 이번 갱신 범위에서 제외되어 그대로 유지됨.

- **v1.10 (2026-07-28)**: v1.9의 이메일 중복 수정(커밋 `30a3db7`)과 로드맵 갱신(커밋 `0ce1023`)을 push한 뒤 실제 GitHub Actions를 재실행(run_id `30346729676`)해 최종 확인한 결과를 반영. **이메일 중복 문제는 해결이 실제로 검증됨**: chrome/firefox 두 job 모두 `test_signup.py` 실패 지점이 더 이상 `/login` 진입 직후 `Email Address already exist!`가 아니라 회원가입 성공(`ACCOUNT CREATED!`) 이후의 후속 단계로 이동했고, `gh run download`로 받은 페이지소스 아티팩트에서 `already exist` 문자열이 0건임을 grep으로 확인함. 이 근거로 섹션 5의 해당 블로커 행을 "(해결 완료, v1.9 → v1.10 CI 검증 완료)"로 최종 확정함. **다만 CI는 여전히 chrome/firefox 모두 2 failed, 18 passed**로 실패 상태이며, 원인은 이메일 중복과 무관한 **새로 발견된 제3자 광고/설문 오버레이 차단** 문제로 확인됨: `gh run view --log`로 확인한 타임아웃 locator(chrome은 `h2[data-qa='account-deleted']`, `a[href='/logout']` / firefox는 `a[href='/logout']`)와 `gh run download`로 받은 실패 스크린샷을 직접 열어본 결과, chrome은 "Powered by Google" 설문조사 전면 모달(질문 1/2, 2/2)이, firefox는 계정 생성 완료 페이지의 "Continue" 버튼을 "Now Withdraw Without Waiting"(Take Profit Trader) 광고 팝업이 정확히 덮고 있어 클릭·대기가 진행되지 못한 것으로 확인함. Task3에서 이미 문서화된 "광고 iframe 클릭 가로채기"와 유사 계열이나 전면 모달 형태는 이번에 처음 관찰되었고, 로컬 3회 연속 실행에서는 재현되지 않아 CI 환경/시점에 따라 노출되는 광고의 성격일 가능성이 있음. 사용자가 "발견한 문제를 로드맵에 업데이트"로 명시적으로 결정하여 **이번 세션에서는 오버레이 처리 로직을 코드로 구현하지 않고 문서화만 수행**함. 이 근거로 섹션 1(작성/갱신 이력 v1.10 요약 추가), 섹션 2(Phase 3 행에 (6) 신규 항목 추가, 미해결 사항 4건 → 5건), 섹션 3(Phase 3 Task4 문단·산출물 목록에 v1.10 CI 재확인 결과 반영), 섹션 5(이메일 중복 행을 최종 해결 완료로 확정 + "광고/설문 오버레이 차단" 신규 블로커 행 추가), 섹션 6(§14.2 설명에 CI 검증 결과 반영, 근거를 이메일 중복 → 광고 오버레이로 교체), 섹션 7(다음 액션 2번을 완료로 갱신, 광고 오버레이 항목을 신규 3번 최우선으로 추가하고 이후 항목 번호 재정렬)을 갱신함. Phase 0/1/2, 섹션 4(기능 의존성 개요), README.md 부재·`SLACK_WEBHOOK_URL` 미등록·CI 대량 실패 가설 관련 기존 블로커는 이번 갱신 범위에서 제외되어 그대로 유지됨.

- **v1.11 (2026-07-30)**: v1.10 섹션 5에 "(신규, v1.10, 최우선)"으로 남아있던 광고/설문 오버레이 블로커를 실제 코드 3커밋으로 수정하고 순차 CI 검증한 결과, 그리고 이 과정에서 사용자가 확정한 Firefox 검증 범위 제외 결정을 반영. **코드 변경 3건**: (1) 커밋 `fa91e03`("🐛 fix: 광고/설문 오버레이가 클릭을 가로채는 문제 해결") — `pages/base_page.py`의 `click_when_clickable()`을 수정해 클릭 직전 JS(`document.elementFromPoint`)로 클릭 좌표를 실제로 가로채는 요소가 있는지 확인(타겟 자신/조상/자손이면 무시하는 안전장치 포함)하고, 가로채는 요소가 있으면 `execute_script`로 JS 클릭을 수행하도록 변경. 없으면 기존처럼 네이티브 클릭을 시도하되 `ElementClickInterceptedException` 발생 시에도 동일하게 JS 클릭 1회를 재시도. 신규 private 메서드 `_get_intercepting_element()` 추가. 로컬 합성 검증(가짜 전체화면 오버레이 주입 후 클릭 성공 확인) 완료. push 후 CI 검증(run_id `30540357346`) 결과 Chrome은 여전히 2 failed, 18 passed였으나 실패 지점이 `account_created_page.py::click_continue_button()` 내부 `wait_for_element_presence` 타임아웃으로 이동했고 클릭 우회 로그가 없어, 클릭 자체는 성공했으나 클릭 *이후*의 대기 단계가 문제임을 확인. `gh run download`로 받은 실패 스크린샷/페이지소스를 직접 확인한 결과 Google AdSense "Vignette"(전면 삽입형) 광고(`position: fixed !important; z-index: 2147483647 !important` 등)가 원인으로 확인됨. (2) 커밋 `5d2d44a`("🐛 fix: 전면 광고 오버레이가 페이지 전환 대기를 막는 문제 추가 수정") — `wait_for_element_presence()`를 단일 `WebDriverWait`에서 전체 타임아웃 예산을 유지한 채 5초 단위 청크 폴링으로 바꾸고, 청크 타임아웃마다 신규 메서드 `_dismiss_full_page_overlay()`로 화면의 50% 이상을 덮는 `position: fixed`/`absolute` 요소를 JS로 제거한 뒤 재시도하도록 수정. 로컬 합성 검증(CI에서 확인된 Vignette 마크업 재현 후 제거 로직 자체의 동작만 검증, 로그인 지연 시나리오 전체 재현은 아님) 완료. 이 시점 로컬 전체 회귀 테스트에서 automationexercise.com 실사이트 자체가 광범위하게 불안정한 현상을 발견했으나(수정 전 커밋으로 되돌려도 동일 재현), 실사이트 측 일시적 문제로 판단해 로컬 검증 대신 CI 검증에 의존하기로 함. push 후 CI 검증(run_id `30543091405`, 최초 실행 + `gh run rerun` 1회 추가): 최초 실행은 Chrome 6 failed·13 passed·1 error로 광범위 실패가 있었으나, 재실행 결과 2 failed·18 passed로 광범위 실패는 사라짐(일시적 불안정성이었음이 재확인됨). `test_signup.py` 2건은 여전히 실패했고 이번엔 `_dismiss_full_page_overlay()`가 제거한 오버레이가 0건이라, 순수 페이지 로딩 지연 가능성 쪽으로 판단을 조정함. (3) 커밋 `67249c2`("🔧 chore: PAGE_LOAD_TIMEOUT을 20초에서 35초로 상향") — `config/settings.py`의 `PAGE_LOAD_TIMEOUT`을 20초 → 35초로 상향(이미 `docs/prd/project-prd.md` v1.4 §11.4에 반영됨). 최종 CI 검증(run_id `30544240887`): **Chrome은 1 failed, 19 passed**로 `test_signup_then_logout_and_relogin`이 안정적으로 통과했고, 로그에서 클릭 우회 로직의 실전 성공 사례(`blocker=<iframe>`가 Delete Account 링크를 가리는 것을 감지해 JS 클릭으로 우회)도 직접 확인됨. 다만 `test_signup_with_valid_info` 1건은 35초로 늘린 뒤에도 여전히 `h2[data-qa='account-deleted']` 대기에서 간헐적으로 타임아웃되어 **완전 해결로 표기하지 않고 "크게 개선됨(2/2 → 1/2 실패로 감소)"으로 정확히 기술함**. 같은 실행에서 **Firefox는 7 failed, 13 passed(19분 9초 소요, 평소 3분 내외)**였고 실패 전부가 `urllib3.exceptions.ReadTimeoutError(read timeout=120)` 등 WebDriver 프로토콜 레벨 무응답으로 페이지·오버레이·테스트 로직과 무관했음(Task3에서 이미 문서화된 로컬 geckodriver 120초 무응답과 동일 계열). 이를 근거로 **사용자가 Firefox 검증을 테스트 범위에서 명시적으로 제외**하기로 결정했고, 이 결정은 `docs/prd/project-prd.md` v1.4(§11.2, §11.4, §15.3, §16.2, §17.1, §18.2)에 이미 반영되어 있음(§11.4의 `PAGE_LOAD_TIMEOUT` 20→35초 문서 동기화도 함께 포함). Firefox 관련 코드(`conftest.py`의 `--browser` 옵션, `.github/workflows/test.yml`의 firefox matrix job) 자체는 이번 세션에서 아직 제거하지 않았으며, 이는 문서 갱신과 별개로 코드 정리가 필요한 작업으로 다음 액션에 남김. 이 근거로 섹션 1(기준 문서 `project-prd.md` 버전 v1.3 → v1.4, 작성/갱신 이력에 v1.11 요약 추가), 섹션 2(Phase 3 행 진행률 약 70% → 약 80%로 상향, 완료/부분완료/보류 항목 수 갱신, Firefox 범위 제외 확정 반영), 섹션 3(Firefox Task를 `[ ]` "부분 완료" → `[x]` "범위 제외로 확정"으로 갱신, CI 최종 점검 Task에 3커밋·순차 CI 검증 결과 상세 추가, Phase 3 산출물 목록에 `pages/base_page.py`/`config/settings.py` v1.11 변경 반영), 섹션 5(광고/설문 오버레이 블로커 행을 "(부분 해결, v1.10 발견 → v1.11 개선)"로 갱신, "크게 개선됨(1/2 실패로 감소)"으로 정확히 기술), 섹션 6("Firefox PASS" 항목을 취소선 처리 후 "해당 없음(범위 제외로 확정)"으로 전환, CI 워크플로우·§14 성공 기준 최종 점검 항목의 §14.2 설명을 Chrome 19/20 결과로 갱신), 섹션 7(다음 액션을 잔여 signup 실패 조사와 Firefox 코드 정리를 신규 최우선 1·2번으로 재정렬, 기존 항목 번호 재조정, Firefox 검증 자체는 확정되어 추가 액션 불필요로 표기)을 갱신함. **주의**: `SLACK_WEBHOOK_URL` 미등록, README.md 부재, 세션 타임아웃 미확정은 이번 세션에서 변경 사항이 없어 그대로 유지됨. Phase 0/1/2와 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨. 실제 코드(`pages/base_page.py`, `config/settings.py`)와 `docs/prd/project-prd.md`는 이번 로드맵 갱신 작업에서 직접 수정하지 않았으며, 이미 별도 세션/커밋에서 반영된 상태를 그대로 인용함.

- **v1.12 (2026-07-30)**: v1.11 섹션 7에 다음 액션 2번("Firefox 관련 코드 정리")으로 남아있던 항목을 사용자의 명시적 요청("Firefox가 검증 범위에서 제외되게 관련 코드도 모두 수정해달라")에 따라 실제로 완료하고, 그 결과 v1.10에서 발견되어 v1.11에서 "1/2 실패로 감소"까지만 개선되었던 광고/설문 오버레이 블로커가 최종적으로 완전히 해결되었음을 확인해 반영함. **코드 변경 1건**: 커밋 `c473106`("🔥 chore: Firefox 크로스 브라우저 코드 제거, Chrome 단일 브라우저로 정리") — `conftest.py`에서 `pytest_addoption`으로 등록했던 `--browser` 옵션 전체와 `FirefoxOptions` import를 삭제하고 `driver` fixture를 Chrome 전용으로 단순화(더 이상 필요 없는 `request` 파라미터도 제거). `.github/workflows/test.yml`에서 `strategy.matrix.browser=[chrome, firefox]`·`fail-fast: false`를 삭제해 matrix 없는 단일 `test` job으로 정리하고, 아티팩트명(`reports-${{ matrix.browser }}` → `reports`, `screenshots-${{ matrix.browser }}` → `screenshots`)과 Slack 알림 문구(`"[${{ matrix.browser }}] 테스트 실패"` → `"테스트 실패"`)에서도 브라우저 접미사를 제거함. 로컬 검증: `python -m py_compile conftest.py` 통과, `pytest --collect-only` 20개 정상 수집, `pytest tests/test_smoke.py` 2개 PASS로 단순화된 fixture 정상 동작 확인, YAML 파서로 workflow 문법 검증 완료. `requirements.txt` 등 다른 파일에는 Firefox/gecko 관련 참조가 원래 없었음을 `grep`으로 재확인함. **CI 최종 검증**: 이 코드 정리 커밋(`c473106`)을 v1.11 반영분 docs 커밋(`ad6fae0`)과 함께 push한 뒤 실제 GitHub Actions로 최종 검증(run_id `30549234555`)함. job 이름이 `test (chrome)`/`test (firefox)`에서 단일 `test`로 바뀌었고, 결과는 **`20 passed in 111.61s`, 실패 0건**. 전체 job 소요 시간도 2분 3초로 안정화됨(기존 Firefox 포함 시 최대 19분+까지 걸리던 것과 대조적). v1.11에서 잔여했던 `test_signup_with_valid_info`의 간헐적 타임아웃도 이번 실행에서는 재현되지 않아, `test_signup.py` 2건을 포함한 20/20 전체 통과로 광고/설문 오버레이 블로커가 **완전히 해결**되었다고 확정함(단일 실행 1회 기준이라 향후 재발 가능성을 100% 배제하지는 않되, 코드 수정이 유지되는 한 재발해도 대응 가능). Slack 실패 알림 스텝은 이번 실행에 실패가 없어 스킵되었을 뿐이며, `SLACK_WEBHOOK_URL` 시크릿 미등록 문제 자체는 실패 시에만 드러나므로 여전히 잠재적으로 남아있고 이번 세션에서 변경하지 않음. 이 근거로 섹션 1(작성/갱신 이력에 v1.12 요약 추가), 섹션 2(Phase 3 행 진행률 약 80% → 약 85%로 상향, 미해결 사항을 4건 → 3건으로 재계산, Firefox 코드 정리 완료 및 오버레이 완전 해결 반영), 섹션 3(Firefox 크로스브라우저 Task와 CI 최종 점검 Task 문단에 v1.12 코드 정리·최종 CI 결과 추가, Phase 3 산출물 목록에 `conftest.py`(단순화)·`.github/workflows/test.yml`(matrix 제거) 반영), 섹션 5(광고/설문 오버레이 블로커 행을 "(해결 완료, v1.10 발견 → v1.11 개선 → v1.12 최종 확인)"으로 갱신, run_id `30549234555`의 20/20 PASS를 근거로 명시), 섹션 6("CI/CD 파이프라인이 매 커밋마다 정상 실행되고 리포트 아티팩트가 저장됨을 확인" 항목을 체크 완료로 갱신하되 Slack 알림 미검증은 별도 추적, Firefox DoD 항목에 코드 정리 완료 메모 추가, "§14 성공 기준 최종 점검" 항목에서 §14.2를 "충족"으로, §14.5를 "부분 충족"으로 갱신하되 §14.4(README.md 부재)는 미충족 그대로 유지해 **Phase 3 전체는 여전히 완료로 표기하지 않음**을 명시), 섹션 7(오버레이 문제·Firefox 코드 정리 항목을 모두 "완료, 추가 액션 불필요"로 정리하고 최우선 자리를 비운 뒤, 남은 정책적 결정 항목(`SLACK_WEBHOOK_URL` 등록, README.md 작성, 세션 타임아웃 확정)을 최우선순위로 재정렬)를 갱신함. **주의**: README.md 부재, 세션 타임아웃 미확정은 이번 세션에서도 변경 사항이 없어 그대로 유지되며, `SLACK_WEBHOOK_URL`은 미등록 상태가 유지되고 실패 알림 자체의 실제 동작은 여전히 검증되지 않아 Phase 3를 ✅ 완료로 표기하지 않음(README.md 부재가 §14.4 미충족의 핵심 근거로 남아 있음). Phase 0/1/2와 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨. 실제 코드(`conftest.py`, `.github/workflows/test.yml`)는 이번 로드맵 갱신 작업에서 직접 수정하지 않았으며, 이미 별도 세션/커밋(`c473106`)에서 반영된 상태를 그대로 인용함.

- **v1.13 (2026-08-01)**: 사용자가 "프로젝트에 이미 Slack hook이 설정되어 있다"고 알려와 확인·검증한 결과와, 그 과정에서 새로 발견한 CI 회귀를 반영함. **(1) 시크릿 등록**: `.claude/hooks/notify-ci-slack.sh`(로컬 hook)는 이미 정상 동작 중이었으나, `.github/workflows/test.yml`의 `Notify Slack on failure` 스텝이 참조하는 GitHub 저장소 시크릿 `SLACK_WEBHOOK_URL`은 `gh api repos/erik042156/AT-EX/actions/secrets`(`total_count: 0`)로 여전히 미등록임을 재확인함. 사용자 승인 하에 `gh secret set SLACK_WEBHOOK_URL --repo erik042156/AT-EX`로 실제 등록 완료(`total_count: 1`로 재확인). **(2) 실패 메시지 상세화**: 사용자가 "실패 시 Slack 메시지가 git 커밋 메시지만 보여줄 뿐 실패 지점을 알 수 없다"고 지적함에 따라, `.github/workflows/test.yml`을 개선함(커밋 `023dd4e`) — `Run pytest` 스텝 출력을 `reports/pytest_output.log`로 저장하고, 실패 시(`if: failure()`)에만 실행되는 "Extract failed test summary" 신규 스텝에서 pytest의 `FAILED ` short summary 줄만 추출해 `GITHUB_ENV`의 `FAILED_TESTS`에 다중행으로 저장한 뒤, `Notify Slack on failure`의 `text`에 포함시킴. 기존 `fields`의 `message`(git 커밋 메시지) 항목은 중복 정보라 제거함. **(3) 검증 중 버그 발견 및 수정**: 검증을 위해 항상 실패하는 임시 테스트 `tests/test_slack_notification_check.py`를 추가해 push했으나(커밋 `480d218`), GitHub Actions의 단일 라인 `run:`이 `bash -e {0}`로만 실행되어(`pipefail` 옵션 미적용) `pytest ... | tee ...`의 종료 코드가 `tee`의 종료 코드(0)로 대체되는 바람에, **pytest가 실제로는 `1 failed, 20 passed`로 실패했음에도 "Run pytest" 스텝이 성공으로 처리되어 `Extract failed test summary`/`Notify Slack on failure`가 전혀 실행되지 않는 버그**를 발견함(run_id `30691136890`). `set -o pipefail`을 명시한 멀티라인 `run:` 블록으로 즉시 수정(커밋 `7c1a74c`)한 뒤 재검증(run_id `30691221750`)한 결과, 두 신규 스텝이 정상 실행되며 `FAILED_TESTS` 환경변수와 Slack 메시지 `text`에 `FAILED tests/test_slack_notification_check.py::test_deliberately_fail_for_slack_notification_check - AssertionError: ...` 문자열이 정확히 포함된 것을 CI 로그로 직접 확인함(**기능 검증 완료**). **(4) 임시 테스트 제거 및 신규 회귀 발견**: 검증이 끝난 뒤 임시 테스트 파일을 삭제해 push했으나(커밋 `6e3fd80`), 예상과 달리 CI가 다시 실패함(run_id `30691294540`) — `tests/test_signup.py::test_signup_with_valid_info`/`test_signup_then_logout_and_relogin`이 `home_page.click_delete_account_link()`(51번 줄) 대기에서 `TimeoutException`으로 실패했고, 우연한 flaky인지 확인하기 위해 `gh run rerun 30691294540 --failed`로 2회 재실행했으나 **매번 동일한 두 테스트가 동일한 지점에서 재현**됨(총 3회 연속). 이는 v1.12(run_id `30549234555`)에서 "완전히 해결"로 확정했던 광고/설문 오버레이 문제와 같은 계열이지만, 그때와 달리 간헐적이 아니라 연속 재현된다는 점에서 사이트 측 광고 패턴이 다시 바뀌었을 가능성이 있는 새로운 회귀로 판단함. AskUserQuestion으로 처리 방향을 확인한 결과 사용자가 "지금은 ROADMAP에 기록만" 하기로 선택해, 이번 세션에서는 원인 조사·코드 수정에 착수하지 않음. 이 근거로 섹션 1(작성/갱신 이력에 v1.13 요약 추가, 최종 갱신일 2026-08-01로 갱신), 섹션 2(Phase 3 행의 진행률 서술과 근거 문단에 v1.13 반영, 8개 항목 서술에 (7)(8) 추가), 섹션 3(Task4 CI 최종 점검 문단과 "Phase 3 산출물" 문단에 v1.13 addendum 추가), 섹션 5(`SLACK_WEBHOOK_URL` 블로커 행을 "해결 완료"로 갱신, `test_signup.py` 오버레이 재발을 신규 블로커 행으로 추가), 섹션 6(`.github/workflows/test.yml` DoD 항목에 v1.13 addendum 추가, §14 최종 점검 항목에서 §14.5를 "충족"으로 상향하되 §14.2를 신규 회귀 근거로 다시 "미충족"으로 되돌림), 섹션 7(다음 액션 1번을 신규 최우선 항목(오버레이 회귀 재조사)으로 교체, 기존 1번(SLACK_WEBHOOK_URL 등록)을 해결 완료 항목으로 이동·재번호, 기존 6번(오버레이 v1.10~v1.12)에 v1.13 재발 사실과 1번 항목으로의 참조 추가)을 갱신함. **주의**: README.md 부재, 세션 타임아웃 미확정은 이번 세션에서도 변경 사항이 없어 그대로 유지됨. 신규 발견된 `test_signup.py` 오버레이 재발 회귀는 원인 조사가 전혀 이루어지지 않은 상태이며, 다음 세션에서 재착수가 필요함. Phase 0/1/2와 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨. 실제 코드(`.github/workflows/test.yml`, `tests/test_slack_notification_check.py` 추가 후 제거)는 이번 세션에서 직접 커밋·push까지 수행함(커밋 `480d218`/`023dd4e`/`7c1a74c`/`6e3fd80`), GitHub 저장소 시크릿 등록(`gh secret set`)도 이번 세션에서 직접 수행함.

- **v1.14 (2026-08-01)**: 사용자가 v1.13에서 도입한 Slack 실패 메시지 포맷(`FAILED <파일::함수> - <예외 전체 메시지>`)을 구체적으로 지정한 새 포맷 — `FAILED` / `실패위치 : example.py ( "203줄" )` / `실패원인 : AssertionError` — 으로 바꿔달라고 요청함에 따라 반영함. **코드 변경**: `.github/scripts/extract_failed_tests.py` 신규 추가(커밋 `a6ccf54`) — pytest 출력(`reports/pytest_output.log`)을 `_____ <테스트명> _____` 헤더 기준으로 테스트별 블록으로 나눈 뒤, 정규식 `^(tests/\S+\.py):(\d+):`로 각 블록에서 테스트 파일 자신의 호출 지점(중첩 호출로 라이브러리 내부까지 들어가기 전, 테스트 코드가 직접 위치한 파일:줄)을 "실패위치"로 추출하고, 정규식 `^E\s+(\S+):`로 첫 예외 표현 줄을 찾아 모듈 경로를 제외한 클래스명만(예: `selenium.common.exceptions.TimeoutException` → `TimeoutException`) "실패원인"으로 추출함. 테스트별로 `FAILED\n실패위치 : <파일> ( "<줄>줄" )\n실패원인 : <예외클래스>` 블록을 생성하고, 여러 테스트가 실패하면 빈 줄로 구분해 이어붙임. 로그 파일이 없거나 파싱 중 예외가 나도 항상 대체 문구를 출력하도록 `try/except`로 감싸 안전장치를 마련함. `.github/workflows/test.yml`의 "Extract failed test summary" 스텝을 `grep "^FAILED "` 방식에서 이 스크립트 호출로 교체함. **로컬 사전 검증**: `gh run download`로 실제 CI 실패 아티팩트(`reports/pytest_output.log`) 2종을 받아 로컬에서 스크립트를 직접 실행함 — (1) run_id `30691294540`의 중첩 호출 실패(Selenium `TimeoutException`, `home_page.click_delete_account_link()` 경유) 로그 → `tests/test_signup.py ( "51줄" )` / `TimeoutException` 정상 추출, (2) run_id `30691221750`의 테스트 함수 내부 직접 `assert False` 실패 로그 → `tests/test_slack_notification_check.py ( "8줄" )` / `AssertionError` 정상 추출. 두 케이스 모두 요청 포맷과 일치함을 push 전에 미리 확인함. **CI 실전 검증**: 커밋 push 후 실제 GitHub Actions(run_id `30691891358`)에서 main HEAD가 여전히 겪고 있는 `test_signup.py` 오버레이 회귀로 실제 실패가 발생했고, 이때 `Notify Slack on failure` 스텝의 `FAILED_TESTS` 환경변수와 Slack 메시지 `text`에 `FAILED\n실패위치 : tests/test_signup.py ( "42줄" )\n실패원인 : TimeoutException`가 두 번째 실패 블록과 빈 줄로 구분되어 정확히 포함된 것을 CI 로그로 직접 확인함(요청 포맷과 완전히 일치, **실제 CI 실패로 기능 검증 완료**). 이번 실행에서 실패 줄이 v1.13 검증 당시의 51번에서 42번으로 달라진 점은, 동일한 오버레이 회귀라도 테스트 실행마다 실패하는 assertion 지점이 달라질 수 있다는 추가 정황으로 섹션 5 블로커 행에 반영함(회귀 자체의 원인 조사는 이번 세션 범위 밖으로 유지). 이 근거로 섹션 1(작성/갱신 이력에 v1.14 요약 추가), 섹션 2(Phase 3 행에 (9)번 항목 추가), 섹션 3(Task4 CI 최종 점검 문단과 "Phase 3 산출물" 문단에 v1.14 addendum 추가), 섹션 5(`test_signup.py` 오버레이 재발 블로커 행에 v1.14 관찰 내용 추가), 섹션 7(다음 액션 4번에 v1.14 포맷 개선 내용 추가)을 갱신함. **주의**: README.md 부재, 세션 타임아웃 미확정, `test_signup.py` 오버레이 회귀의 근본 원인은 이번 세션에서도 변경 사항이 없어 그대로 유지됨(원인 조사는 여전히 다음 세션 과제). Phase 0/1/2와 섹션 4(기능 의존성 개요), 섹션 6(Definition of Done)은 이번 갱신 범위에서 제외되어 그대로 유지됨(이 변경은 알림 메시지의 표현 방식 개선으로, DoD의 실패율·성공 기준 판정 자체에는 영향을 주지 않음). 실제 코드(`.github/scripts/extract_failed_tests.py` 신규, `.github/workflows/test.yml` 수정)는 이번 세션에서 직접 작성·커밋·push까지 수행함(커밋 `a6ccf54`).

- **v1.18 (2026-08-03)**: Phase 3의 마지막 미해결 사항이던 "세션 타임아웃 값 확정"(project-prd.md §19.1)을 해소함. 사용자가 세션 타임아웃 조사부터 진행해달라고 요청함에 따라, Playwright MCP로 `automationexercise.com`을 직접 조사함(CLAUDE.md §6 절차 준수). **조사 방법**: (1) `/login` 접속 직후(로그인 전) 쿠키 전체를 확인 — `automationexercise.com` 도메인에는 `csrftoken`만 있고 `sessionid`는 없음(로그인 전에는 세션 쿠키 미발급). (2) 해당 도메인 쿠키를 초기화한 뒤 미리 준비된 테스트 계정(`test_data/accounts.json`의 `shared_login_accounts.valid_account`)으로 실제 로그인 수행. (3) 로그인 클릭 직전 시각(Unix epoch `1785748031`)과 로그인 직후 발급된 `sessionid` 쿠키의 `expires`(`1786957631.791659`)를 직접 비교한 결과 **정확히 1,209,600초(14일)** — Django 프레임워크의 기본 `SESSION_COOKIE_AGE`(2주)와 소수점 단위까지 정확히 일치함(logout.md가 이미 확인해둔 이 사이트의 Django 3.2.8 사용 사실과도 정합적). 로그인 폼에 "로그인 상태 유지" 등 세션 기간에 영향을 줄 수 있는 별도 옵션은 없어 모든 로그인에 동일 적용됨을 스냅샷으로 확인함. **한계 명시**: 이 방식은 브라우저 쿠키 만료 시각 실측 기준으로, 서버 측 idle timeout이나 세션 스토어 만료 존재 여부, 고정 만료인지 매 요청마다 갱신되는 sliding expiration인지까지는 완전히 배제하지 못함. **정책 결정**: 값 확정 후 "이 값을 근거로 logout.md의 '세션 만료 후 로그아웃 시도' 시나리오를 자동화할지"를 AskUserQuestion으로 물었고, 사용자는 **"값만 문서에 확정하고 자동화는 영구 제외"**를 명시적으로 선택함(사유: 14일 실제 대기 E2E가 CI에서 비현실적이고, 쿠키 조작으로 만료를 시뮬레이션하는 대안도 logout.md의 기존 LOGOUT-REQ-005 "세션/쿠키 값 자체는 검증 대상 아님" 원칙과 배치되어 채택하지 않음). **PRD 반영**: 이 결정을 프로젝트 관례에 따라 `automation-prd-writer` 서브에이전트에 위임해 정식 반영함 — `docs/prd/project-prd.md`(v1.4 → v1.5) §19.1의 "세션 타임아웃 시간은?" 항목을 취소선 처리 후 확인 완료로 갱신, `docs/prd/features/logout.md`(v1.4 → v1.5) §9 흐름2("값 미확정이라 제외" → "값은 확정되었으나 영구 제외로 재작성")·§12(자동화 제외 범위 표)·§20(확인 완료 항목에 실측 근거 기록)·§22(다음 단계 항목 종료 처리)를 갱신함(기존 "브라우저 뒤로가기 흐름" 제외 결정과 동일한 톤의 영구 스코프 결정으로 기술). 이로써 **Phase 3의 8개 항목이 전부 완료되어 Phase 3 전체가 최종적으로 ✅ 완료로 확정됨**. 이 근거로 섹션 1(기준 문서 `project-prd.md` v1.4→v1.5, `logout.md` v1.4→v1.5, 작성/갱신 이력에 v1.18 요약 추가), 섹션 2(Phase 3 행을 🟨 진행중 약 95% → ✅ 완료 100%로 갱신), 섹션 3(Phase 3 "로그아웃 세션 만료 관련 시나리오 자동화 검토" Task를 `[ ]` → `[x]`로 갱신, 최종 결론 반영), 섹션 5(세션 타임아웃 미확정 블로커 행을 해결 완료로 갱신), 섹션 6(Phase 3 완료 조건 마지막 두 체크박스(검토 결과 및 §14 최종 점검)를 `[x]`로 전환), 섹션 7(다음 액션 2번을 해결 완료로 갱신, 상단 요약 문단을 "Phase 3까지 전부 완료, 신규 액션 없음, 다음 단계는 사용자 결정 필요"로 교체)을 반영함. **주의**: `project-prd.md`·`logout.md` 실제 파일 변경은 automation-prd-writer 서브에이전트가 이번 세션에서 직접 수행함(README.md와 함께 아직 커밋/push 전). Phase 0/1/2와 섹션 4(기능 의존성 개요)는 이번 갱신 범위에서 제외되어 그대로 유지됨.

- **v1.17 (2026-08-03)**: 섹션 7 다음 액션 1번으로 남아있던 "README.md 작성 여부 및 시점 결정"을 반영함. 사용자가 Phase 3에 남은 두 항목(README.md 작성 vs 세션 타임아웃 확정) 중 무엇부터 진행할지 AskUserQuestion으로 확인한 결과 README.md 작성을 선택함. **문서 신규 작성**: 저장소 루트에 `README.md`를 신규 작성함(프로젝트 개요, 기술 스택 표, 디렉터리 구조, 설치 방법, `.env.example`/`config/settings.py` 근거의 환경변수 설명, `pytest` 실행 방법(전체/개별 파일/개별 함수/스모크/HTML 리포트), 테스트 파일 6개(`test_signup.py`~`test_smoke.py`)별 커버 기능 표, `.github/workflows/test.yml`의 job 체이닝(attempt-1~3) 구조 요약, `CLAUDE.md`/`docs/prd/`/`docs/ROADMAP.md` 참조 링크). 내용은 실제 `requirements.txt`, `pytest.ini`, `.env.example`, `config/settings.py`, `conftest.py`, `.github/workflows/` 구성을 직접 읽어 확인한 값만 반영함(추측 서술 없음). 문서 신규 작성이라 코드 변경이 없어 별도 pytest 재실행은 수행하지 않음. 이 변경으로 v1.7부터 §14.4 미충족의 근거로 반복 언급되던 README.md 부재 문제가 해소되어, **project-prd.md §14(전체 성공 기준) §14.1~§14.5 전 항목이 충족 상태로 전환됨**. 다만 Phase 3에는 §14 충족과 별개로 "로그아웃 세션 만료 시나리오 검토"가 세션 타임아웃 값 미확정(project-prd.md §19.1)으로 여전히 보류 상태로 남아 있어, **Phase 3 전체는 여전히 ✅ 완료로 표기하지 않음** — Phase 3의 남은 미해결 사항은 세션 타임아웃 확정 1가지로 좁혀짐. 이 근거로 섹션 1(작성/갱신 이력에 v1.17 요약 추가, 최종 갱신일 2026-08-03으로 갱신), 섹션 2(Phase 3 행 진행률 약 90% → 약 95%로 상향, 완료 항목 7→8, README.md 완료 반영), 섹션 5(README.md 블로커 행을 "해결 완료"로 갱신), 섹션 6(§14 최종 점검 항목에 v1.17 addendum 추가, §14.4를 충족으로 갱신하되 세션 타임아웃 보류로 체크박스 자체는 미체크 유지), 섹션 7(다음 액션 1번을 완료로 갱신, 2번(세션 타임아웃)을 유일한 남은 액션으로 명시)을 갱신함. **주의**: 세션 타임아웃 미확정은 이번 세션에서도 변경 사항이 없어 그대로 유지됨(대상 사이트의 실제 세션 정책 조사 또는 사용자의 임의 기준 결정이 필요, 여전히 사용자 결정 대기 중). Phase 0/1/2와 섹션 4(기능 의존성 개요), 섹션 3(Phase별 세부 Task 체크박스)은 이번 갱신 범위에서 제외되어 그대로 유지됨(README.md는 원래 로드맵 Task 목록에 명시된 항목이 아니라 §14.4 성공 기준 충족을 위한 별도 문서 산출물이라 섹션 3에는 반영하지 않음). 실제 파일(`README.md`)은 이번 세션에서 직접 작성함(아직 커밋/push 전, 사용자 확인 후 커밋 예정).

- **v1.16 (2026-08-02)**: v1.8에서 진단 장치만 추가하고 "원인 미확정"으로 남겨뒀던 CI 환경(GitHub Actions)의 간헐적 대량 실패(16 failed/2 passed/2 errors 패턴, v1.15에서 재관찰)의 정체를 실측으로 확정하고 대응을 구현한 결과를 반영함. **(1) 정체 확정**: 먼저 push된 커밋 `c5348b0`(pytest 재시도 3회 + 리치빌리티 체크의 챌린지 문구 탐지, `::warning::`)를 대상으로 `gh run rerun`을 최대 10회 반복 실행하며 관찰한 결과, 3회차(run_id `30744291712`, job `91488177524`)에서 실제 재현됨을 확인함 — 리치빌리티 체크의 경고가 실제로 트리거됐고(`##[warning]` 어노테이션), `gh run download`로 받은 실패 스크린샷 전부가 동일 파일 크기(26313 bytes)였으며 직접 열어본 결과 `Please wait while your request is being verified...`(Cloudflare 챌린지 페이지)였음. 이 차단은 job 시작(10:55)부터 종료(11:05)까지 약 10분간 끊김 없이 지속되어, 그 사이 pytest 재시도 3회(각 30초 대기)가 전부 동일하게 `16 failed, 2 passed, 2 errors`로 실패함 — 같은 job(러너)은 egress IP가 고정되므로 step-level 재시도로는 지속형 IP 차단을 극복할 수 없음을 실증함. **(2) 대응 설계 및 구현**: 이 근본 원인(IP 고정)을 직접 겨냥해 사용자와 함께 A(job 재시도)+B(fail-fast 사전 체크) 조합을 설계하고 구현함. `.github/workflows/_run-e2e-tests.yml`(신규, 재사용 워크플로우)로 기존 checkout~pytest~아티팩트 업로드 스텝을 분리하고, `.github/workflows/test.yml`을 `attempt-1`→`attempt-2`(`needs.attempt-1.result == 'failure'`일 때만)→`attempt-3`(notify_on_failure) 3개 job 체이닝 구조로 재편함(각 job이 새 러너=새 IP를 받음, 아티팩트명은 `reports-attempt-N`/`screenshots-attempt-N`으로 분리, Slack 알림·실패 요약 추출은 마지막 attempt-3 실패 시에만 실행). `conftest.py`에는 `pytest_sessionstart` 훅을 추가해 Selenium 드라이버 기동 전 `urllib.request`(표준 라이브러리, 신규 의존성 없음)로 BASE_URL을 가볍게 확인하고 챌린지 문구 감지 시 `pytest.exit(returncode=75)`로 세션을 즉시 중단하도록 함 — 차단 상태에서도 매 재시도가 전체 20개 테스트를 3분씩 완주하던 것을 수 초 내 실패로 단축시켜 job 재시도 사이클을 빠르게 함(커밋 `2fba20a`). **(3) 검증**: 로컬에서 YAML 파서로 문법 검증, `pytest --collect-only`로 정상 사이트 대상 20개 정상 수집(사전 체크가 정상 케이스를 막지 않음 확인), `unittest.mock`으로 챌린지 응답을 흉내내 `pytest_sessionstart`가 실제로 `pytest.exit`를 트리거하는지 단위 검증, `tests/test_smoke.py` 스모크 테스트로 회귀 없음 확인 후 push함. 실제 CI(run_id `30747736911`)에서 happy path(attempt-1 성공 시 attempt-2/3가 `needs.attempt-1.result == 'failure'` 조건에 따라 정확히 skip되어 전체 workflow success)를 확인함. 이어서 추가로 10회 더 `gh run rerun`을 반복했으나(누적 20회) 이번에는 Cloudflare 차단이 재현되지 않아 attempt-1이 매번 성공했고, 따라서 "attempt-1 실제 실패 → attempt-2가 새 러너로 기동해 복구"라는 마지막 한 조각은 라이브로 검증하지 못함. 사용자가 "현재 검증 수준으로 마무리"를 명시적으로 결정함 — job 체이닝의 조건부 skip/실행 로직은 GitHub Actions 표준 `needs.result` 평가 방식이라 구조적으로 신뢰할 수 있고, fail-fast 훅은 단위 검증으로 트리거가 확인된 상태이므로 실제 지속형 차단 상황에서의 end-to-end 자동 복구만 다음 재발 시 자연 검증되는 채로 남김. 이 근거로 섹션 1(작성/갱신 이력에 v1.16 요약 추가), 섹션 2(Phase 3 행에 v1.16 addendum 추가), 섹션 3(Task4 CI 최종 점검 문단과 "Phase 3 산출물" 문단에 v1.16 addendum 추가), 섹션 5(CI 대량 실패 블로커 행을 "근본 원인 규명 + 대응 구현 완료, end-to-end 복구는 미검증"으로 갱신), 섹션 7(다음 액션 4번 갱신)을 반영함. **주의**: README.md 부재, 세션 타임아웃 미확정은 이번 세션에서도 변경 사항이 없어 그대로 유지됨. Phase 0/1/2와 섹션 4(기능 의존성 개요), 섹션 6(Definition of Done)은 이번 갱신 범위에서 제외되어 그대로 유지됨(이 변경은 CI 인프라 안정성 대응으로, DoD의 실패율·성공 기준 판정 문면 자체를 바꾸지는 않음). 실제 코드(`.github/workflows/test.yml` 재작성, `.github/workflows/_run-e2e-tests.yml` 신규, `conftest.py` 확장)는 이번 세션에서 직접 작성·커밋·push까지 수행함(커밋 `2fba20a`).

---

이 문서는 진행 상황이 바뀔 때(착수/완료/블록)마다 갱신됩니다. 요구사항 자체의 변경이 필요한 경우 이 문서가 아니라 `docs/prd/` 하위 PRD 문서를 automation-prd-writer 서브에이전트를 통해 수정해야 합니다.
